# Admin app env (BP §13). Distinct trust domain: NEVER reuse JWT_SECRET here.
# Generate with: openssl rand -base64 32
ADMIN_JWT_SECRET=
# Comma-separated allowlist of admin emails
ADMIN_EMAILS=
RESEND_API_KEY=
RESEND_FROM_EMAIL=
REDIS_URL=redis://localhost:6379
