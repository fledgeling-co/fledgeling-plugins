
## API

Vercel (syd1) via the same rules as web, or the Dockerfile (SWC build — same transform as dev). A persistent deployment schedules its own cron in-process (ScheduleModule) — no CRON_SECRET endpoints for work it can run directly (BP §16).
