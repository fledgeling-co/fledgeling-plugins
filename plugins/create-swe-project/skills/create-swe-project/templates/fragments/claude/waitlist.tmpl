- Waitlist — `/waitlist` + `POST /api/waitlist` implement the referral-loop pre-launch pattern: each signup gets a share code, referrals jump the queue (`lib/waitlist.ts`). Embed `components/waitlist-form.tsx` in the marketing page when launching pre-product. Position ranks by referrals then join order; never fake the counts.
