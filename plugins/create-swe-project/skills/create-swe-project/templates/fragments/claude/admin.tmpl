- `apps/admin/` — admin console at `http://admin.{{CODENAME}}.local` (port {{PORT_ADMIN}}). **Distinct trust domain** (BP §9.5): `ADMIN_JWT_SECRET` + audience `{{CODENAME}}-admin`, email-code login against the `ADMIN_EMAILS` allowlist, 12-hour sessions, server-side guard on every page. Audit-log every admin-initiated mutation (who/when/before/after) in the same transaction (BP §12), and give privileged Server Actions an authz-denial test before "done" (BP §15).
