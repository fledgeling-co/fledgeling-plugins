export default function Home() {
  return (
    <main>
      <h1>{{DISPLAY}}</h1>
      <p>{{DESCRIPTION}}</p>
      <p>
        Scaffolded and ready. Next: <code>SETUP-NEXT-STEPS.md</code>, then triage the first
        feature into <code>docs/features-to-triage/</code>.
      </p>
    </main>
  );
}
