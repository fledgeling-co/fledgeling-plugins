# r01 — material (one edit class)

Sampled off C1 on matched geometry, then authored:

- its slab face is **flat across its width** and lit top-to-bottom (L 0.61 at the
  top rim to 0.33 at the base), where the master had a diagonal ramp. The gel
  gradient's dominant axis became vertical, and the lateral darkening moved into
  its own layer (`gelSide`) rather than being written as a lean on the first —
  a gradient carries one axis.
- its face reads **lighter at both side edges** (0.43 and 0.47 against a 0.37
  body): a translucent body has a transmitted rim all round, not only where the
  key is. The rim ramp now lifts the far edges too.
- its body is **darker overall** (median 0.37 against 0.47): the gel ramp was
  deepened at both ends.
- its seam core peaks at **L 0.79–0.84** against the master's 0.67–0.72: the hot
  stroke at the slab's edge was widened and strengthened.

Every size improved. Composite mean **0.7317 → 0.7479**; 32px self-contrast
0.563 → 0.609. Saturation was deliberately NOT tracked to C1 (its accent sits at
S 0.50, peach; the family's is 0.70–0.90) — the reference is not the ceiling.
