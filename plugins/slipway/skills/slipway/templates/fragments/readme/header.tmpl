# {{DISPLAY}}

{{DESCRIPTION}}

pnpm + Turborepo monorepo, scaffolded by slipway on {{DATE}}. Read `SETUP-NEXT-STEPS.md` first (hosts + Caddy + accounts), then:

```bash
pnpm install
pnpm dev          # all dev servers
pnpm gate         # lint + typecheck + build (the pre-push gate runs this)
```

Local origin: http://{{CODENAME}}.local (shared machine Caddy — see `Caddyfile`).

## What's here
