
## Mobile (Expo — no EAS)

Release builds are **local**: `npx expo prebuild` then Xcode/fastlane with the same ASC key. Expo modules are fine; EAS Build/Submit/Update and expo-updates are never used — updates ship through the App Store.
