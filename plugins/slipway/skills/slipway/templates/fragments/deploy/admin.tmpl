
## Admin (Vercel)

Same rules as web; separate Vercel project with Root Directory = `apps/admin`. Keep `ADMIN_JWT_SECRET` distinct per environment — a user-side compromise must not mint admin tokens (BP §9.5).
