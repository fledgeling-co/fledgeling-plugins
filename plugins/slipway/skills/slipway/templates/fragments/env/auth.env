
# Auth (BP §9) — JWT + refresh rotation; generate secrets: openssl rand -base64 32
JWT_SECRET=
# Resend (BP §8) — verified sender domain
RESEND_API_KEY=
RESEND_FROM_EMAIL=
# Dev login (non-production only; resolved server-side)
DEV_LOGIN_ENABLED=true
DEV_LOGIN_EMAIL=
