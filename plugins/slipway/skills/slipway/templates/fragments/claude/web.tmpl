
- `apps/web/` — Next.js App Router app (port {{PORT_WEB}}, `http://{{CODENAME}}.local`). Server Components by default; every server-side `lib/` file starts with `import 'server-only'` (BP §3). Security headers live in `next.config.ts`; `output: 'standalone'` is on for Docker parity (BP §17). Deploys to Vercel (one project, Root Directory = `apps/web`, `vercel.json` has turbo-ignore). Next.js vendors its full docs at `node_modules/next/dist/docs/` (16.2+) — for Next APIs, those docs are the source of truth over training data.
