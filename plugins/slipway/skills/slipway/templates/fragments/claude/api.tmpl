- `apps/api/` — NestJS API (port {{PORT_API}}, `http://api.{{CODENAME}}.local`). Built AND dev-booted through SWC (`nest build -b swc` / `node -r @swc-node/register`) so dev and prod share one transform (BP §15 — this is what catches circular-import TDZ boot crashes locally). Zod-validate at boundaries (CP §1); the browser never calls this directly — it goes through the web app's route handlers (BFF rule, BP §24).
