- `apps/api/` — NestJS API (port {{PORT_API}}, `http://api.{{CODENAME}}.local`). Built AND dev-booted through SWC (`nest build -b swc` / `node -r @swc-node/register`) so dev and prod share one transform (BP §15 — this is what catches circular-import TDZ boot crashes locally). Zod-validate at boundaries (CP §1); the browser never calls this directly — it goes through the web app's route handlers (BFF rule, BP §24). AI calls go through the Vercel AI SDK via `src/ai.ts` (models in one config, Gateway routing, structured output via Output.object + Zod) — the same convention as apps/web, never raw provider SDKs (BP §6). Platform baseline per dAIolog: global ConfigModule, ThrottlerGuard rate limit on every route, ScheduleModule for in-process cron (BP §16), helmet, global ValidationPipe (whitelist+transform) with class-validator DTOs, OpenAPI served at `/docs` (@nestjs/swagger) — the contract endpoint web and native clients consume.
