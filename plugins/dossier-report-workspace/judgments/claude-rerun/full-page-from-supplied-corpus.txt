WINNER: A

REASON: A treats the five runs as data rather than as five drafts to average: §01 quantifies the overlap (170 source slots, 154 distinct URLs, only 13 shared, and 55 of Gemini's 77 entries unusable search-redirects), every load-bearing claim carries a C/O/G/P/X panel strip, §11 is an eleven-row disagreement ledger with a stated resolution per row, §12 is a 10×5 assert/partial/contest/silent matrix, and all 84 registry entries record which backends cited them — where B's panel exploitation stops at a five-row methods table, four disagreements and a 50-entry registry with no per-source backend provenance. Both pages are internally honest and link-tight (I verified every citation in both resolves bidirectionally: A 171 cite instances across 84 sources, B 81 across 50, zero dangling anchors either way), and both police their own numbers well — A refuses the untraceable 35% vestibular figure and separates the Wikipedia 0.29% reference click-through from the unrelated 31% Page Previews lift, B records its failed three.js gate and flags that its "reached half the page" funnel bar is an inference. With JavaScript off, A loses nothing but an inert motion button: its charts are hand-authored static inline SVG with titles, descs and `<details>` data tables, and every marker is a real `href="#s-N"` anchor into the registry — whereas B's three signature figures are empty `<div>`s behind `<noscript>` notes, its depth gauge never switches on, and its citation markers are `<button>` elements with no href, so the claim-to-source bond it argues for dies with the script. On self-containment A is one file with zero network requests and a full dark-mode palette; B pulls four typefaces from Google Fonts and points og:image at an absolute URL. B's calibration-sheet concept and margin depth gauge are the single best design idea across the two, and its one bounded sticky episode genuinely demonstrates the form it describes, but A's rail-and-marginalia editorial grid, agreement matrix and dual-series attention chart are equally hand-made rather than templated — leaving corpus exploitation, evidence density and no-JS survival to decide it.
