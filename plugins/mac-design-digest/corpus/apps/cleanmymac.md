# CleanMyMac — profile

- **Source:** macapp.supply (MacPaw) · **Surfaces digested:** none (no UI surface supplied) · **Last updated:** 2026-07-19
- **One-sentence identity:** Cannot be stated from the supplied evidence — the source pack contains a marketing cover and the app icon only; no window UI was provided, so this app's interior design is unobserved.
- **Cluster:** unassigned (no UI evidence to place)
- **Lineage:** unknown (low) — undeterminable from an icon; an app icon carries no reliable body-density or control-metric tells, and inferring "native" from prior knowledge of MacPaw would be fabrication. Non-native evidence never feeds macOS canon; here there is no evidence of any kind to feed it.
- **Era (chrome):** unknown for the app UI. The *icon* is **Big Sur-era squircle** (2020 icon grammar: rounded-square background tile + a single foreground object floating on soft shadow) — this is an icon-era marker, not evidence of the app's window/material era.

## Digestion result (honest)

**This is the explicitly-handled "marketing-only cover" case.** Per the skill's cover rule and boundary conditions: the app *window* inside a composite is the design evidence; the backdrop/typography around it is brand evidence. Here there is **no window** — the composite is icon-on-backdrop. The 14-point rubric and 10-point native-tells audit require a UI surface (regions to name in platform vocabulary, controls to measure, selection/toolbar/glass grammar to check) and therefore **could not be run**. No tokens, patterns, lineage, or defects can be sourced for the app's UI. Nothing here is eligible for macOS UI canon.

What *can* be recorded is brand + icon evidence, kept strictly separate from UI canon below.

## Brand & icon evidence (not UI canon — never promoted)

### The app icon (from `icon.png` clean render + as the focal element of `cover.jpg`)
| Dimension | Reading |
|---|---|
| Era | Big Sur squircle (2020), not Liquid-Glass Icon-Composer era — layered object-on-tile with baked gloss/shadow, not multi-layer adaptive glass |
| Background tile | Near-white pale-lavender squircle, subtle top-to-bottom gradient (~#F4F0FA → #ECE8F5) (estimated) |
| Foreground object | Stylized iMac/display: magenta→hot-pink gradient trapezoidal screen in perspective (tilted top edge), on a lavender-grey neck + base; a chrome/silver squeegee-wiper arm laid diagonally across the screen (the "cleaning" metaphor) |
| Base mark | Small circular mono-line "C" roundel (CleanMyMac/MacPaw emblem) centered on the stand |
| Palette | One committed brand hue — hot magenta/fuchsia ~#E0189F–#F52FB0 (estimated) — against restrained pale-lavender neutrals ~#C7C4DE for stand/squeegee; "committed" object color on a "restrained" ground |
| Light model | Top-lit; glossy specular highlight across the magenta screen; soft ambient-occlusion shadow under display and base; skeuomorphic depth via layered soft shadows |

### The cover composite (brand mood)
- Backdrop: heavily gaussian-blurred landscape — pink/lavender hills with pink cherry-blossom-like foliage, hazy mauve horizon and pale sky. Reads as a processed/AI wallpaper-style scene. Monochrome-pink tonal family deliberately matched to the icon's magenta so the whole frame is pink-coherent.
- Composition: the icon is the only sharp, saturated element on an otherwise defocused ground — a Von Restorff focal isolation.
- **No headline typography, no device frame, no UI.** Even brand *type* evidence is absent — this cover carries mood and icon only.

## Layout skeletons
None — no UI surface was supplied.

## Signature moves
- (Icon/brand, not UI) The squeegee-across-a-glossy-magenta-display motif is the whole product metaphor in one object: a "screen" being wiped clean. Systematic to the MacPaw CleanMyMac identity.
- (Brand) Monochrome-pink discipline across icon + backdrop, with the sole sharp focal object floating on a blurred blossom field — the marketing look is "calm spring freshness," a single committed hue carrying the frame.
- **UI signature: unknown** — requires window screenshots to find.

## Defects
- None recordable. UI defects cannot be assessed without a UI surface; asserting any would be fabrication.

## Rubric history
| Surface | Score | Failures |
|---|---|---|
| (none supplied) | n/a | No UI surface present — rubric + native-tells audit not runnable |

## What this app needs to be profiled
Real window captures at @2x: the main scan/dashboard view, the sidebar (module list), and one results/detail surface — light and ideally dark. Those would establish lineage (density is the discriminator), material/glass era, selection grammar, and the token set. A proper icon digest belongs under Workflow B (`icons/cleanmymac.md`), which this Workflow-A pass does not write.
