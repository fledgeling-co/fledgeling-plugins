# Uninstally — profile

- **Source:** macapp.supply (meta.json; GitHub gostonx/uninstally) · **Surfaces digested:** cover.png (marketing composite), icon.png (brand mark) · **Last updated:** 2026-07-19
- **One-sentence identity:** A free open-source Mac app-uninstaller whose *marketing* dresses the CleanMyMac / AppCleaner cleaner-category in a purple-gradient 3D hero — but supplies **no actual app-UI screenshot**, so its native identity is unknown.
- **Cluster:** unassigned (no app-UI evidence to place it)
- **Lineage:** unknown (low) — no window chrome, controls, or density present to classify; marketing tagline claims "Native macOS Experience" but nothing verifies it
- **Era (chrome):** unknown — no window captured

## Evidence status — READ FIRST

**No app UI was supplied.** The only two images are a marketing cover and the app icon. The cover is a composite (dark hero backdrop, wordmark, tagline, feature-icon row, 3D-rendered trash can, a stylized "Uninstall Complete" toast card, trust-badge row) — none of it is a screenshot of the running application. There is no toolbar, sidebar, source list, table, traffic-light cluster, or any native control to measure. Consequently:

- The 14-point rubric and 10-point native-tells audit were **not run** (no UI surface to score — running them on a marketing render would be a category error the persona forbids).
- Lineage and era are **unclassifiable** from this evidence.
- All tokens below are **brand/marketing** evidence, explicitly *not* app-UI tokens, and never feed macOS canon or clusters.
- The icon is noted for brand context only; a full icon digest is Workflow B (out of scope for this pass).

**To digest this app properly, the corpus needs real app-window screenshots** (main window / app list, an in-progress scan, the leftover-files review sheet, and any settings surface).

## Tokens (BRAND / MARKETING evidence only — not app-UI tokens)

| Token | Value | Provenance | Notes |
|---|---|---|---|
| brand/backdrop | near-black indigo, ~#0A0713 center → ~#080610 edges, faint purple vignette + right-side starfield (estimated)(inferred) | | cover hero background, dark |
| brand/accent | violet ~#7C6CE8–#8B7CF0 (estimated)(inferred) | | tagline highlight words, 5 feature line-icons; matches icon gradient |
| brand/icon-gradient | periwinkle top ~#8E82EE → indigo bottom ~#6A4BD8, vertical (estimated)(inferred) | | app-icon squircle fill |
| brand/wordmark | pure white #FFFFFF, geometric/SF-Pro-Display-class sans, tight tracking (estimated)(inferred) | | "Uninstally" set very large |
| brand/toast-card | charcoal ~#1C1C24, ~14–18px corner radius, white primary + gray secondary text, green check ~#34C759-class (estimated)(inferred) | | stylized "Uninstall Complete" render — NOT a captured OS notification |
| brand/glyph | white broom + 3 four-point sparkles on violet (estimated)(confirmed) | | appears in icon AND cover (re-evidenced within one asset set) |

## Layout skeletons

**cover.png (1536×1024) — marketing composite, dark:** Left column (~top-to-bottom): app icon squircle → oversized "Uninstally" wordmark → two-line tagline with violet-highlighted words → a horizontal row of 5 stroke feature-icons with two-line labels (Remove Apps Completely / Find Leftover Files / Safe & Smart Uninstalls / Free Up Space / Blazing Fast), separated by hairline dividers → a "Built for macOS" pill (Apple glyph + label, outlined on dark). Right half: a large 3D-rendered black trash can with the app icon, blue folders, a document, and an Xcode-ish tile spilling out, purple rim-glow; a dark "Uninstall Complete" toast card overlaps its lower edge (green check + headline + secondary body). Bottom band: 3 trust items — Safe & Secure · Optimized for Apple Silicon · Native macOS Experience. A subtle wave divider separates the bottom band. **No app window is present anywhere.**

## Signature moves

- None attributable to the app's UI (no UI seen). At the *brand* layer: a single-hue violet-gradient identity carried consistently from icon → wordmark highlights → feature icons, with one green check as the lone warm/positive accent — disciplined brand palette, but this is marketing, not product design.

## Defects

- **Unverifiable native claim (evidence gap, not a UI defect):** the cover asserts "Native macOS Experience" while supplying zero native-UI evidence. Record as a claim to test, not a fact.
- **Potential feedback-pattern tell (unconfirmed):** the rendered "Uninstall Complete" *toast card* — if it mirrors the real app — would lean on an in-app toast, where macOS favors user notifications / inline confirmation (HIG feedback: toasts used sparingly). Cannot attribute to the shipping app; it is a marketing render. Flagged for verification only.

## Rubric history

| Surface | Score | Failures |
|---|---|---|
| cover.png (marketing composite) | not run | No app-UI window present — rubric/native-audit inapplicable to a marketing render |
