# Coreviz Studio — profile

- **Source:** macapp.supply · **Surfaces digested:** none (marketing cover only — no app UI supplied) · **Last updated:** 2026-07-19
- **One-sentence identity:** An AI "media workspace" for photos/videos whose only public design evidence is a cold-luxury marketing card — a Didone-class serif headline on near-black with a whisper grid — reading like the Vercel/Antler startup-brand register (Vercel's own OG cards, Linear's landing, Family); **the app UI itself is undigested.**
- **Cluster:** unassigned (no UI evidence to place)
- **Lineage:** unknown (low) — no app window, chrome, or control was supplied; classification impossible. Web signals (coreviz.io, a Vercel `og?h=` route generating the cover, "Backed by Vercel") *hint* at a web/Next.js product and therefore a possible web-Electron desktop wrapper, but this is a hint, not evidence, and never feeds macOS canon.
- **Era (chrome):** unknown — no window chrome visible.

## Evidence status

**This entry is brand evidence only.** The single supplied UI-adjacent asset (`cover.png`) is a 1200×630 Open Graph marketing composite with **zero application interface**: no traffic lights, no titlebar/toolbar, no sidebar, no content surface, not even a device frame. Per the skill's boundary rule ("if an image shows no actual app UI, record that honestly and move on"), no 14-point rubric and no 10-point native-tells audit could be run — there is no surface to grade. The `icon.png` asset (monochrome rounded-hexagon aperture mark) is Workflow B and out of this task's scope; noted below as brand context only.

All tokens below are `(estimated)` from a compressed 1200×630 raster and `(inferred)` from one artifact. They describe the **brand layer**, not the product, and must not be promoted or read as macOS taste.

## Tokens (brand layer — marketing cover)

| Token | Value | Provenance | Notes |
|---|---|---|---|
| brand/bg-base | `#141414` (estimated)(inferred) | | near-black field, sampled (20,20,20) |
| brand/bg-vignette | `~#262727` top-left lift (estimated)(inferred) | | subtle radial brightening toward the logo corner |
| brand/grid-line | `~#201E1C`, ~48px cells, near-invisible (estimated)(inferred) | | faint blueprint grid; +12 luma over base, very slightly warm (R>G>B) — a texture whisper, not structure |
| brand/display | high-contrast modern/transitional serif (Didone-adjacent), roman + italic mix, cap-height ~90px on the 630px card (estimated)(inferred) | | "The world's most powerful" roman; "media workspace." **italic** — the emphasis drop is the move |
| brand/display-fill | `#FFFFFF` (estimated)(inferred) | | pure white headline on near-black |
| brand/label-caps | `#747474`, tracked uppercase, ~13px sans (estimated)(inferred) | | "BACKED BY" — neutral mid-gray, SF/Inter-class |
| partner/antler-red | `#E92F3B` (measured-ish)(inferred) | | Antler wordmark red — a **partner logo colour**, NOT confirmed as the app's own accent; do not treat as brand accent |
| brand/logo | monochrome off-white `~#F5F5F5` rounded-hexagon **aperture** mark (outlined variant on cover; filled variant in `icon.png`) (estimated)(inferred) | | geometric "core/lens" motif fitting "Coreviz" |
| media/tile-radius | ~8–10px rounded rects (estimated)(inferred) | | decorative photo thumbnails bleeding in from card edges — signals the product's media subject; not app chrome |

## Layout skeletons

**cover.png — marketing OG card (1200×630, dark), NOT an app surface.** Left-aligned brand stack in the lower-left two-thirds: logo mark (top) → two-line serif headline (dominant) → a row of two tracked-caps trust badges separated by a thin vertical rule. Scattered, slightly-rotated media thumbnails bleed inward from the top and side edges as ambient backdrop. Whisper grid across the full field. No CTA, no nav, no window — it is an OG share card, not a hero.

## Signature moves

- **[brand] The cold-luxury emphasis drop.** A high-contrast Didone-class serif with the second line thrown into italic ("*media workspace.*") on near-black + a whisper grid. This is the current default-model editorial-serif reflex rendered in its **cold (black) variant** rather than the warm-cream default — a deliberate-looking rotation, and the one thing the card is remembered by (Von Restorff: the italic line is the single different thing).

## Defects

- None recordable. There is no UI surface to audit against the rubric or the native-tells checklist. Auditing the marketing card as a graphic artifact would be a category error for this corpus. The only *finding* is provenance: this source yields **no native/UI evidence** and cannot be placed in a cluster or feed canon.

## Rubric history

| Surface | Score | Failures |
|---|---|---|
| cover.png (marketing OG card) | N/A | Not an app UI surface — 14-point rubric and 10-point native audit inapplicable; brand composite only |
