# Codex — profile

- **Source:** macapp.supply (`sources/codex/`) · **Surfaces digested:** cover composite only (1600×900) — **no app UI present** · **Last updated:** 2026-07-19
- **One-sentence identity:** OpenAI's terminal-first coding agent wearing a consumer-app face — a glassy blue-violet squircle icon and drenched cool-violet marketing field, but **zero window/UI evidence** to place it against native peers.
- **Cluster:** unassigned — cannot cluster a UI-less source; brand-aesthetic note only.
- **Lineage:** unknown (low) — no application window, toolbar, sidebar, or content surface exists in the supplied image, so framework lineage cannot be classified. The icon uses Apple squircle+glass grammar (a *brand* choice to read as an Apple-platform app), but the product itself is a CLI / cloud coding agent — icon grammar is not UI-body evidence.
- **Era (chrome):** unknown for the app. Icon composition reads **liquid-glass-era** (Big Sur+ superellipse tile, layered translucent gradient blob) — brand evidence, not a UI-material reading.

> **Digest discipline flag:** This entry is built entirely from a **marketing cover** — a photographic backdrop + centered app icon + wordmark. Per skill boundary rules ("if an image shows no actual app UI, record that honestly and move on"), nothing here feeds macOS UI canon, patterns, or clusters. The 14-point rubric and 10-point native-tells audit require a UI surface and were **not run** (no window to measure). All values below are *brand* tokens at `(inferred)` strength from a single compressed PNG. To digest Codex as a mac UI, real window screenshots (toolbar / conversation surface / diff view / settings) are needed.

## Tokens

*(Brand-composite tokens only — sampled pixels from `cover.png`, a compressed marketing PNG. Never promote to UI canon.)*

| Token | Value | Provenance | Notes |
|---|---|---|---|
| brand/field-bloom | `#B8C7FF`–`#BACAFF` periwinkle | (measured)(inferred) | upper-left near-white lavender bloom of the photographic backdrop |
| brand/field-deep | `#3936D6` (right) → `#4F5BF3` (bottom) | (measured)(inferred) | saturated indigo→electric blue-violet; a **drenched** cool field |
| brand/icon-tile | `#F2F2F2` near-white | (measured)(inferred) | squircle app-icon tile, frosted-glass light mode |
| brand/icon-cloud-hi | `#8487FF` lavender | (measured)(inferred) | upper lobe of the cloud/blob glyph |
| brand/icon-cloud-mid | `#4C5CFF` blue-violet | (measured)(inferred) | lower blob gradient stop; glassy translucency |
| brand/icon-prompt | `~#FFFFFF` (edge `#E8F0FF`) | (measured)(inferred) | white `>` chevron + `_` dash forming the terminal prompt glyph |
| brand/wordmark | `#FFFFFF`, geometric-rounded sans, medium-heavy | (estimated)(inferred) | "Codex" — circular bowls (C/o/d/e), rounded terminals, single display weight; face unnamed (SF-Rounded/Circular/Poppins-class) |
| brand/color-strategy | drenched, cool violet-blue, dark-on-color | (estimated)(inferred) | design-craft: "the surface *is* the color"; ~full-viewport saturation |

**Palette rhyme (context, not canon):** the blue-violet band (`#4C5CFF`–`#3936D6`) sits between macOS-27 system **Blue** (`#0088FF`) and **Indigo** (`#6155F5`) but is more saturated/violet — a brand hue, not the system accent. A native Codex UI would legitimately still bind selection/focus to the *user's* system accent, not this brand violet (native-feel grammar rule 3).

## Layout skeletons

- **Cover composite (marketing, not UI):** centered vertical lockup on a full-bleed photographic field — icon (≈15% of frame height, optically centered) stacked above the wordmark (≈4px-equivalent generous gap). Backdrop is a blurred macro photograph (petal/fabric-like organic wave) graded blue-violet, brightest upper-left, deepening to lower-right. This is a **brand lockup**, not an application layout; no window chrome, traffic lights, sidebar, toolbar, or content region present.

## Signature moves

- **[BRAND, not UI] Cloud + prompt fusion in the app mark:** a soft multi-lobed cloud silhouette (cloud-agent) overlaid with the `>_` terminal prompt (CLI heritage) — the product's dual "cloud coding agent + terminal" nature compressed into one glyph. Systematic within the mark, purposeful, memorable. Recorded as an **icon/brand** signature; it is *not* a UI signature and cannot be promoted from a single UI-less source.

## Defects

- No UI-level defect assessable — there is no interface to fault. The only *corpus* observation is a coverage gap: this source yields **brand evidence only**, so Codex remains undigested as a macOS UI.

## Rubric history

| Surface | Score | Failures |
|---|---|---|
| cover composite (marketing) | n/a — not run | 14-point rubric & 10-point native audit require an app-UI surface; the supplied image contains none. Not a pass, not a fail — **out of scope for UI scoring.** |
