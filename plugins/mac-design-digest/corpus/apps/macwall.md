# MacWall — profile

- **Source:** macapp.supply (cover.jpg + icon.png only; `shots: []` — no app UI supplied) · **Surfaces digested:** none (marketing cover carries no app window) · **Last updated:** 2026-07-19
- **One-sentence identity:** Cannot yet be stated for the *app* — the only evidence is a marketing OG card in the current warm-editorial house look (cream ground + serif display + espresso ink, the register of a Craft/Things landing page rather than a native control surface).
- **Cluster:** unassigned (no native UI evidence to place)
- **Lineage:** unknown (low) — no app *body* was supplied; the tagline "native macOS app for cinematic live wallpaper" is marketing copy, not design evidence. Judge lineage from body density/controls, which are absent here.
- **Era (chrome):** unknown — no window chrome, toolbar, sidebar, or content surface present in any input.

## What was actually supplied

1. **cover.jpg** (1200×630) — a social/OpenGraph marketing composite, *not* a screenshot. Anatomy:
   - Left column (brand): app icon (small squircle, top-left) · outline pill badge "macOS" · two-line serif-display headline "Your Desktop Deserves Better." · author lockup (circular avatar + "foundrceo") · solid pill CTA "Try free at macwall.app".
   - Right: a dramatic black-and-white stock/marketing photograph of a MacBook seen **from behind** — closed-ish lid with the glowing Apple logo, erupting in a cloud of white dust/particles. This is the *live-wallpaper metaphor*, not the product: no screen, no app window, no on-screen UI is visible anywhere in the frame.
   - Design conclusion: **zero native-app design evidence.** The rubric and native-tells audit cannot run — there is no macOS surface to score. Everything measurable here is brand/marketing, which by the corpus rules never feeds macOS canon or style clusters.

2. **icon.png** (192×192) — the app icon (a "scene-in-a-squircle": pale cool-white gradient ground, a flat red rising-sun disc, dark blue-grey mountain-range silhouette in front). Noted for context only; a proper icon read belongs to **Workflow B** and is out of scope for this pass. Not digested to `icons/` here.

## Tokens

All tokens below are **brand/marketing** readings from the cover composite — recorded so the synthesis pass has the OG-card palette, and explicitly walled off from macOS canon (no native surface evidenced them). Provenance marks reflect JPEG-sampled values.

| Token | Value | Provenance | Notes |
|---|---|---|---|
| brand/bg | `#F6F6F2`–`#F7F7F3` warm cream/off-white | (measured)(inferred) | OG-card ground, not an app window background |
| brand/ink | `#2C2801` very dark olive-espresso | (measured)(inferred) | headline text AND CTA-pill fill are the same hue — one committed ink |
| brand/cta-text | `#FFFFFF` knockout | (measured)(inferred) | white label inside the espresso pill |
| brand/headline-face | serif display, old-style/transitional (bracketed serifs, moderate contrast) | (estimated)(inferred) | not SF Pro — an editorial book-serif; brand voice, not app type |
| brand/badge | outline pill, cream fill, thin espresso stroke, label "macOS" | (estimated)(inferred) | category chip |
| brand/cta-shape | full pill (capsule), solid espresso fill | (estimated)(inferred) | one primary action, centred bottom |
| brand/avatar | circular, thin ring, dark portrait | (estimated)(inferred) | author attribution "foundrceo" |
| app/* | — none — | — | **no app UI tokens obtainable**; awaiting real screenshots |

## Layout skeletons
- **cover.jpg (marketing OG card, not an app surface):** two-column composite on a cream field. Left ~48% is a top-aligned brand stack (icon → badge → 2-line headline → author lockup) with a floating centred pill CTA below the fold line; right ~52% is a rounded-corner photo card (the MacBook dust shot) vertically centred. Structural note only — no platform-vocabulary regions (no toolbar / source list / content area) exist to name.
- **App surfaces:** none supplied.

## Signature moves
- None attributable to the *app*. The cover's one committed choice worth noting as **brand** (not app) character: a single espresso ink (`#2C2801`) doing triple duty — display type, CTA fill, and badge stroke — against warm cream, which is the disciplined "one committed color" move of the warm-editorial family. It tells us nothing about how the native window looks.

## Defects
- Not assessable — no app surface. (The cover itself commits no rubric defect worth logging against the app; grading a marketing composite against the macOS native rubric would be a category error.)

## Rubric history
| Surface | Score | Failures |
|---|---|---|
| cover.jpg (marketing composite) | n/a — not rubric-eligible | No macOS surface present; 14-point rubric and 10-point native-tells audit both require an app window, toolbar, or content region — none exist in the input. |

## What the corpus needs before MacWall can be profiled
Real app screenshots: the main window (wallpaper gallery/grid + preview), any menu-bar-extra control (this is likely a menu-bar utility given the category), and the settings/preferences surface. Until then MacWall is a **name and a brand card**, not a digestible native design.
