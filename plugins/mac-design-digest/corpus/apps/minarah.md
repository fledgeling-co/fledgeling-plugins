# Minarah — profile

- **Source:** macapp.supply · **Surfaces digested:** none (no app UI supplied) · **Last updated:** 2026-07-19
- **One-sentence identity:** A prayer-notification Lifestyle app whose only supplied assets are a marketing hero and an iOS-style icon — brand reads "confident, serene, approachable" (bold grotesk wordmark + monospace tagline over royal-blue gradient, warmed by a single 3D claymorphic minaret), but no macOS interface is observable.
- **Cluster:** unassigned — no macOS UI evidence to cluster on
- **Lineage:** unknown (low) — no window, chrome, or surface in either image; the icon's rounded-rect + light-stroke shape is a weak iOS-first tell, not proof of the Mac build's lineage. Non-native/brand evidence never feeds macOS canon.
- **Era (chrome):** unknown — no chrome present to date

## Status: MARKETING + ICON ONLY — no UI digested

This record is deliberately not a UI profile. Both inputs were analysed successfully; neither contains an app surface:

- **cover.png (1200×680)** — a marketing hero composite: a diagonal royal-blue gradient backdrop, the white wordmark "Minarah" set large in a bold neo-grotesque sans, a monospace tagline beneath it, and a floating 3D-rendered (claymorphic) minaret with a faint concentric notification-ripple motif behind it. No device frame, no app window, no macOS chrome.
- **icon.png (112×114)** — the app icon: the same minaret + gold crescent on a blue gradient, drawn as a rounded rectangle with a light outer stroke (an iOS icon silhouette, not the macOS Big Sur squircle). Recorded as contrast evidence only; per this task's Workflow-A-only scope it is not digested as an icon profile.

No 14-point rubric or 10-point native-tells audit was scored — 0 in the record means **N/A (no surface to measure)**, not a failed check.

## Tokens

All tokens below are **brand / illustration / icon evidence only**. None describe app UI, and none may be promoted to macOS canon or a style cluster.

| Token | Value | Provenance | Notes |
|---|---|---|---|
| brand/backdrop-gradient | #82C2FE → #246ADF, diagonal royal-blue | (measured)(inferred) | cover backdrop, light→deep top-left to bottom-right |
| brand/wordmark | white #FFFFFF, bold neo-grotesque sans, tight tracking, ~130px cap height | (estimated)(inferred) | Helvetica-Now / Inter-Display / SF-Pro-Display-Bold class; double-story a |
| brand/tagline | white monospace, ~15–16px, wide default tracking | (estimated)(inferred) | typewriter-mono class (SF Mono / JetBrains Mono / IBM Plex Mono); em-dash |
| illustration/minaret-body | #F1D7B4 cream | (measured)(inferred) | 3D claymorphic render (Spline/Blender look) |
| illustration/dome+bands | #4081B6 blue | (measured)(inferred) | dome and accent bands share the brand blue family |
| illustration/crescent | ~#F0A81E gold | (estimated)(inferred) | the one warm accent against an all-blue field |
| icon/bg-gradient | #1785D5 (top) → warm cream where the tower sits | (measured)(inferred) | iOS-style icon; contrast evidence only |

## Layout skeletons

None — no app surface supplied. The cover's own composition (brand, not UI): left-anchored text stack (wordmark over tagline) occupying the left ~55% of the canvas; the 3D minaret floats right-of-centre as the single focal object; faint concentric ripple centred behind it.

## Signature moves

All **brand-level** (marketing), not app-UI signatures:
- **Grotesk-display + monospace-tagline pairing** — a deliberate contrast-axis type pairing; the whole brand voice compressed into one type decision.
- **Single 3D claymorphic focal object over a ripple motif** — the minaret is the mark and the ripple encodes the "get notified" promise pre-attentively.
- **One warm gold crescent in an otherwise all-blue palette** — devotional signal held to a single accent moment.

## Defects

- No UI defects are scorable — zero app surfaces were supplied. The only corpus-relevant observation is that the icon is a rounded-rect with a light outer stroke (iOS shape) rather than a macOS squircle; recorded as a lineage tell, not a defect.

## Rubric history

| Surface | Score | Failures |
|---|---|---|
| cover.png (marketing composite) | N/A | No app UI to score — 14-pt rubric and 10-pt native audit inapplicable |
| icon.png (app icon) | N/A | Icon, not a UI screenshot; out of Workflow-A scope, noted as contrast evidence |

## What would make this a real profile

A single actual app-window screenshot. A prayer-times / per-prayer-notification utility most plausibly ships as a **menu-bar extra** (popover with the five daily prayer rows + next-prayer countdown) and/or a small **settings window** (location, calculation method, notification toggles). Any one of those would let the lineage/era classification and both rubrics run.
