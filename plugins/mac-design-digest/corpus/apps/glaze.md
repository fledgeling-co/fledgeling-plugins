# Glaze — profile

- **Source:** macapp.supply · **Surfaces digested:** none (no app UI supplied — see boundary note) · **Last updated:** 2026-07-19
- **One-sentence identity:** Brand only — an obsidian-and-molten-glass dark identity in the Vercel/Linear register (black field, single white geometric wordmark, rendered liquid-chrome hero); the actual macOS app UI is unseen and unassessable.
- **Cluster:** unassigned (no UI evidence — cannot place a UI cluster; brand sits near an "obsidian-glass dark-brand" family that is *not* a macOS-UI cluster)
- **Lineage:** unknown (low) — **no app window in either input.** A brand composite and an icon carry zero framework tells (body density, control heights, menu bar, sidebar grammar). AI app-builder startups of this kind commonly ship Electron, but that is a prior, not evidence — recorded as unknown pending a real screenshot.
- **Era (chrome):** unknown — no chrome present to classify (no traffic lights, toolbar, sidebar, or content surface visible).

## Boundary note (why this profile carries no UI tokens)

macapp.supply provided exactly two images and an empty gallery:
- **cover.jpg (2400×1260):** a marketing brand composite — a near-black molten-glass / liquid-chrome rendered backdrop (with faint prismatic dispersion on the ridges) and a centred logomark+wordmark lockup. **No application window, chrome, or content is present.** This is *brand* evidence, not *design* evidence of the app's UI.
- **icon.png (512×512):** the app icon (digested elsewhere under Workflow B if/when run; noted here for brand context only).

Per the skill's cover rule and boundary conditions: the app WINDOW inside a composite is the design evidence — here there is none. So this file records the brand reading honestly, marks everything `brand:` (walled off from macOS canon and UI clusters), and asks for real screenshots. **No lineage, era, rubric, native-tells audit, UI tokens, layout skeletons, or UI signature moves can be produced from these inputs.**

## Tokens (brand only — NOT app-UI tokens; excluded from macOS canon)

| Token | Value | Provenance | Notes |
|---|---|---|---|
| brand/bg | `#000000`–`#060606` near-black (measured)(inferred) | | cover field + icon squircle background both sample pure/near black |
| brand/wordmark | `#FFFFFF` pure white (measured)(inferred) | | "glaze" set in a single-weight geometric grotesk with rounded terminals (a/e/z) — Vercel-adjacent but warmer/rounder; custom or customised |
| brand/logomark | white rounded-square, concave 4-lobed cutout + central diamond hole (measured)(inferred) | | reads as aperture / node / molecule; silhouette echoed by the icon's 4-lobed blob |
| brand/texture | rendered liquid-chrome / obsidian glass, faint chromatic-aberration edges (estimated)(inferred) | | 3D fluid render, not a CSS/material effect; incidental prism dispersion, not a committed accent hue |
| brand/icon-accent-warm | `#EA7529` amber core (measured)(inferred) | | icon molten-glass core; only saturated colour in the identity |
| brand/icon-accent-cool | `~#3A46A0` cobalt/violet iridescence (estimated)(inferred) | | icon glass edges; estimated from a 512px PNG, compressed |
| brand/accent(committed) | none | (inferred) | brand carries no committed UI accent colour — identity is black + white + one rendered material; the system accent would govern any real app UI |

## Layout skeletons
None — no app surface supplied. Cover is a centred brand lockup on a full-bleed dark hero field (logomark left, wordmark right, optically centred), which is composition of a *marketing image*, not an app layout.

## Signature moves
- **(brand, not UI)** The entire identity is carried by one rendered material — obsidian/molten glass — against pure black with a single white geometric wordmark. Restraint everywhere except one high-production 3D render. This is a *brand* signature; whether it reaches the app UI is unknown.

## Defects
- None assessable — no UI to audit. (The absence of app screenshots in the source set is a *corpus* gap, not an app defect.)

## Rubric history
| Surface | Score | Failures |
|---|---|---|
| — (no UI surface supplied) | n/a | Marketing cover + icon only; gallery empty. 14-point rubric and 10-point native-tells audit require an app surface — neither input qualifies. |

## What would make this digestible
One or two real screenshots of the Glaze macOS app window (main window + any settings/empty-state). With those, lineage (native vs Catalyst vs Electron — the AI-builder prior suggests testing for Electron tells first), era, density, and the full rubric become answerable.
