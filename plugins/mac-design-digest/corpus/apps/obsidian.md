# Obsidian — profile

- **Source:** macapp.supply · **Surfaces digested:** none (cover is a marketing composite; no app UI window supplied) · **Last updated:** 2026-07-19
- **One-sentence identity:** A markdown/PKM heavyweight whose *shipped* Mac design evidence is, as supplied, brand-only — a purple crystal glyph on graphite over a black vignette — with no window UI to read; on external knowledge it is an Electron/CodeMirror app (Linear's dark-purple palette register, but that's inference, not observed).
- **Cluster:** unassigned (no native UI evidence to cluster on)
- **Lineage:** web-electron (low confidence) — inferred from external product knowledge + a cross-platform identical icon tile, NOT from any observed UI. Contributes zero evidence to macOS canon or clusters.
- **Era (chrome):** unknown — no window chrome present in any supplied image.

## What was actually supplied

- `cover.png` (1200×688, SHA ba1a1301) — a **marketing composite**, not an app screenshot: pure-black backdrop with a soft radial vignette and fine film-grain noise, the app icon at left, the "Obsidian" wordmark, and the tagline "Sharpen your thinking." No app window, no toolbar, no content region. This is **brand evidence only**.
- `icon.webp` (204×204, SHA c380460b) — the app icon (same artwork shown in the cover). Recorded here as brand context; per this task's Workflow-A-only scope it is **not** written up as a full icon digest (no `icons/obsidian.md`).

> Honest boundary: because no UI surface exists in the inputs, the 14-point rubric and the 10-point native-tells audit **cannot be run** — there is nothing native to score. Everything below is brand/icon reading, marked as such, and feeds nothing into macOS canon.

## Tokens (brand evidence — marketing composite, not UI)

| Token | Value | Provenance | Notes |
|---|---|---|---|
| brand/backdrop | `#000000` pure black w/ radial vignette + film-grain noise | (estimated)(inferred) | composite backdrop, not a window background |
| brand/wordmark-color | `#FFFFFF` | (measured)(inferred) | "Obsidian" wordmark |
| brand/wordmark-type | heavy geometric/neo-grotesque sans, ~Bold/Heavy, near-circular `O`, double-story `a` | (estimated)(inferred) | custom-ish brand face; **not** SF Pro — brand type, not UI type |
| brand/accent-purple | `#7C5CFF`–`#8B5CF6` range | (estimated)(inferred) | tagline colour; the Obsidian brand violet |
| brand/tagline | "Sharpen your thinking." | (measured)(inferred) | ⚠ differs from meta.json tagline ("The free and flexible app for your private thoughts.") — marketing variance, noted |
| icon/tile-shape | rounded-square / superellipse (iOS-family squircle) | (estimated)(inferred) | cross-platform identical tile, not a mac-specific scene icon |
| icon/tile-bg | graphite gradient ~`#2B2B2E` → `#1A1A1C` | (estimated)(inferred) | near-black charcoal, subtle top-lit gradient |
| icon/glyph | faceted purple "obsidian crystal" — lavender highlight facet top-left `~#C9B3F5`, mid-violet body, deep-violet base `~#5B2FBF`→`#7C4DFF` | (estimated)(inferred) | 3–4 flat facets + a pebble-round bottom lobe; internal light-model is flat-shaded, not glossy |

## Layout skeletons

- **cover.png** — Marketing composite, single row: icon block (left ~third), then a left-aligned two-line text stack (wordmark over tagline) occupying the centre; large empty right third carrying the vignette falloff. No app regions (no toolbar / sidebar / content) to name in platform vocabulary — there is no window.

## Signature moves

- **Brand only:** the purple faceted-crystal glyph on graphite is the entire identity — a literal "obsidian gemstone" metaphor rendered as flat-shaded facets rather than glossy 3D. Distinctive as a mark; says nothing about the app's UI craft.
- No *UI* signature can be claimed — no UI was supplied.

## Defects

- None assessable — no UI surface. (The tagline/meta.json mismatch is a marketing-copy inconsistency, not a UI defect.)

## Rubric history

| Surface | Score | Failures |
|---|---|---|
| (none — cover is a marketing composite, no app UI) | n/a | 14-pt rubric and 10-pt native audit not runnable: no native surface present |

## For the synthesis pass

- Do **not** promote anything from this profile: zero UI evidence, and the lineage read (web-electron) is external-knowledge inference, not observed — it must not seed native canon or clusters.
- If real Obsidian window screenshots arrive later (main editor, settings, command palette, graph view), this profile can finally be given a lineage classification from the *body* and scored. Until then it is a brand/icon placeholder.
