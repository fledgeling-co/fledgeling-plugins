---
title: "Building a Drift-Resistant macOS Design Corpus From Primary Sources"
run_id: dr_8e0658af6abce024
question: "Three linked questions for a skill that maintains a persistent, machine-written design-knowledge corpus across many separate AI sessions, by digesting macOS application screenshots and official Apple UI kit files.\n\n(1) MACOS DESIGN LANGUAGE AS PRIMARY SOURCE: What are the authoritative, citable primary sources for the current macOS design language (Apple Human Interface Guidelines, Apple developer documentation, WWDC session material, Apple Design Resources UI kits) as of 2026 — specifically the material properties and usage rules of the current translucency/\"Liquid Glass\" material system, control metrics and type ramps, concentric corner-radius rules, sidebar and toolbar specifications, and how Apple's own published values are versioned so a claim can be dated. Which values are actually SPECIFIED in Apple's published resources versus which are only measurable from renders? Name the exact documents and their dates.\n\n(2) DESIGN FILE FORMAT STRUCTURE: How is the Sketch (.sketch) file format structured for programmatic extraction — the ZIP layout, document.json / meta.json / pages/*.json schemas, sharedSwatches, layerTextStyles, layerStyles, symbol naming conventions, and the encoding of corner radii including how fully-rounded (\"capsule\") radii are represented as sentinel float values. What is reliably extractable versus what requires rendering? Same question for Figma: what .fig files do and do not permit, and what the Figma REST API exposes about a published UI kit. Cite format documentation, open-source parsers, and reverse-engineering write-ups.\n\n(3) MACHINE-WRITTEN KNOWLEDGE CORPUS MAINTAINED ACROSS SESSIONS WITHOUT DRIFT: What evidence-backed techniques exist for keeping a knowledge base written incrementally by a language model correct and internally consistent over many sessions? Cover: provenance/confidence typing of individual facts and promotion rules between confidence levels; corroboration thresholds (how many independent sources before a claim is treated as established) and how independence is defined and defended against correlated sources; deduplication and content-addressed identity of ingested evidence; schema validation and machine-checkable invariants over a written knowledge store; detection of model-introduced drift, contradiction and confabulation accumulating across generations of self-read-and-rewrite; the documented failure mode where a model trained or conditioned on its own prior output degrades; and how knowledge-base and truth-maintenance systems handle contradiction without destroying the losing evidence. Include known failure modes and negative results, not only techniques.\n\nBound to 2023–2026 for (3) and current-version for (1) and (2). EXCLUDE: icon design and icon aesthetics surveys (owned elsewhere); general prompt engineering; general RAG retrieval-quality tuning except where it speaks directly to corroboration thresholds or provenance."
provider: perplexity
model: sonar-deep-research
tier: fast
archetype: technical
sources: 19
tools: [google_search, url_context, code_execution]
estimated_cost_usd: 2.00
completed: 2026-08-18T01:52:04.103Z
---
# Persistent macOS Design Knowledge from UI Kits and Screenshots: Sources, File Formats, and Drift-Resistant Machine-Written Corpora

Maintaining a persistent, machine-written macOS design-knowledge corpus that digests application screenshots and official UI kit files requires anchoring every fact in authoritative primary sources, extracting structural semantics from design file formats, and engineering a knowledge system that resists drift as language models iteratively read and rewrite their own outputs. With High confidence, Apple’s Human Interface Guidelines, technology overviews, API documentation, WWDC session materials, and official Apple Design Resources UI kits form the core authoritative sources for the current macOS design language, including the Liquid Glass material system, window hierarchy, and platform behaviors.[1][4][11][17] With Medium confidence, some quantitative values such as exact corner radii and control metrics are only recoverable by measurement from UI kits or screenshots, because Apple’s public documentation specifies qualitative behaviors and component roles more often than pixel metrics.[1][4][11] With High confidence, Sketch’s ZIP-and-JSON file format and Figma’s REST API, combined with their schemas and open-source parsers, provide reliable programmatic access to layer structures, styles, and symbol hierarchies, though certain visual properties (for example, rendering of vibrancy or platform-native blur) still require a rendering pipeline to fully resolve.[5][8][13][14][15] With Medium confidence, public documentation and reverse-engineering sources establish most aspects of Sketch’s JSON structure, including shared styles and symbol naming, whereas details such as capsule-radius sentinels for fully-rounded corners remain insufficiently documented and must be inferred or empirically validated on sample files.<INSUFFICIENT_EVIDENCE>[exact numeric sentinel values for capsules in Sketch]</INSUFFICIENT_EVIDENCE>[13][15][19] With High confidence, the period 2023–2026 saw the emergence of an evidence-backed understanding of “model collapse” and self-pollution in generative models, along with renewed interest in truth-maintenance systems and provenance-aware knowledge bases, providing concrete techniques—provenance typing, corroboration thresholds, content-addressed storage, schema validation, and contradiction-preserving TMS architectures—to keep an incrementally model-written corpus internally consistent and resistant to degradation.[9][10][16]  

## Authoritative Sources for the Current macOS Design Language

### Overview of Apple’s macOS Design Language Ecosystem

With High confidence, Apple’s macOS design language is documented across four primary channels: the Human Interface Guidelines (HIG), technology overview documents, API references, and WWDC session videos, supplemented by official UI kits in Apple Design Resources.[1][3][11][17] The Human Interface Guidelines provide narrative and prescriptive guidance for interface patterns, materials, navigation, and platform conventions across Apple platforms, including macOS.[11] The technology overview for Liquid Glass describes the new dynamic material system that unifies design language across platforms and explains conceptual design principles and implementation guidance for developers.[17] AppKit API documentation, such as NSGlassEffectView, NSBackgroundExtensionView, and NSVisualEffectView.Material, specify how to adopt Liquid Glass and other materials in macOS apps, including platform versioning and configuration behaviors.[2][12][18] WWDC session materials—particularly the “Build an AppKit app with the new design” talk from WWDC 2025—provide detailed, time-stamped guidance on how macOS windows, toolbars, sidebars, and content regions are restructured around the glass material in recent releases.[4] Apple Design Resources, notably the macOS UI kits, deliver downloadable Sketch or Photoshop templates that encode concrete component metrics and layouts as design files rather than prose, making them primary sources for measurement of control sizes, corner radii, and type ramps.[3]

With Medium confidence, Apple’s ecosystem of documentation is versioned primarily by platform major version and WWDC year, rather than by explicit document publication dates in the snippets visible in the search results.[4][17] For example, the NSBackgroundExtensionView documentation explicitly notes that it is available in macOS 26.0+, tying that API—and therefore the associated background extension design behavior—to a specific OS generation.[18] Similarly, WWDC session titles carry their year in the URL and metadata, such as “WWDC25,” which anchors the session’s guidance to a specific conference year and OS cycle.[4] <INFERENCE from="[WWDC session naming conventions][4][technology overview tying features to OS versions][17][API docs indicating macOS 26.0+ availability][18]">It is therefore reasonable, with High confidence, to treat the OS-version indicators in API docs and the WWDC year in session references as the primary temporal anchors for claims about when particular design language elements entered the macOS ecosystem.</INFERENCE> Apple Design Resources UI kits are similarly versioned by platform (for example, “macOS 27 UI Kit”) rather than explicit dates, indicating the OS generation for which their metrics and styles apply.[3]

With High confidence, among the search results used here, the most authoritative sources for macOS design language are those directly published by Apple on the developer documentation domain and in conference materials. The Human Interface Guidelines are an official Apple resource describing best practices and principles, making them the primary narrative source for design rules and usage guidelines.[11] The Liquid Glass technology overview is an official technology overview by Apple, describing the material’s purpose, platform-wide role, and developer guidance, which makes it a primary source for material semantics and usage patterns.[17] AppKit references like NSGlassEffectView and NSBackgroundExtensionView are official API documents, providing precise technical details and versioning information; these are primary sources for implementation and structural behavior.[12][18] WWDC videos are official engineering presentations from Apple, describing the intended design system and recommended migration strategies; they function as authoritative interpretations and extensions of the written HIG for practitioners.[4] Apple Design Resources UI kits are official design artifacts distributed by Apple; as such, they are the primary empirical sources for measurements and concrete metrics embedded in macOS’s official design language.[3]

### Liquid Glass and the Material System: Properties, Usage Rules, and Specified Versus Measured Values

With High confidence, Apple’s materials guidance establishes Liquid Glass as a dynamic material that visually separates foreground controls and navigation from background content while allowing color and imagery to pass through, thereby creating a sense of depth, layering, and hierarchy.[1][17] The HIG’s materials section explains that materials help visually separate foreground elements such as text and controls from background content and solid colors, and that by allowing color to pass through from background to foreground, materials establish visual hierarchy and a sense of place.[1] The Liquid Glass technology overview frames Liquid Glass as a dynamic material combining the optical properties of glass with a sense of fluidity, unifying design language across Apple platforms and enabling interfaces that establish hierarchy, harmony, and consistency.[17] <INFERENCE from="[HIG materials description][1][Liquid Glass overview stating hierarchy and harmony][17]">Taken together, these documents specify the conceptual role of Liquid Glass: it is not merely a blur effect, but a structural material that forms a functional layer for controls and navigation above content, while maintaining legibility through modulation of blur and luminosity.</INFERENCE>

With High confidence, Apple HIG materials guidance explicitly distinguishes Liquid Glass from standard materials, and prescribes where each should be used.[1] Liquid Glass is described as forming a distinct functional layer for controls and navigation elements such as tab bars and sidebars that floats above the content layer, reinforcing hierarchy.[1] The guidance explicitly warns: “Don’t use Liquid Glass in the content layer,” emphasizing that Liquid Glass works best when it distinguishes interactive elements from content and that using it in the content layer can produce unnecessary complexity and confusing hierarchy.[1] Instead, standard materials are recommended for content-layer elements such as app backgrounds, with an exception for transient controls like sliders and toggles that can adopt a Liquid Glass appearance while active to emphasize interactivity.[1] These prescriptions are conceptual rather than numeric; they define where Liquid Glass is appropriate and where it is forbidden, but do not specify pixel-level metrics. With High confidence, this means that for a machine-written corpus, the presence of Liquid Glass should be treated as a categorical property of regions defined by their functional role (for example, “toolbar,” “sidebar,” “navigation bar”) rather than a measured blur radius or opacity value.[1][17]

With High confidence, Apple provides specific guidance on Liquid Glass variants and their usage contexts, but again in qualitative terms rather than explicit numeric values.[1][17] The HIG materials section notes that Liquid Glass provides two variants, “regular” and “clear,” which can be chosen when building custom components or styling system components.[1] The regular variant blurs and adjusts the luminosity of background content to maintain legibility of text and foreground elements, and scroll-edge effects further enhance legibility by blurring and reducing the opacity of background content; most system components use this variant.[1] The clear variant is highly translucent, prioritizing visibility of underlying content and making visually rich backgrounds like photos and videos remain prominent; this variant is recommended for components that float above media backgrounds to create an immersive experience.[1] Apple further suggests adding a dimming layer behind clear Liquid Glass in certain brightness conditions: a dark dimming layer of 35% opacity is suggested when underlying content is bright, while sufficiently dark content or standard AVKit media controls may not require additional dimming.[1] The only explicit numeric value here is the recommended 35% opacity for the dimming layer in bright contexts, which is a directly specified design parameter in Apple’s documentation, not an inferred measurement.[1]

With Medium confidence, other quantitative properties of Liquid Glass—such as the specific blur kernel size, the exact color-space transformations applied to background content, or the precise translucency curves across different system appearance settings—are not specified in the available HIG and technology overview snippets.[1][17] These properties are implemented by system frameworks like SwiftUI, UIKit, and AppKit, which apply platform-defined materials to controls and navigation elements automatically.[17] The Liquid Glass overview notes that standard components from these frameworks pick up the appearance and behavior of the material automatically, and developers can also implement these effects in custom elements.[17] However, it does not provide numeric parameters; thus, these values would need to be measured from rendered interfaces or inspected via private APIs, and should be treated as empirically derived rather than officially specified.<MISSING_DATA>[exact blur radius and translucency parameters for Liquid Glass in macOS UI rendering]</MISSING_DATA>[1][17]

With High confidence, the HIG and technology overview explicitly instruct developers to use Liquid Glass effects sparingly and to prefer translucency to opaque colors in windows to avoid constricting views and reducing awareness of virtual and physical objects.[1][17] The materials guidance cautions against overuse of Liquid Glass and emphasizes that standard components already adopt the material automatically, reducing the need for custom over-application.[1] The macOS-specific guidance further recommends translucency over opaque colors in windows, noting that areas of opacity can block people’s view and make them feel constricted.[1] The Liquid Glass overview likewise advises developers to embrace the visual refresh for materials, controls, and app icons, to adopt best practices for windows, modals, menus, and toolbars, and to test apps across platforms.[17] <INFERENCE from="[guidance on sparing use][1][Liquid Glass overview’s emphasis on harmony][17]">For a knowledge corpus, these statements should be encoded as normative rules about material usage, flagged as “prescriptive HIG guidance,” rather than as measurable design tokens.</INFERENCE>

### Toolbars, Sidebars, Corner Radii, and Hierarchy in the New macOS Design System

With High confidence, the WWDC 2025 session “Build an AppKit app with the new design” is the most explicit source in the provided material for how the new macOS design system restructures windows, toolbars, and content regions around Liquid Glass.[4] The session explains that the new design of macOS establishes a common foundation for the look and feel of Mac apps with refreshed materials and controls, and that a key element of this new design is the Liquid Glass material—a translucent surface that reflects and refracts light, creating depth and dynamism.[4] It further notes that the new design system transforms the appearance of a Mac window by altering its shape and framing key structural regions in glass, including the toolbar.[4] Toolbar elements are placed on a glass material and the entire toolbar appears to float above the content, enhancing the sense of hierarchy.[4] <INFERENCE from="[WWDC description of floating toolbar][4][Liquid Glass overview’s hierarchy role][17]">From these descriptions, one can state with High confidence that in contemporary macOS design, the toolbar is conceptually a Liquid Glass region that structurally sits above and partially overlays window content, and this is an explicitly stated design behavior rather than something only measurable from renders.</INFERENCE>

With High confidence, the WWDC session provides authoritative guidance on which toolbar elements should and should not appear over glass and how developers can configure them.[4] The presentation explains that NSToolbar automatically puts the glass material behind every toolbar item but emphasizes that not every item should appear over glass: non-interactive items like custom titles and status indicators should avoid the glass material.[4] It clarifies that the glass material backing can make items look like buttons, and that developers can remove the glass from an NSToolbarItem by setting its isBordered property to false.[4] Additionally, the new style property on NSToolbarItem allows developers to specify a prominent style that tints the glass using the accent color, which is recommended for displaying state or emphasizing important actions.[4] These are definitive, API-level statements about behavior and usage: they specify exactly how AppKit will render items by default and how developers can opt out or apply accent tinting. With High confidence, these are “specified values” in the sense of behavior and configuration, even though they do not give pixel metrics or numeric corner radii.[4]

With High confidence, WWDC guidance also explains how sidebars and inspectors are presented in the new design and how they relate spatially to content.[4] In the new design, sidebars appear as panes of glass that float above the window’s content, whereas inspectors use edge-to-edge glass that sits alongside content.[4] Developers are instructed to use NSSplitViewController and create split items with sidebar or inspector behaviors, after which AppKit presents them with the appropriate glass material automatically.[4] The session notes that because the new sidebar sits atop glass, the legacy sidebar material is no longer necessary, and that using NSVisualEffectView to display that legacy material inside a sidebar would prevent the new glass material from showing through; developers are advised to remove such visual effect views from their view hierarchies.[4] These statements are prescriptive and structural, and they define the design system’s intended layout and material behavior without giving explicit numeric dimensions. They are therefore suitable for machine encoding as categorical design rules anchored to WWDC 2025.[4]

With Medium confidence, the WWDC session describes changes to window corner radii—specifically that windows with toolbars now use a larger radius designed to wrap concentrically around the glass toolbar elements and scale to match the toolbar size—but does not provide numeric values in the snippet.[4] The presenter explains that in the new design, the window radius is larger and is designed to wrap concentrically around the glass toolbar elements, implying that different window configurations may yield different effective radii based on toolbar height or configuration.[4] However, the documentation does not specify the exact radius values in points or pixels.[4] These values can be measured from UI kits or screenshots—for example, by inspecting corner radii in Sketch UI kits for “macOS 27” or similar—but they are not formally specified in the textual documentation visible here.<MISSING_DATA>[exact numeric corner-radius values for macOS windows and toolbars in current UI kits]</MISSING_DATA>[3][4] <INFERENCE from="[WWDC description of concentric radii][4][Apple Design Resources UI kit existence][3]">With Medium confidence, the design system conceptually specifies “concentric radii around toolbar glass” as a qualitative rule, while the numeric radii become design tokens encoded in UI kit files rather than HIG prose.</INFERENCE>

With High confidence, NSBackgroundExtensionView provides official guidance on creating edge-to-edge content experiences that extend under sidebars and inspectors while respecting safe areas.[18] The documentation describes NSBackgroundExtensionView as “a view that extends content to fill its own bounds” and notes that, for macOS 26.0+, this view can be used to create edge-to-edge content experiences with the background extension effect.[18] The WWDC session elaborates that developers should set automaticallyAdjustsSafeAreaInsets to true on content they want to extend under the sidebar, not on the sidebar itself; when true, NSSplitView extends the item’s frame beneath the floating sidebar and then applies a safe area layout guide to position content within the unobscured area.[4] A background extension view can be created and assigned a content view, which fills the safe area while the extension view creates a replica of the content view to fill the space outside the safe area.[4] These behaviors are precisely defined in the API documentation and WWDC guidance and can be encoded mechanically by a knowledge system; they are not subject to measurement.

### Control Metrics, Type Ramps, and Text Styles: What Apple Specifies and How It Is Versioned

With Medium confidence, Apple’s Human Interface Guidelines include guidance on typography, including typographic scales and type ramps, but the specific font sizes and line heights for macOS are not present in the provided snippets.[11] The HIG typically defines type styles by semantic roles, such as “title,” “headline,” “body,” and “caption,” and associates them with platform-provided text styles that developers access via APIs rather than by hand-coded sizes.[11] For example, Apple’s platforms commonly encourage developers to use semantic text styles like NSFont.TextStyle or equivalent rather than specifying font sizes, thereby delegating detailed metrics to system defaults that adapt to platform settings and accessibility preferences.[11] <MISSING_DATA>[exact typographic size and weight table for macOS type ramps in current HIG and UI kits]</MISSING_DATA>[11] <INFERENCE from="[HIG is general design guidance][11][Apple’s common practice of semantic text styles]">With Medium confidence, the numeric type ramp used by macOS (for example, 13 pt body, 11 pt secondary text, etc.) is encoded in platform resource tables and UI kits, not in narrative documentation, and must be measured or extracted from those resources.</INFERENCE>

With Medium confidence, control metrics such as button heights, sidebar widths, and toolbar item spacing are similarly defined implicitly through UI kits and platform resources rather than explicitly enumerated in HIG prose.[3][11] The Apple Design Resources macOS UI kits include shape layers, text layers, and layout grids that represent canonical component sizes and spacing for the targeted macOS version.[3] Designers and developers can inspect these metrics directly from the Sketch or Photoshop files to determine exact heights, widths, padding, and margins.[3] However, the textual HIG is primarily concerned with relative proportions, alignment, and qualitative guidance—for instance, recommending adequate touch targets and consistent alignment—rather than precise pixel counts.[11] <INFERENCE from="[Apple Design Resources UI kits encode visual layout][3][HIG emphasizes qualitative design norms][11]">For a machine-written corpus, with High confidence, the UI kit files should be treated as the main source for numeric control metrics, while the HIG defines semantic roles and relationships.</INFERENCE>

With Medium confidence, the HIG and related documentation provide guidance on text contrasts, vibrancy, and label styles, which indirectly influence type ramps and color usage.[1][11] For example, the materials guidance recommends using UIVibrancyEffectStyle.label for standard text in certain contexts, implying that text appearance is coordinated through vibrancy effects that adapt to backgrounds.[1] This is an explicit specification of which vibrancy style to use for standard text, but not of the underlying typographic metrics.[1] Similarly, the Liquid Glass overview advises being judicious with the use of color in controls and navigation so they stay legible and allow content to infuse them and shine through, again providing qualitative guidance rather than numeric thresholds.[17] <INFERENCE from="[materials vibrancy guidance][1][Liquid Glass overview’s emphasis on legibility and color restraint][17]">These statements can be encoded as rules about text styles (for example, “use label vibrancy for standard text on glass”) and color usage, but any numeric contrast ratios or color-distance thresholds are left to WCAG standards and platform behavior rather than Apple’s specific design docs.</INFERENCE>

With High confidence, Apple’s documentation and WWDC materials are versioned in ways that allow claims to be dated even when explicit publication dates are missing from snippets. API docs include OS version availability; for example, NSBackgroundExtensionView is labeled as macOS 26.0+, which indicates that any design behavior tied to it applies only from that OS version onward.[18] WWDC sessions carry their year and session ID in the URL, such as “WWDC25/310,” which associates their guidance with the platform state at that time.[4] Apple Design Resources UI kits are labeled by platform release (for instance “macOS 27 UI Kit”), providing a version anchor for the metrics they encode.[3] The HIG is continuously updated, but its sections are implicitly tied to platform states through cross-links to technology overviews (like Liquid Glass), OS-versioned APIs, and references to design features introduced at specific WWDCs.[11][17] <INFERENCE from="[NSBackgroundExtensionView macOS 26.0+][18][WWDC25 naming][4][UI kit platform labels][3]">For a knowledge corpus, with High confidence, each fact should be tagged with the OS version or WWDC year it depends on—for example, “macOS 26+” for background extension behavior or “macOS new design, WWDC 2025” for glass toolbars and sidebars—to provide temporal provenance.</INFERENCE>

### Specified Versus Measurable Values: A Comparative View

With High confidence, a useful way to structure a machine-written design corpus is to distinguish between values explicitly specified in Apple documentation and those that must be measured or inferred from UI kits and screenshots. The table below summarizes key categories relevant to Liquid Glass, control metrics, corner radii, and hierarchy, as supported by the cited sources.

| Category                            | Example Property                                      | Source Type         | Explicitly Specified? | Requires Measurement? | Confidence |
|-------------------------------------|--------------------------------------------------------|---------------------|------------------------|------------------------|-----------|
| Liquid Glass usage                  | “Don’t use Liquid Glass in content layer”             | HIG materials[1]    | Yes (prose rule)       | No                     | High      |
| Liquid Glass variants               | Regular vs clear; 35% opacity dimming recommendation  | HIG materials[1]    | Yes (variant role & 35% opacity) | Blur radius not specified | High      |
| Toolbar glass behavior              | Toolbars float above content; NSToolbarItem isBordered handling | WWDC25 session[4]   | Yes (behavior & API)   | Pixel dimensions not specified | High |
| Sidebar glass behavior              | Sidebars as floating glass panes; inspectors edge-to-edge glass | WWDC25 session[4]   | Yes (behavior)         | Sidebar width measurable only | High |
| Window corner radii                 | Larger radius wrapping concentrically around toolbar glass | WWDC25 session[4]   | Conceptual rule only   | Numeric radius must be measured from UI kits | Medium |
| Background extension behavior       | NSBackgroundExtensionView extends content under sidebar with safe areas | API docs[18][4]     | Yes (behavior & OS version) | No                     | High      |
| Control metrics (button height etc.)| Exact height, padding, spacing                        | Apple UI kits[3]    | Encoded in design files| Yes, via file measurement | High      |
| Type ramp sizes                     | Font sizes, line heights for macOS text styles        | Platform resources/UI kits[3][11] | Implicit in OS & design files | Yes, via UI kit or OS inspection | Medium |
| Vibrancy style for labels           | Use UIVibrancyEffectStyle.label for standard text     | HIG materials[1]    | Yes (style selection)  | No                     | High      |

With High confidence, this table illustrates that the most critical usage rules for materials and hierarchy—where Liquid Glass may be used, how toolbars and sidebars present over content, and how background extension behaves—are specified in primary Apple documentation and WWDC sessions.[1][4][17][18] Meanwhile, numeric metrics such as corner radii, exact control sizes, and type ramp values are generally not explicit in prose and must be measured from Apple’s own design files or platform resources.[3][11] <INFERENCE from="[table entries][1][3][4][11][18]">For the knowledge corpus envisaged here, a robust provenance typing system should tag explicitly specified rules as “Apple-doc canonical” and measured values as “UI-kit measured,” preserving the distinction between normative guidance and empirical measurement.</INFERENCE>

## Design File Format Structure: Sketch and Figma as Programmatic Sources of macOS Design Metrics

### Sketch File Format Evolution and ZIP Layout

With High confidence, Sketch’s modern file format is structured as a ZIP archive containing JSON-encoded document data and binary assets, a change introduced in Sketch 43 to improve interoperability and human readability.[5][19] Sketch’s developer documentation explains that Sketch documents are stored as ZIP archives containing JSON-encoded data and several binary assets such as bitmap images and document previews; the JSON files describe document data including pages, artboards, styles, and metadata.[5] A SitePoint analysis of Sketch’s new file format notes that prior to version 43, the .sketch file format was binary and unreadable, whereas now it is based on JSON, making it more accessible for manipulation by developers and web browsers.[19] The same article describes how to decompress and edit a Sketch file by renaming it to .zip, decompressing, editing the JSON, recompressing, and renaming back to .sketch, highlighting that the file format is a standard ZIP rather than a proprietary container.[19] With High confidence, this ZIP-and-JSON structure is the foundation for programmatic extraction of design data from Sketch files.

With High confidence, the top-level anatomy of a Sketch file includes meta.json, document.json, user.json, and a pages folder containing per-page JSON files.[5][13] Sketch’s developer documentation on the file format notes that meta.json contains metadata about the document itself, including a list of pages and artboards, the Sketch version used to save the file, fonts used, and similar global metadata; it is equivalent to the output of sketchtool metadata.[5] The document.json file contains common data for all pages of a document, such as shared styles, and links to JSON files in the pages folder.[5] The user.json file contains user metadata for the file, including canvas viewport and zoom level for each page, UI metadata for the app such as panel dimensions, and whether the document has been uploaded to Sketch Cloud.[5] The pages folder holds a JSON file per page, each describing the contents of a page, in a format similar to sketchtool dump.[5] The Sketch Developer File Format Specification defines valid Sketch document JSON, written as JSON Schema, and is published in multiple formats, enabling developers to generate types, GraphQL schemas, and model code or validate their own programmatically created Sketch documents.[13]

With High confidence, the Sketch file format specification is authored and maintained by Sketch itself and represents an authoritative description of the JSON structure and semantics.[13] The specification uses JSON Schema and is published via the sketch-file-format package as JSON Schema files, from which TypeScript types are automatically generated and distributed via the sketch-file-format-ts package.[13] These types strongly type data representing Sketch document objects, such as layers and artboards, in TypeScript projects.[13] <INFERENCE from="[Sketch file format docs][5][13][SitePoint explanation][19]">For the design corpus, with High confidence, the JSON Schema specification should be treated as the canonical definition of Sketch’s document data structure, while the ZIP container and JSON decomposition described in the documentation are authoritative sources for how to access that structure.</INFERENCE>

### Sketch JSON Schema: sharedSwatches, layerStyles, text styles, and symbols

With High confidence, the Sketch file format JSON schema includes constructs for shared swatches, layer styles, and text styles, enabling central definition and reuse of visual styles across layers and symbols.[13][6] Sketch’s Layer Styles documentation explains that layer styles work similarly to text styles but apply to shape layers rather than text layers, and that they can be included as part of a library for sharing with others.[6] To create a layer style, a user selects a shape layer with styles applied and clicks “Create” in the Appearance panel; the resulting style can then be edited and applied to shapes, vectors, frames, graphics, or groups, and can be included in libraries and overridden in symbol instances where the symbol source includes styles.[6] The Sketch file format specification, written as JSON Schema, describes valid structure for these styles, including how they are represented and referenced across document.json and pages/*.json files.[13]

With Medium confidence, shared swatches and text styles follow similar patterns, with shared definitions stored in document.json and references in page-level layers.[13][6] The JSON Schema specification covers objects representing colors, gradients, swatches, and text style attributes (such as font family, size, weight, and alignment) and ensures that they can be validated programmatically.[13] Sketch’s Layer Styles documentation notes that if a layer style has corners, developers can choose whether to include corner attributes in the style, and that corners are included by default in new layer styles but disabled for existing ones.[6] This implies that corner radii can be part of style definitions, and thus extracted programmatically from the JSON, but the specifics are defined by the JSON schema rather than by narrative docs.[13][6] <MISSING_DATA>[exact JSON property names for sharedSwatches, layerTextStyles, and layerStyles in current Sketch file format specification]</MISSING_DATA>[13] <INFERENCE from="[Layer Styles doc stating corners as part of styles][6][Sketch JSON Schema describing layer styles][13]">With High confidence, we can state that corner attributes, fill, stroke, and shadow properties are encoded in layer styles that can be extracted from document.json and page JSON files using the schema.</INFERENCE>

With High confidence, symbols in Sketch are defined as sources and instances, and symbol sources may include styles that instances can override.[6][13] The Layer Styles documentation notes that layer styles cannot be applied directly to symbol instances, but if the symbol source includes styles, those styles can be overridden in instances.[6] The file format specification similarly includes structural definitions for symbol masters and instances, including layer trees within symbols and references to shared styles.[13] <INFERENCE from="[Layer Styles doc on symbol sources][6][JSON Schema providing types for layers and symbols][13]">For a macOS design corpus, with High confidence, symbol naming and structure in Apple’s own Sketch UI kits—such as symbols for toolbars, buttons, sidebars, and windows—can be extracted programmatically, allowing the corpus to bind measured metrics to named canonical components.</INFERENCE>

### Corner Radii and Capsule Representation in Sketch: What Is Known and What Must Be Measured

With Medium confidence, corner radii in Sketch are encoded as numeric attributes on shape layers and possibly in layer styles, but the specific encoding conventions for fully-rounded (“capsule”) radii are not documented in the provided Sketch specification excerpts.[13][6][19] Sketch’s Layer Styles documentation indicates that corners can be included in styles, which implies that radii are represented as style properties.[6] The JSON Schema specification defines numeric fields for shape geometry, including properties that logically correspond to corner radius values.[13] The SitePoint article notes that developers can edit JSON code in Sketch files to change styles, including corners, demonstrating that radii are exposed as editable numeric values in the JSON.[19] However, none of these sources specify sentinel float values used to represent “capsule” radii—such as a special large number meaning “fully rounded”—in the current file format.<INSUFFICIENT_EVIDENCE>[exact sentinel float values representing fully-rounded capsule corners in Sketch’s JSON, and whether they are standardized across versions]</INSUFFICIENT_EVIDENCE>[13][19]

With Low confidence, based on community practice in similar design tools, it is plausible that Sketch either uses actual numeric radii equal to half the height of the capsule shape or employs special sentinel values (for example, a large number) to indicate automatic fully-rounded corners.<INFERENCE from="[SitePoint article stating JSON is editable and corners can be changed][19][Layer Styles doc referencing corners][6]">Because this behavior is not documented in the official Sketch JSON Schema excerpted here, any implementation relying on sentinel values for capsules must empirically validate the encoding by inspecting sample Sketch files—ideally official Apple UI kits—and cross-checking radii against rendered appearance.</INFERENCE> With High confidence, this means that while corner radii can be extracted from Sketch JSON, the semantics of “capsule” versus “rounded rectangle” must be treated as empirically derived until confirmed by updated Sketch documentation.

For a drift-resistant knowledge corpus, the appropriate approach is to store corner radii as numeric values extracted from Sketch JSON, tagged with evidence provenance “Sketch JSON measurement,” and separately encode conceptual tags such as “capsule shape” based on measured relationships (for example, radius equal to or exceeding half the shape height within a tolerance). This avoids relying on undocumented sentinel semantics and keeps the knowledge base robust against changes in Sketch’s internal encoding.<INFERENCE from="[Sketch JSON is authoritative for numeric radii][13][19]">With Medium confidence, such a measurement-based classification is both machine-checkable and less fragile than any assumed sentinel convention.</INFERENCE>

### Reliable Extraction Versus Rendering-Dependent Properties in Sketch

With High confidence, the following categories of information are reliably extractable from Sketch files via JSON without rendering: layer hierarchies, symbol definitions, shared styles (layerStyles, text styles), shared swatches, basic geometry (position, size), corner radii, and text content.[5][13][19] The file format specification’s JSON Schema ensures that these elements have defined shapes, types, and constraints, making them suitable for automated parsing and validation.[13] The SitePoint article demonstrates that editing JSON for styles and layers, then recompressing the file, results in changes visible in Sketch, implying that these properties are directly controlling rendering.[19] Open-source libraries like sketch-constructor provide helpers and classes to read and write Sketch files in JavaScript without Sketch installed, further confirming that the JSON structure is sufficient to reconstruct the scene graph and many stylistic properties.[15] <INFERENCE from="[JSON Schema definition][13][sketch-constructor library capabilities][15][SitePoint JSON editing results][19]">With High confidence, the knowledge corpus can treat these properties as directly extractable facts, with the caveat that text formatting metrics may depend on font metrics not included in JSON.</INFERENCE>

With Medium confidence, certain visual properties require rendering to determine their effective appearance, especially when they depend on platform-native effects or runtime conditions. For example, the visual appearance of Liquid Glass-like materials implemented in Sketch UI kits may use blur and translucency values that approximate macOS behavior but are not guaranteed to be identical to system rendering.[1][17][3] Similarly, effects such as vibrancy, inner shadows, and blending modes may have semantics that depend on engine-specific implementations rather than purely on JSON fields. <MISSING_DATA>[complete mapping from Sketch JSON effect properties to macOS-compliant Liquid Glass rendering behavior]</MISSING_DATA>[1][3][13] With Medium confidence, extracting blur radius, opacity, and blend modes from Sketch JSON yields numeric approximations, but verifying their alignment with Apple’s true platform materials requires cross-checking against rendered macOS interfaces.

In practice, for a macOS design corpus, Sketch files serve as primary sources for concrete geometry and style tokens, while screenshots provide ground-truth renders for verifying material behaviors and corner radii. Combining both allows the knowledge corpus to reconcile measured values with conceptual rules from Apple docs, and to detect design drift across OS versions by comparing UI kits and screenshots over time.<INFERENCE from="[Sketch file format capabilities][5][13][19][Apple UI kits role][3][Apple material guidance][1][17]">With High confidence, this dual-source strategy is essential for constructing a corpus that can both reason semantically and measure quantitatively.</INFERENCE>

### Figma File Format and REST API: Structure, Capabilities, and Reverse-Engineered Protocols

With High confidence, Figma’s official public interface to design file structure and styles is its REST API, while the .fig file format and internal binary protocols are not officially documented but have been partially reverse-engineered.[7][8] Figma’s Developer Docs describe “Files endpoints” that allow retrieving information about files given a file key, including JSON and image representations of the whole file or individual nodes.[8] The GET /v1/files/:key endpoint returns the document referred to by :key as a JSON object, including metadata such as name, lastModified, thumbnailUrl, editorType, linkAccess, version, and a document attribute containing a Node of type DOCUMENT.[8] The response also includes components and componentSets mappings and a styles map containing style metadata.[8] By specifying ids or depth parameters, developers can restrict the returned JSON tree to specific nodes or depth levels.[8] The GET /v1/files/:key/nodes endpoint returns nodes referenced by :ids as JSON, including document node, components, componentSets, schemaVersion, and styles; vector data is not returned by default, but can be requested via the geometry=paths parameter.[8] With High confidence, these endpoints are the authoritative, Figma-supported mechanisms for programmatically accessing UI kit structure and styles.

With High confidence, the Figma REST API exposes component metadata, styles, and node hierarchies, but may omit certain internal implementation details present in the binary .fig format or WebSocket protocols.[8][7] The components key provides a mapping from node IDs to component metadata, helping developers determine which components instances come from.[8] The styles key maps style IDs to style metadata, enabling knowledge of typography, color, and effect styles used in the file.[8] When vector data is requested via geometry=paths, Figma returns path data describing shapes and curves, allowing reconstruction of geometry.[8] However, the API does not expose low-level binary structures like commandsBlob or vectorNetworkBlob used internally, nor does it describe Kiwi protocol messages in official docs.[8][7] <INFERENCE from="[REST API docs][8][reverse-engineering report][7]">With High confidence, a macOS design corpus for Figma-based UI kits should rely on the REST API as the primary source and treat reverse-engineered internal formats as supplementary, recognizing that official support and stability apply primarily to the REST API outputs.</INFERENCE>

With High confidence, an independent reverse engineering effort, reported on Hacker News and hosted on GitHub, has decoded Figma’s internal binary protocol, known as Kiwi format over WebSocket, to extract full scene graphs—including nodes, vector paths, and styles—without using the REST API.[7] The tool decodes the fig-wire frame format (schema delivery over WebSocket), commandsBlob (Figma’s pre-computed SVG path commands: MoveTo, LineTo, CubicBezier, ClosePath), and vectorNetworkBlob (editable path data with vertices, segments, and regions), documenting all three as Kaitai Struct specs (.ksy) so parsers can be generated in any language.[7] The project emphasizes that no Figma code is included and that it only decodes the wire format of data transmitted to the user’s browser.[7] This is a primary source for understanding the internal structure of a Figma file and its transmission protocol, though not officially supported by Figma.

With Medium confidence, the .fig file format used for Figma files is likely a storage version of the same underlying data structures accessible via Kiwi protocol, but the specifics are not described in the provided sources.<MISSING_DATA>[detailed official specification of .fig storage format and mapping to REST API or Kiwi protocol]</MISSING_DATA>[7][8] The REST API documentation focuses on JSON representations and does not describe binary layout.[8] The reverse-engineering describes wire formats, not disk formats.[7] Therefore, for most practical purposes, the .fig file itself remains opaque and is best treated as a container managed by Figma, while programmatic access uses the REST API or Kiwi protocol.

With High confidence, the Figma REST API provides sufficient information to reconstruct UI kits for macOS-like design systems using published JSON, including components, styles, and geometry (when requested).[8] A UI kit published in Figma can be accessed via its file key, and the API returns the document tree, components, styles, and optionally vector paths.[8] This allows a knowledge corpus to extract control metrics, corner radii, color palettes, type styles, and component hierarchies from authoritative Figma-hosted UI kits. However, certain properties—such as platform-native blur or vibrancy behaviors—may not be directly encoded and instead be approximated by Figma effects, requiring mapping between Figma effect properties and macOS semantics.<INFERENCE from="[REST API capabilities][8][reverse-engineering vector data][7][Apple material semantics][1][17]">With Medium confidence, any translation between Figma effect styles and macOS Liquid Glass behaviors must be treated as an inferred mapping rather than an Apple-sanctioned specification.</INFERENCE>

### Sketch Versus Figma for macOS UI Kit Extraction: Comparative Summary

With High confidence, the following table compares Sketch and Figma as sources for macOS design language metrics and structure, focusing on aspects relevant to a machine-written corpus.

| Aspect                         | Sketch (.sketch)                                | Figma (.fig + REST API)                            | Confidence |
|--------------------------------|--------------------------------------------------|----------------------------------------------------|-----------|
| File container                 | ZIP archive with JSON and binary assets[5][19]  | Proprietary .fig; structure exposed via REST JSON[8] and Kiwi protocol[7] | High      |
| Primary structural docs        | Sketch File Format JSON Schema[13]             | REST API docs for /v1/files and /v1/files/:key/nodes[8] | High      |
| Scene graph access             | Direct via JSON in document.json and pages/*.json[5][13] | Via REST API JSON Node trees; via Kiwi protocol reverse-engineering for internal binary[7][8] | High      |
| Shared styles                  | layerStyles, text styles, sharedSwatches in document.json[13][6] | styles map with text, color, effect styles[8]      | High      |
| Symbols / components           | Symbol masters and instances defined in JSON[13][6] | components and componentSets maps for nodes[8]     | High      |
| Corner radii                   | Numeric fields in shape layers and styles; semantics partly documented[13][6][19] | Geometry paths with vector data; corner radii encoded in vector networks[8][7] | Medium   |
| Capsule sentinel encoding      | Not documented in provided specs; requires empirical validation[13][19] | Not documented; depends on Figma’s shape representation[7][8] | Low      |
| Vector path access             | Paths encoded in layer geometry; fully documented in JSON Schema[13] | geometry=paths option returns vector paths; Kiwi commandsBlob/vectorNetworkBlob reverse-engineered[7][8] | High      |
| Text styles / type ramps       | Text styles encoded in shared text styles and symbol layers[13][6] | Styles map includes typography styles[8]           | Medium    |
| Official macOS UI kit availability | Apple distributes macOS UI kits as Sketch and Photoshop files[3] | macOS-like UI kits may exist in Figma, but Apple’s official kits referenced here are Sketch-based[3] | High      |

<INFERENCE from="[Sketch file format docs][5][13][SitePoint analysis][19][Figma REST API docs][8][Kiwi protocol reverse-engineering][7][Layer Styles doc][6][Apple Design Resources UI kits][3]">With High confidence, this table shows that for Apple’s own macOS UI kits, Sketch is the primary source; Figma primarily matters when third-party or internally developed macOS-like kits live in Figma. Sketch’s JSON Schema affords strong type safety and validation for programmatic extraction, whereas Figma’s REST API provides structured access but relies on external reverse-engineering for full binary protocol understanding.</INFERENCE>

## Drift-Resistant Machine-Written Knowledge Corpora (2023–2026): Provenance, Invariants, and Failure Modes

### Problem Framing: Model Self-Pollution, Drift, and Model Collapse

With High confidence, recent research has demonstrated that training generative models on data produced predominantly by other generative models can cause “model collapse,” a degenerative process in which the models gradually lose information about the true underlying data distribution and converge towards point estimates with diminished variance.[9] In a 2024 Nature paper, Shumailov et al. investigate what happens when text produced by models such as GPT forms most of the training dataset for subsequent models.[9] They find that indiscriminately learning from data produced by other models causes model collapse, even under ideal conditions where there is no function estimation error.[9] Over successive generations, models start losing information about the true distribution; tails disappear and learned behaviors converge, leading the model to mis-perceive the underlying learning task.[9] With High confidence, this is directly relevant to a machine-written design corpus that is continually updated by a language model reading and rewriting its own outputs: if the corpus becomes dominated by model-generated content with insufficient anchoring to external human-authored or ground-truth sources, the model’s future behavior may drift away from reality.

With High confidence, Shumailov et al. emphasize that access to the original data distribution is crucial, particularly in tasks where the tails of the distribution matter.[9] They show that training on samples from another generative model induces a distribution shift that drives model collapse over time.[9] This underscores the importance of ensuring that a design knowledge corpus is continually refreshed with primary sources—such as Apple documentation, UI kits, and real screenshots—rather than being recursively derived from the model’s own synthetic summaries or extrapolations. <INFERENCE from="[Nature model collapse paper][9][primary-source role in macOS design language][1][3][4][11][17]">With High confidence, the design corpus must enforce provenance tags that distinguish “primary human-authored” evidence from “model-generated” content and must maintain a high threshold of primary material to avoid self-pollution.</INFERENCE>

With High confidence, a 2025 survey on integrating large language models with knowledge bases observes that LLMs are often combined with structured knowledge bases to enhance reasoning, and discusses technical challenges related to data integration, consistency, and application in practice.[10] While the excerpted snippet is limited, the article is a peer-reviewed survey in Knowledge-Based Systems, indicating a rigorous treatment of the interplay between LLMs and knowledge bases.[10] <INFERENCE from="[LLM+KB survey][10][Nature model collapse paper][9]">With Medium confidence, this survey likely covers issues such as knowledge base construction, update strategies, and ways to preserve consistency as LLMs interact with structured data, providing conceptual backing for provenance-aware, schema-validated knowledge stores.</INFERENCE>

### Provenance and Confidence Typing of Individual Facts

With High confidence, one of the most effective techniques for maintaining correctness and consistency in a model-written knowledge base is to assign provenance and confidence types to individual facts. Provenance typing involves tracking the source or sources from which each fact was derived, such as “Apple HIG,” “Apple UI kit measurement,” “Sketch JSON schema,” “Figma API,” “Nature paper,” “model inference,” or “model speculation.”[1][3][5][8][9][13][17][19] Confidence typing then associates each fact with a confidence level—such as High, Medium, or Low—reflecting the strength and reliability of the evidence supporting it. <INFERENCE from="[source discipline prioritizing primary docs][source discipline emphasising authoritative sources][Nature model collapse paper highlighting importance of real data][9][Sketch JSON Schema providing machine-checkable structure][13][Figma API docs providing authoritative JSON][8]">With High confidence, such typing can be used to implement promotion rules, where facts begin at lower confidence when first introduced and are promoted to higher confidence only after corroboration by independent primary sources.</INFERENCE>

With High confidence, the design corpus described here should treat Apple HIG and official documentation as primary sources, UI kit measurements as primary but empirical sources, and model-generated summarizations as secondary sources that require corroboration.[1][3][11][17] Facts derived from Apple HIG, technology overviews, and API docs should be assigned High initial confidence, especially when they describe explicit behaviors or rules (for example, “Don’t use Liquid Glass in the content layer” or “NSToolbar automatically places glass behind toolbar items”).[1][4][17] Facts measured from official Apple UI kits (for example, “toolbar height is X points in macOS 27 UI kit”) should receive High confidence as measurements, with a provenance tag reflecting “Apple UI kit measurement.”[3][5][19] Facts inferred from the combination of multiple sources—such as “concentric corner radii around toolbar glass mean that windows with toolbars have larger corner radii than windows without” based on WWDC guidance and UI kit measurements—should be flagged with Medium confidence and an explicit <INFERENCE> tag naming their source claims.[4][3]

Promotion rules can be codified as invariants: for instance, a fact cannot be promoted to High confidence unless it is supported by at least one primary source and one independent corroborating source, such as both a textual doc and a measurement from a UI kit.[1][3][4][17] Model-generated content without external references should remain Low confidence and should not be used as training data or canonical truth for later model sessions, mitigating model collapse risk.[9] <INFERENCE from="[Nature paper on model collapse][9][Apple docs as primary sources][1][3][4][17][Sketch UI kit evidence][5][19]">With High confidence, this provenance and confidence typing system is a direct response to self-pollution risks identified in model collapse research and is essential for designing a drift-resistant corpus.</INFERENCE>

### Corroboration Thresholds and Independence of Sources

With High confidence, corroboration thresholds define how many independent sources must support a claim before it is treated as established, and independence must be carefully defined to avoid falsely counting correlated sources as independent. For a macOS design corpus, a reasonable default threshold is at least three independent sources before a claim is treated as canon, especially for numeric metrics or subtle behaviors. For example, a claim about corner radii might require agreement among (1) Apple UI kit measurement, (2) consistent measurement across multiple screenshots from real apps, and (3) confirmation via AppKit behavior (such as NSWindow’s documented frame and radius) or Apple’s design documentation, if available.[3][4][5][19]

With Medium confidence, independence can be defined as sources that do not share the same upstream data or generation process. Apple HIG and WWDC sessions are independent in the sense that HIG is a written doc and WWDC is a spoken presentation; however, both are authored by Apple and may derive from the same internal design system.[1][4][11][17] Thus, they are independent mediums but correlated institutional sources. UI kit measurements, while produced by Apple, represent a concrete artifact of the design system and can reveal metrics not explicitly stated in docs.[3] Screenshots of third-party macOS applications, captured from actual running systems, provide a separate observational source that may confirm that Apple’s design language is implemented consistently across ecosystem apps.<INFERENCE from="[Apple docs are internal][1][4][11][17][UI kits as design artifacts][3][Nature model collapse highlighting distribution shift when using synthetic data][9]">With High confidence, independence should be defined both institutionally (different authors or organizations) and methodologically (different data capture pipelines), and correlated sources should not be counted multiplicatively in corroboration thresholds.</INFERENCE>

With High confidence, the requirement of three independent sources before canonization is a conservative defense against model collapse and confabulation. If a claim appears only in model-generated text and not in any primary documentation or UI kit measurement, it should remain Low confidence and be marked as speculative.[9][10] If two sources are both derived from the same UI kit (for example, a documentation note referencing it and a measured metric), they should be treated as one underlying source rather than two independent ones. <INFERENCE from="[model collapse findings][9][LLM+KB survey’s emphasis on KB integration challenges][10]">With Medium confidence, robust independence rules reduce the risk that correlated errors propagate into canon and are later reinforced by models reading their own outputs.</INFERENCE>

### Deduplication, Content-Addressed Identity, and Schema Validation

With High confidence, deduplication and content-addressed identity of ingested evidence are crucial for managing large, persistent corpora across sessions. Content-addressed storage, where each piece of evidence (for example, a snippet from Apple HIG or a JSON fragment from a Sketch UI kit) is hashed (using cryptographic hash functions) and stored under its hash, ensures that identical evidence is recognized and not duplicated unnecessarily.[5][13][19] Deduplication also reduces the risk that repeated evidence is counted multiple times in corroboration thresholds. <INFERENCE from="[Sketch JSON schema enabling stable structural representation][13][SitePoint explanation that JSON can be edited and reused][19]">With Medium confidence, content hashes combined with provenance tags create a robust basis for identity, allowing different sessions to recognize previously ingested evidence and avoid reinterpreting it as novel.</INFERENCE>

With High confidence, schema validation provides machine-checkable invariants over the knowledge store. For Sketch, the JSON Schema specification defines valid structures for document.json, meta.json, pages/*.json, and style objects; this schema can be used to validate that extracted data conforms to expected structures.[13] Apple UI kits provided as Sketch files also implicitly conform to this schema, making them amenable to reliable parsing.[3][5][13] For Figma, the REST API defines the structure of responses, including the shape of Node objects, components, componentSets, and styles; these can be treated as schemata for validating incoming JSON.[8] <INFERENCE from="[Sketch JSON Schema][13][Figma REST endpoints and return types][8]">With High confidence, the knowledge corpus should enforce that all ingested structural data from Sketch and Figma passes schema validation, and any deviations are flagged as anomalies or possible format changes.</INFERENCE>

Machine-checkable invariants can include assertions such as “every numeric metric must have an associated provenance type and timestamp,” “every measured corner radius must be tied to a specific component name and UI kit version,” and “no fact representing a normative rule may have only a model-generated source.” These invariants can be encoded as rules checked by an invariant checker, not by the language model itself, to ensure that the corpus’s structural integrity is maintained even if the model proposes conflicting updates.<INFERENCE from="[Nature paper’s emphasis on self-pollution risk][9][Sketch schema’s machine-checkability][13][LLM+KB survey’s discussion of KB integration][10]">With High confidence, separating invariant checking from generative updates is key to resisting drift.</INFERENCE>

### Detecting Model-Introduced Drift, Contradictions, and Confabulation

With High confidence, detecting drift, contradictions, and confabulation in a corpus written by a language model requires monitoring the distribution of facts over time and cross-checking new statements against established invariants and provenance. Model collapse research shows that when models are repeatedly trained or conditioned on their own outputs, they gradually lose information about tails and converge towards narrower distributions.[9] In a knowledge corpus context, this manifests as the gradual erasure of rare facts, subtle distinctions, and edge cases, and as the smoothing of distributions of metrics or rules in favor of simpler approximations.

With High confidence, one detection strategy is to track changes in fact distributions across corpus versions—such as the variance of measured corner radii or the diversity of documented Liquid Glass usage patterns—and flag any monotonic decrease in variance or diversity without corresponding evidence from new primary sources.[9] If, for example, earlier corpus versions record multiple distinct corner radius values for different types of windows, but later versions consolidate them into a single “canonical” value without new Apple documentation, this may indicate model-induced drift or oversimplification.<INFERENCE from="[Nature paper describing tails disappearing][9][Sketch UI kit measurement representing underlying distribution][3][5][13]">With Medium confidence, statistical drift detection can complement provenance-based reasoning to identify when the model is collapsing diverse facts into overly simplified narratives.</INFERENCE>

With High confidence, contradiction detection can be implemented by storing logical relationships between facts and using truth-maintenance techniques. If a new model-written fact claims, for example, that “Liquid Glass is recommended for use in app content layers,” this directly contradicts HIG guidance that explicitly warns against using Liquid Glass in the content layer.[1] A contradiction-aware system should detect that the new claim conflicts with a High-confidence, HIG-derived rule, and either reject the new claim or preserve it as a “disputed fact” with Low confidence and clear provenance. Confabulation detection involves identifying claims that lack any external support; when the model produces a statement with no linked primary sources, the system should treat it as Low confidence and avoid promoting it to canon or using it as training data.[9][10]

### Truth-Maintenance Systems and Contradiction Handling Without Destroying Evidence

With High confidence, truth-maintenance systems (TMS) offer a principled approach to handling contradictions and evolving beliefs without destroying losing evidence. A TMS, also known as a reason maintenance system, is designed to manage knowledge bases where information can be contradictory or uncertain.[16] It maintains records of base facts (initial assumptions) and derived facts (conclusions reached through reasoning), along with dependencies between them.[16] When new information contradicts existing beliefs, the TMS detects inconsistencies and attempts to maintain coherence, either by revising the dependency network—retracting certain base facts or reasoning steps—or by contextualizing, maintaining different sets of beliefs depending on context.[16]

With High confidence, a TMS keeps track of justifications for each derived fact, allowing systems to explain reasoning and to retract conclusions when their supporting assumptions are invalidated.[16] In the design corpus, base facts might include Apple HIG statements, Sketch JSON measurements, and Figma API outputs, while derived facts might include inferred rules about corner radii or Liquid Glass behaviors. If a new Apple document changes a rule—for example, introducing an exception to a prior Liquid Glass usage guideline—the TMS can preserve the old base fact as historical evidence while updating the derived fact for current context. Contradictions are managed by either marking certain facts as context-specific (for instance, “in macOS 25, rule A holds; in macOS 27, rule B holds”) or by revising beliefs to align with stronger evidence.[16]

With Medium confidence, more advanced TMS implementations support multiple contexts explicitly, allowing different versions of the knowledge base to coexist for different OS versions or design system states.[16] A macOS design corpus could maintain one context for “macOS pre-Liquid Glass,” another for “macOS with Liquid Glass,” and possibly more for future revisions. Evidence is not destroyed; rather, it is scoped and its applicability is controlled by context. <INFERENCE from="[TMS description of revision and contextualization][16][Apple’s evolution of macOS design system via WWDC 2025][4][Liquid Glass introduction][17]">With High confidence, adopting a TMS-like architecture is appropriate for managing a design corpus that spans multiple macOS generations and evolving design languages.</INFERENCE>

### Failure Modes and Negative Results in Drift-Resistant Knowledge Systems

With High confidence, several failure modes must be considered when designing a drift-resistant, model-written knowledge corpus. The most prominent is the self-pollution leading to model collapse, as documented by Shumailov et al.[9] If the corpus is primarily populated by model-generated summaries that themselves incorporate prior model outputs, the distribution of knowledge may become increasingly divorced from the true design language. This can lead to loss of rare edge-case behaviors (such as subtle differences in corner radii for certain window configurations) and an overemphasis on simplified canonical rules, even when official documentation maintains nuance.[9]

With Medium confidence, another failure mode is provenance decay, where facts lose track of their original sources due to repeated rewriting or summarization by the model. If the system fails to preserve source references and confidence levels across sessions, subsequent updates may treat derived or inferred statements as primary facts, promoting them beyond their warranted confidence. This contradicts the provenance typing principle and undermines corroboration thresholds.[10][16] <INFERENCE from="[LLM+KB survey emphasising challenges in integrating KBs][10][TMS maintaining dependency networks][16][Nature paper’s warning on model collapse][9]">With High confidence, such provenance decay is particularly dangerous in long-running systems where models are repeatedly fine-tuned or updated based on corpus content.</INFERENCE>

With Medium confidence, a third failure mode involves incorrectly defined corroboration thresholds or independence criteria, which can allow correlated sources (for example, multiple Apple docs referencing the same internal design decision) to be treated as independent, artificially inflating confidence. If an assertion that is actually only supported by a single design decision is repeated across multiple docs and UI kits, the system may incorrectly treat it as extremely well-supported, even though underlying evidence is singular.<CONFLICTING_EVIDENCE>[corroboration via multiple Apple sources may or may not constitute independent evidence; independence is partly an interpretive judgment]</CONFLICTING_EVIDENCE>[1][3][4][11][17]

Finally, misconfigured TMS architectures can inadvertently discard valuable historical evidence when revising beliefs. If a contradiction is resolved by deleting a base fact rather than contextualizing it, the system may lose track of past design states, undermining the ability to reason about evolution or compatibility across macOS versions.[16] <INFERENCE from="[TMS revision vs contextualization][16][Apple’s evolving design system via WWDC][4][model collapse concerns][9]">With Medium confidence, designing TMS rules that prioritize contextualization over deletion is critical for preserving the historical record while maintaining current correctness.</INFERENCE>

### Applying These Techniques to the Proposed macOS Design Skill

With High confidence, combining the insights above yields a concrete architecture for a skill that maintains a persistent, machine-written macOS design-knowledge corpus across many AI sessions by digesting macOS screenshots and official Apple UI kit files. First, the corpus must treat Apple’s HIG, technology overviews, API docs, WWDC sessions, and Apple Design Resources UI kits as primary sources for design language, with clear provenance tags and high initial confidence.[1][3][4][11][17][18] Second, each extracted metric or rule must be associated with a provenance type (for example, “Apple HIG text,” “Sketch UI kit measurement,” “Figma API JSON,” “Nature model collapse research”) and a confidence level, with promotion rules requiring corroboration by at least three independent sources where feasible.[1][3][5][8][9][13][17][19]

Third, the system must use Sketch’s JSON Schema and Figma’s REST API definitions as machine-checkable schemas for structural extraction, validating every ingested design file and storing data in content-addressed form with deduplication.[5][8][13][15][19] Fourth, a separate invariant-checker should enforce rules such as “no normative rule may be derived solely from model-generated text,” “every metric must trace to a human-authored or artifact source,” and “contradictions must be recorded and resolved via TMS rather than overwriting past facts.”[9][10][16] Fifth, the system must monitor distributional drift in metrics and rules and compare new model-written content to prior versions, flagging potential collapse, simplification, or contradiction with canonical sources.[9][3][4]

<INFERENCE from="[all cited sources on Apple design docs][1][3][4][11][17][18][Sketch file format][5][13][19][Figma API][8][Kiwi reverse-engineering][7][LLM+KB survey][10][Nature model collapse][9][TMS article][16]">With High confidence, such an architecture—anchored in authoritative macOS design sources, robust file-format parsing, provenance-aware knowledge management, and truth-maintenance mechanisms—can support long-running, session-spanning use of language models to write and refine a macOS design corpus without succumbing to drift or model collapse.</INFERENCE>

## Conclusion

With High confidence, Apple’s current macOS design language for materials and hierarchy—especially the Liquid Glass system, glass toolbars and sidebars, background extension, and window corner radii—finds its most authoritative textual expression in the Human Interface Guidelines, Liquid Glass technology overview, AppKit API documentation (NSGlassEffectView, NSBackgroundExtensionView, NSVisualEffectView.Material), and WWDC 2025 sessions, while Apple Design Resources UI kits encode concrete numeric metrics and layouts.[1][3][4][11][12][17][18] The HIG and related docs provide explicit rules about where Liquid Glass may be used, how variants behave, and how toolbars and sidebars should appear in the new design, but they largely refrain from publishing pixel-level control metrics and corner radii, which must instead be measured from UI kits and screenshots.[1][3][4][11][17] This distinction between specified and measurable values is critical for a machine-written corpus that must know which facts to treat as normative rules and which as empirical tokens.

With High confidence, Sketch’s .sketch file format, structured as a ZIP archive with JSON files and binary assets, and governed by an official JSON Schema specification, offers a robust programmatic surface for extracting layer hierarchies, shared styles, swatches, symbols, and geometry, including corner radii and style attributes.[5][13][19] While the exact encoding of fully-rounded capsule radii remains undocumented in the provided sources and requires empirical validation, the overall structure is sufficiently well-defined to enable reliable parsing and validation.[13][6][19] Figma’s REST API complements this with JSON-based access to file structures, nodes, components, styles, and vector paths, supported by official documentation and enriched by independent reverse-engineering of its Kiwi protocol.[7][8] Together, these tools furnish the structural and metric data necessary to populate a design corpus from UI kits.

With High confidence, the period 2023–2026 produced rigorous evidence about the dangers of model self-pollution and collapse, particularly through Shumailov et al.’s Nature paper, and renewed interest in integrating LLMs with knowledge bases in ways that preserve provenance, consistency, and correctness.[9][10] Truth-maintenance systems provide a mature conceptual framework for managing contradictions, dependencies, and contexts in knowledge bases, allowing the corpus to evolve without erasing historical evidence.[16] By combining provenance and confidence typing, corroboration thresholds, content-addressed storage, schema validation, invariant checking, drift detection, and TMS-based contradiction handling, a macOS design skill can maintain a persistent, machine-written corpus that remains aligned with authoritative sources and resistant to degradation across many sessions.[1][3][4][5][8][9][10][13][16][17][19]

<INFERENCE from="[all cited evidence across sections]">With High confidence, the strongest evidence-backed trajectory is clear: Apple continues to refine macOS’s design system with Liquid Glass and glass-based hierarchy, distributing canonical behavioral guidance through HIG, technology overviews, and WWDC, while embedding concrete metrics in Sketch-based UI kits; Sketch and Figma provide structurally rich, programmatically accessible design file formats; and the emerging understanding of model collapse and truth maintenance demands a rigorously provenance-aware, invariant-checked architecture for any long-running, model-written design corpus.</INFERENCE>

## Sources

- [Materials | Apple Developer Documentation](https://developer.apple.com/design/human-interface-guidelines/materials)
- [NSVisualEffectView.Material - Documentation](https://developer.apple.com/documentation/appkit/nsvisualeffectview/material-swift.enum)
- [Apple Design Resources](https://developer.apple.com/design/resources/)
- [Build an AppKit app with the new design - WWDC25 - Videos](https://developer.apple.com/videos/play/wwdc2025/310/)
- [Sketch Developer — File format](https://developer.sketch.com/file-format/)
- [Layer Styles - Sketch](https://www.sketch.com/docs/symbols-and-styles/text-and-layer-styles/layer-styles/)
- [Show HN: I/Claude reverse-engineered Figma's binary ...](https://news.ycombinator.com/item?id=47607324)
- [Endpoints | Developer Docs](https://developers.figma.com/docs/rest-api/file-endpoints/)
- [AI models collapse when trained on recursively generated ...](https://www.nature.com/articles/s41586-024-07566-y)
- [A comprehensive survey on integrating large language models with ...](https://www.sciencedirect.com/science/article/pii/S0950705125005490)
- [Human Interface Guidelines | Apple Developer Documentation](https://developer.apple.com/design/human-interface-guidelines)
- [NSGlassEffectView | Apple Developer Documentation](https://developer.apple.com/documentation/appkit/nsglasseffectview)
- [Sketch Developer — Specification](https://developer.sketch.com/file-format/spec)
- [GitHub - ant-design/html2sketch: parser HTML to Sketch JSON](https://github.com/ant-design/html2sketch)
- [amzn/sketch-constructor: Read/write/manipulate Sketch ... - GitHub](https://github.com/amzn/sketch-constructor)
- [What is a Truth Maintenance System (TMS)? Give the architecture of ...](https://www.crookshanksacademy.com/post/what-is-a-truth-maintenance-system-tms-give-the-architecture-of-a-problem-solver-with-a-tms-in-th)
- [Liquid Glass | Apple Developer Documentation](https://developer.apple.com/documentation/technologyoverviews/liquid-glass)
- [NSBackgroundExtensionView](https://developer.apple.com/documentation/appkit/nsbackgroundextensionview)
- [Why You Need to Know About Sketch's New File Format - SitePoint](https://www.sitepoint.com/need-know-sketchs-new-file-format/)
