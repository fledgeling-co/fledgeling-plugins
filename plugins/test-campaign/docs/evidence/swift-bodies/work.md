# F25-050 with F25-059/F25-060 — author work/gap-fix receipt

Status: ready for fresh primary and additional adversarial review; not independently verified,
merged, pushed or installed. User-owned upstream branch codex/test-campaign-swift-bodies;
Perch receipt branch codex/f25-050. Workflow/out-of-family lanes were unavailable; native
execution and same-family review fallback are explicit. No subagents or resource throttles.

## Grounding and stages

Perch brief/spec/plan predate implementation. Conductor approved exact lexical body extraction,
retention of uncalled helpers as own candidates, explicit @Test/test* entries, recursive executable
interpolation, and file-level NOT MEASURED for unsupported regex/ambiguous operators. Both
repository CLAUDE files, Perch CP/BP and ship-feature/work/gap-fix stages were applied. This is
instrument code only; no Relay implementation, credentials, provider requests or native app work.

- Reproduction: original four fixtures against pinned65119b6 match Perch's preserved baseline:
  the following private helper is wrongly attributed to testHasRead. The repaired fixture reports
  the helper under its own name; it intentionally does not force the entire fixture to blind0.
  Genuine blind and nested-return-only cases stay findings; separate reader remains clean.
- Baseline: exported unchanged65119b6 full gate111passed/2failed, preserved in
  baseline-pinned-gate.log. The earlier baseline-gate.log ran before scanner edits, but the pinned
  export is the authoritative reproducible baseline. Initial focused-first.log is retained history;
  final focused logs carry the expanded14-method suite.
- Work: Swift-only masking and balanced body extraction support comments, raw/multiline/ordinary
  strings, escaped identifiers, recursive interpolation, attributes/modifiers/generic/default
  signatures, nested declarations, protocol bodyless requirements and simple division. Functions
  retain source lines; rejected files carry diagnostic/denominator and always fail --gate, even mixed.
- Gap-fix author review found and corrected a return-type substring boundary (Myfunc), nested
  attribute reader leakage, and ordinary trailing-closure division. Public CLI fixtures exercise
  clean/blind/unsupported/mixed/empty/non-UTF8 outcomes. No product registry or vocabulary edits.
- F25-059 commit877cd1c: only the two stale control diagnostic expectations change; production
  campaign.py unchanged. Child's original green113/0 twice remains distinct from later114/0 runs.
  Its mutation runner overlays current scanner/fixture files on the pinned plugin so the standing
  suite can include the new Swift group without a missing-file failure.
- F25-060 commitc052df7: repair the inherited investor-portal1.3.0/marketplace1.2.0 mismatch only,
  including generated row. Initial catalogue.log and child060 baseline remain failed history.

## Actual checks and negative controls

- focused-green1.log / focused-green2.log:14 test methods, including table-driven cases and
 8 public CLI populations, both exit0. No third-party Python dependencies or Swift toolchain.
- full-gate-green1.log / full-gate-green2.log:114passed/0failed (113 existing checks plus one
 focused14-method group), both exit0. Other-language and provider/requirement checks remain.
- boundary-mutant.json/log: replace the actual body integration with next-declaration slicing in
 a disposable scanner copy; named nested-parent-reader falsifier fails,3 failures overall, exit1.
 Source bytes remain unchanged. This is actual scanner execution, not source-string matching.
- child059-structural-mutant.json/log: mutate actual campaign actuation credit to accept a passing
 structural case. The named below-outcome control fails (exit0 wanted1); production bytes unchanged.
- child060-catalogue-mutant.json/log: put the wrong version into the actual isolated marketplace;
 actual node gate exits1 with the precise mismatch; finally restores original bytes. Both actual
 catalogue green runs exit0,53skills/9groups/53icons. Generated diff is confined to the two plugin
 entries (test-campaign version/README and investor-portal version).
- Captured logs are normalized only by trimming trailing whitespace/blank EOF lines; diagnostic
 text, warnings, counts and failures are retained. All subprocesses use argv arrays. No live
 state or secret data is read. Files are source/tests and synthetic fixtures only.

## CP §7 and limits

Reviewed vacuity-check.py, test_swift_bodies.py, all three arming scripts, run.sh, plugin/marketplace
versions, generated catalogue and accompanying docs. Input rejection, balanced boundaries,
mutation restoration, independent negative/positive controls and failure propagation were checked.
No general semantic Swift parser, macro expansion, control-flow/closure execution, overload
resolution, transitive helper effects or reader independence is claimed. Regex/ambiguous slash
remains explicitly unmeasured; all named functions are candidates, not a runnable-test census.
No arbitrary blanket vocabulary exclusion was added. Reader matching remains deliberately loose.
The new stricter empty-corpus outcome is intentional: zero recognized candidates cannot prove clean.

Perch's final candidate source/commit/hash and exhaustive own-body classifications live in its
F25-050 receipts, not in a replaced installed cache. Preliminary485/484 measured files was fixed
by supporting ordinary trailing-closure division; final485/485 has no lexical exclusion. It still
has105 blind candidates and REQ-056's missing provider, so the complete vacuity gate remains1.
Additional oracle/measurement work is named for intake. These counts are tied to the recorded
Perch snapshot and are not a claim about later main or an independently verified whole product.
