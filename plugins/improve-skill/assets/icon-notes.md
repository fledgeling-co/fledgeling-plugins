# improve-skill — icon notes

**Direction: "The Honed Edge".** The studio's *Tahoe Gel-Glass* sub-register (a), porcelain cushion tile carrying a coloured gel object, crossed with device bank **#16** (the icon performs the verb) and **#5** (dual-function primitive). The subject is a pipeline that takes a skill which already exists and makes it measurably better, so the tile is not a background: it is the workpiece. A worn plane iron lies mid-pass on a rising diagonal, and the surface behind it is visibly brighter and truer than the surface still to come.

Runner-up, and the near-neighbour worth naming: **Direction 7, Diagonal Tool**, which the catalogue calls the single most template-worn move in the corpus. This icon enters that territory deliberately rather than by reflex, and inverts it. In Direction 7 a tool crosses an inert field and the tool is the identity; here the tool has *changed* the field, and the identity is the change. Also considered and dropped: Direction 8 *Instrument Emblem*, because improve-skill has no gauge or chart to quote; its in-product artifact is an eval table, and a table is failure mode #4.

**Glyph (subject-mined, device bank #16 + #5 + #20):** one plane iron and the shaving it took off. The iron is authored as a real extruded solid — a 640 × 204 top face at 33° lifted clear of the ground, with a front face dropping back down to it — occupying 61.5% of the tile. Since round 6 that lift is *pitched*: 48px at the leading end where the iron is buried in the timber, 132px at the trailing end, so the front face is a wedge and the block rides nose-down at a working angle rather than sitting level. Crisp corners on the honed edge because it is sharpened, worn rounding and a shallow sag on the back because it is used. Underneath it, one family of grain lines runs along the travel direction and **crosses the boundary** — torn and broken above it, continuous and fine below it. That continuity is what makes the split read as one surface in two states rather than as two different materials, which is the whole argument: same skill, better. Rising off the iron's top rear edge is the shaving — a ribbon swept along the blade's own axis through an open hook of 0.78 of a turn, 285 × 282 against the blade's 630 × 634, so the blade stays the hero.

**Signature:** the before-and-after boundary *is* the diagonal, and the one vermilion hone line *is* that boundary. Since the round-4 rebuild a single shape does four jobs: the tool's cutting edge, the division between the two states, the icon's only accent, and the line where the solid meets the ground. **Risk taken:** walking into the corpus's most worn composition on purpose, on the bet that inverting it is stronger than avoiding it.

**Palette:** two families only. Warm-neutral porcelain (`#D8D2C4→#ABA391` un-planed, `#FFFDF6→#F1EAD9` trued) and achromatic graphite (`#5D636B→#181B20`), with one vermilion reserved strictly for the honed edge (`#C4341A`/`#FA6231`/`#FF9159`). One soft top-left light plus the sanctioned second source, an emissive interior; here the hone itself, spilling onto the surface it has just cut and lighting the underside of the iron's front face.

**Audit — A ships at 11/12.** `icon.png`, `icon-256.png` and `icon-128.png` all render from `icon.svg`. The one deduction is at #4, and it is the price of the shaving; see below. Full scores, verdicts and evidence: `audit.html`.

## Round 4 — the material rebuild, and what it fixed

For three rounds the sheet carried a split: the highest-scoring take was the vector master A, and the take the user chose on look was the raster C2. The split is now closed, and it closed by **rebuilding A rather than re-scoring anything**. The brief was to pull the vector toward the best of both rasters — C2's whetstone mass, both rasters' warm textured ground, a genuinely emissive hone, a deep soft contact shadow — while fixing the flaw the rasters shipped with.

**What materially changed.** The flat bar became a solid. It is now an extruded body: the top face is lifted 88px clear of the ground and a front face drops back to it, with a chamfer highlight between the two faces and a rim light along the worn back. The geometry is derived, not drawn twice — the outline is flattened to a polyline in the blade's local frame, the lower silhouette chain is swept straight down, and the ground contact of that sweep *is* local y=0, so the solid physically cannot drift out of register with the boundary or the hone. Both ground planes gained warm mottle pushed through a heavy blur over the boundary-crossing grain. The hone became real light: a heavily blurred spill on the trued plane, a warm bounce running up the front face, and a hot core line. The shadow became a deep soft cast plus a tight contact occlusion.

**What got fixed.** Polarity — the flaw C2 shipped with. The trued side now measures **L 0.920 against the un-planed side's L 0.746, +0.174 the right way round**, where C2 measured 0.653 against 0.696, **−0.043 inverted**. The side that is meant to be better now reads better. Measured by `measure.py` on the real render, every iteration, not eyeballed.

**The #10 point, earned back on evidence.** Every previous round deducted a point at variant robustness: under an aggressive monochrome tint that collapses mid-tones, the blade and the un-planed field flattened to one value and the cutting edge was lost. Rendered side by side with the old master under both a mono and a dark tint, the old one still collapses and the rebuilt solid holds. The reason is structural: identity is now a **form** relationship — a lit solid casting a shadow onto a two-state ground — rather than a colour relationship. The raw field ratio under that tint is a modest 1.69:1; what carries the read is the chamfer, the rim light and the cast shadow, none of which a tint can remove.

**Measured on the shipped renders:** focal 61.5% of tile, optical centre (494, 457), safe-zone margins L179 R216 T140 B250 — re-measured after the round-8 depth change (they read 58.7%, (508, 479), L207 T184 through rounds 6 and 7). Figure-ground was 4.67:1 against the un-planed side and 7.56:1 against the trued side with the fields 1.62:1 apart when this was first taken at round 4, and the pitch leaves all three unchanged within measurement noise (see round 6 below). Four real layers named `#bg` / `#mid` / `#fg` / `#highlight`, mapping 1:1 onto Icon Composer. Full-bleed 1024 artwork with the squircle as a *clip*, no baked corners or shadow. Edit `build_icon.py`, never `icon.svg`.

**The shaving curl, cut three times, and what made the fourth work.** Three rounds failed because a spiral **outline** is the wrong model. An outline is a closed curve with a hole in it — a shell — and no amount of redrawing it makes it material. What ships is a swept **surface**: one cross-section curve (a nearly straight tail leaving the blade, easing tangentially into a loose open hook) swept along the blade's own axis by the ribbon's width, then cut into 96 bands, each shaded by its real facing angle to the single top-left light. Bands whose outer face turns toward the viewer are lit; the ones on the far side are seen from the *inside*, through the open end of the roll, so they take the shadow family plus a transmitted lift where the outer face is in light. The free end tapers in opacity, because that is the thinnest, most-curled material and the ground has to show through it.

Two things were measured off C2 rather than invented. **Its cross-section is a circle seen obliquely**, so the section here is compressed to 0.54 along the roll's axis; drawn as a true circle the mouth traces a full ellipse and the whole thing reads as a capped tin can. And **its curl is not a pale shape on a dark ground** — its lit top measures L 0.576 against ground of L 0.635 right beside it, falling to L 0.27 at the bottom, so it reads by form-shading and hairline cut edges, never by a value jump. That is the trap all three failed attempts fell into. This one measures **1.25:1 against the un-planed ground it was shaved from, against an internal modelling range of 3.66:1** — nearly all of its legibility is its own shading. Staying under a full turn matters twice: past one turn the swept ribbon closes into a tube and reads as a roll of tape, and an open hook is what *partially unrolled* actually looks like.

**What it costs, honestly: one point, at #4.** Rendered at 16px beside the no-curl master, the blade's dark diagonal, the vermilion hone and the two-state split are all completely intact — identity is not damaged, which is why this is a deduction and not a hard fail — but the curl contributes a small low-information tan patch above-left of the blade where the previous master had clean empty ground. It simplifies to a soft pale note, as intended, and it is still noise. It repays that at #11: the tile now carries the physical evidence that the plane cut. Polarity is unharmed and marginally better at **+0.184** (trued L 0.920, un-planed L 0.737), up from +0.174, and the blade and both fields are geometrically untouched so figure-ground is unchanged. `SHAVING=0` in `build_icon.py` rebuilds the round-4 two-object tile.

## Round 6 — the pitch: why it still didn't look like planing

The whole brief was one sentence: *"It doesn't look like wood is being planed because the angle of the block is flat unlike C2."* That was right, and the diagnosis was cheap once stated. Round 4 lifted the top face by **one constant rise**, so the top face was a parallel copy of the footprint and the front face was a band of even height. The iron had mass and no attitude: a bar lying on the boards, not a tool taking a shaving off them.

**What was measured off C2 rather than invented.** Its ground/hone line runs **38.9°**, its top-face shoulder runs **41.9°**, and its front face grows from about **55px deep at the near end to 90px at the far end**. The front face is a *wedge*, not a band, and that wedge is the entire tell — it says the block is riding nose-down, buried in the timber at the leading end and lifted behind. (Two independent detectors on C2 bracket the taper at 1.6:1 to 2.6:1 and the shoulder-vs-ground offset at +3.0° to +5.8°; the shipped value sits inside both.)

**What changed, and only this.** The lift is now **linear in local x**: `RISE_NEAR = 48` at the leading end, `RISE_FAR = 132` at the trailing end, **2.75:1**. Because a linear lift stays affine, the lifted top face is still **one matrix** — `MATRIX_TOP`, a screen-vertical shear of the blade's own frame — so the stone texture, the top-face gradient, the grind striations and the rim light all ride it with no second transform and no chance of drifting off the metal. The chains and faces are now derived by *index* so each top-face point drops to its own footprint point rather than to a constant offset. Top-face edges come out at **38.9° against the hone's 33°, +5.9° of pitch**, matching C2's shoulder-versus-ground offset. The front-face gradient is re-anchored by *absolute distance from the cutting edge* rather than by fraction of the rise, so the hone's falloff is identical whether the wedge is 48px deep or 132px there.

**What deliberately did not move.** The footprint, the cutting edge and the before/after boundary all live at local y=0 and the shear leaves local y=0 fixed, so the signature line cannot drift — and the banner's derived split needed no re-derivation. `ANGLE` stays 33° and `EDGE_MID` stays (543, 604) for the same reason: changing them is a different edit class and would kink the banner. The curl construction is untouched; its base is now held in the blade's *local* frame (`CURL_BASE_L = (289, 130)`) and mapped through `to_top`, so the tail's exit point rides the pitch instead of floating off the metal. Verified on a 4× crop of the junction — the tail still plunges behind the worn back edge exactly as it did before.

**Scored, not asserted.** Against C2 (`icon-engineC-f5665d-2.png`) on the create-mac-icon fidelity harness, runs in `fidelity-runs-block/`:

| size | before (flat) | after (pitched) | delta |
|---:|---:|---:|---:|
| 1024 | 0.3504 | **0.3623** | +0.0119 |
| 256 | 0.3334 | **0.3495** | +0.0161 |
| 128 | 0.3436 | **0.3567** | +0.0131 |
| 32 | 0.6747 | **0.6898** | +0.0151 |
| 16 | 0.7500 | **0.7595** | +0.0095 |

Every size improves; the Pareto gate **ACCEPTs at +0.0657 net** with nothing regressing. A parameter sweep over seven near-neighbour pitches picked 48/132 on the small-size floors (32 and 16 are its best sizes of the whole sweep) with the taper landing at the top of C2's measured range rather than past it.

**Polarity is untouched: +0.177** (trued L 0.920, un-planed L 0.743), against **+0.178** for the flat build measured the same way — `measure.py icon.png 33.0 543 604 640`. Figure-ground is unchanged within noise: one sampler reads 6.20:1 against the un-planed side after the pitch versus 6.34:1 before, and the two fields sit 1.75:1 apart either way.

**What it costs.** The trailing end stands 42px taller, so the top safe-zone margin goes **226 → 184** (L207 R216 B250 unchanged) and the optical centre lifts to **(508, 479)**; focal is still 58.7% of the tile. 184px is 18% of the canvas, so this is budget spent rather than a deduction. **Audit holds at 11/12** — the #4 deduction is the curl's small-size noise and the pitch neither worsens nor repairs it. At 32px and 16px the dark diagonal, the vermilion hone and the two-state split are all intact, and the wedge is still legible at 32.

**One latent bug fell out of the same edit.** The `topFace` gradient was still anchored to the *un*-lifted blade frame, so it had been 88px out of register with the face it filled since round 4. Rebuilding the flat geometry (`RISE_NEAR = RISE_FAR = 88`) with the fix in place scores **+0.0246 net** on its own — the pitch's +0.0657 includes it.

## Round 7 — the material round: the scene has no cool light in it

Instrumented round against C2 on the create-mac-icon fidelity harness (`loop-runs/r00-baseline/` -> `loop-runs/r01/`), with the fixture's declared envelope `--max-paths 3000 --max-bytes 350000` — the swept ribbon's 96 bands are construction, not path soup. The brief was the shape of the baseline numbers: 16px and 32px were already close to C2 (0.76 / 0.69) and 1024/256/128 were not (0.36 / 0.35 / 0.36). Composition had converged; material had not.

**What was measured off C2, not assumed.** Five readings, each taken in C2's *own* hone frame (its line fits 38.93 deg from (392,681) to (670,456)) so the two icons could be compared at matching heights above the cutting edge rather than at matching canvas pixels.

1. **The dark end's hue is inverted.** C2's darkest 1% of block reads rgb **(0.155,0.132,0.119) — R>G>B, warm**; the master's read **(0.096,0.108,0.128) — B>G>R, cool**. Saturation *magnitude* was almost identical (0.236 vs 0.246), so a saturation check passes and a range check passes; only sampling the dark end's actual channel order catches it. This is the create-mac-icon "check the dark end's hue" finding, arriving with the sign flipped: not desaturated, mis-hued. Every stop of `topFace` ran #2E3238 -> #5D636B with B ten points above R, and the rendered top face measured a cool cast (sat 0.13-0.15, B>R) at *every* point of *every* cross-section. The comment above that gradient had said "warm-leaning graphite, not blue steel" since round 4; the constants never said it.
2. **The hone was a tube, not a cut.** Sampled along the line itself, C2 holds L 0.95-0.98 for the leading 30% of the blade and then falls off a cliff — 0.91 / 0.74 / 0.53 / 0.30 — extinguished into the ground by about 62% along. The master measured a **dead-flat L 0.89 for all 640px**. C2 is physically right here for the icon's own reason: the glow is the cut happening, and the cut is where the iron is buried in the timber at the leading end.
3. **The front face had no along-length shading.** At 15px above the cutting edge the master read a flat L 0.281 end to end; C2 runs 0.274 -> 0.138 over the same span, i.e. it halves. Same physical fact as (2), applied to the surface the hone lights.
4. **The chamfer was a pinstripe where C2 has a trough.** Across three cross-sections, C2's top/front junction is the **darkest** part of the whole section over the leading two thirds (0.245 -> 0.209 -> 0.201 going up through it) and only becomes a rolled highlight near the trailing end, at +18% and warm. The master carried a 4.4px #848E9C stroke at 0.56 — cool, and +72% over the face beside it.
5. **The ground's brightest point was in the wrong corner.** Measured corners: C2 TL 0.869 / TR 0.509 / BL 0.556 / BR 0.562 — one key, up and left, every corner away from it at 0.6-0.65x. The master: TL 0.780 / TR 0.678 / **BL 0.932** / BR 0.848. Its brightest ground sat in the corner *furthest from its own light*. That is a single-light violation in the master, not merely a mismatch with C2 — which is why fixing it is a rubric gain and a score gain at once.

**What changed, all of it material.** One class, `build_icon.py` only. `topFace` re-hued to warm-neutral graphite with **the luminance of every stop held to within 0.01**, so the cast moves and the modelling does not. `frontFace` re-hued warm and its floor lifted off black (L 0.106-0.128 -> 0.15-0.23) — the master's ground is *brighter* than C2's, so its bounce should be stronger, not weaker. A new `honeFall` mask carries C2's measured along-length falloff onto every layer the hone owns at once — wide bloom, glow, core, specular — so they cannot drift apart, plus `frontFall` on the face it lights and `seatRamp` for the seat's own occlusion (C2's ground reads 0.66x its far field under the lit leading end and 0.41x under the trailing end; the master was a flat 0.59-0.60x). The pinstripe became a soft warm occlusion trough plus a roll that ramps in toward the trailing end. Rim light, grind striations and the stone mottle all re-hued off blue. Fields and vignette re-aimed onto the key.

**Deliberately not followed: C2's register.** Its two ground fields sit at 0.65 and 0.65 — C2 has essentially no polarity, which is why it fails #11. Roughly two thirds of the remaining `lum_delta` is the master's trued field standing 0.26 L above C2's, and closing that would delete the icon's signature. Only the part of it with a *physical* reason — the falloff away from the light — was taken. This is the Ledgerline rule: fade where no boundary lives, never across one.

**Scored, not asserted.** `loop-runs/r01/score.json`:

| size | before | after | delta |
|---:|---:|---:|---:|
| 1024 | 0.3623 | **0.3950** | +0.0327 |
| 256 | 0.3495 | **0.3869** | +0.0374 |
| 128 | 0.3567 | **0.3940** | +0.0373 |
| 32 | 0.6898 | **0.7098** | +0.0200 |
| 16 | 0.7595 | **0.7748** | +0.0153 |

Pareto gate **ACCEPT at +0.1427 net**, nothing regressed — more than double the round-6 pitch's +0.0657. `lum_delta` 0.1839 -> 0.1623 and SSIM rose at all five sizes (1024: 0.6392 -> 0.6506). `edge_f1` fell slightly at 1024/256/128/16 (1024: 0.0564 -> 0.0480): that is the pinstripe leaving, and it is paid for several times over. The 1024 residual's whole lower-right quadrant drops from 0.27-0.35 to 0.20-0.29.

**What it costs.** Polarity **+0.177 -> +0.146** (trued L 0.878, un-planed 0.732) — the trued field's far corner is where the falloff lands, and the sign and legibility are untouched (C1 hard-failed #11 at 0.09). Figure-ground at 128px goes 7.38:1 -> 5.27:1 as the front face comes off black and the ground comes off white; C2's own is 2.77:1, so the master still has nearly twice C2's separation. The vermilion's 16px footprint drops from 5.47% to 3.91% of the tile — the one place the hone falloff bites. **The falloff therefore holds a floor at 0.30 rather than following C2 to zero**, which is the round's one deliberate refusal to converge: C2 extinguishes its hone and loses the boundary, and the boundary is the whole icon. Verified at 16px, where the dark diagonal, the vermilion and the two-state split all still read. **Audit holds at 11/12**; the #4 deduction is still the curl's small-size noise, untouched this round. Structure 2434 -> 2437 paths, 297KB -> 308KB, gradients 8 -> 12, filters 9 -> 11.

**Reusable recipe — "single-emitter falloff mask".** The hone change is the transferable one, and it generalises past this icon. Where an object is lit by an *emitter it carries* (a glowing edge, a screen bezel, a filament, a heated tip), author the emitter's decay **once** as a `linearGradient` of white with varying `stop-opacity` along the emitter's own axis, wrap it in a `<mask>` over the full canvas, and apply that one mask to the group holding *every* layer the emitter owns — bloom, glow, core, specular, and the spill it throws on the ground. Cost is one gradient and one mask; the payoff is that the emitter's falloff, the shading of the surfaces it lights, and the ground's brightness beneath it can never drift out of agreement, because they are one number. Give it a floor rather than letting it reach zero whenever the emitter is also carrying identity at small sizes.

## Round 8 — the coarse-structure round: the curl was the wrong solid

`loop-runs/r04/`, measured against `loop-runs/r01/`. Edit class: coarse structure — silhouette, centring, object scale, attitude, major colour fields. The human's review named the curled flake twice as the priority and noted it had not changed across rounds; that is squarely this class, so it was the round's whole job. The same review's other two items — the left side's lighting and texture, and re-applying the r02 fibre — are fine material work and were deliberately left for the round that owns them.

**Where the gap actually was.** The candidate's curl silhouette was isolated exactly by differencing a `SHAVING=0` render against a `SHAVING=1` one: bbox x[183,467] y[232,483], 284×251, centroid (313,361). Tracing C2's curl by gradient failed outright — the flake is only about 1.25:1 against the ground it sits on, so the board's texture swamped every edge filter. What worked instead was **fitting circles through three points read off each rim arc** and drawing the solved circles back onto the reference to check them (`loop-runs/r04/fitcheck.png`, near-exact):

    near rim   centre (294, 253)   R 115     residual < 2px over a 240px arc
    far  rim   centre (359, 186)   R 121     offset d = (+65, -67), |d| = 93.3, bearing 45.9 deg

Three facts fall out of that fit, and the master disagreed with all three.

1. **Both rims are circles.** Under orthographic projection a 3D circle projects to a circle only when its plane is parallel to the image plane, so C2's roll axis points essentially at the viewer and there is **no in-plane foreshortening of the section left to draw**. `CURL_FORE` had been compressing it by 0.54; it is 1.00.
2. **The roll is a hoop, not a pipe.** Centre-to-centre 93px against R 115 is 0.81:1 — diameter to length 2.5:1. The master was built at 106/78 = 1.36:1 and read as a length of pipe lying on the boards. `CURL_R` 78 -> 115, `CURL_SWEEP` 106 -> 93.
3. **The roll has tipped off the edge it came from.** The rim offset subtends 45.9 deg, not the blade's 33. A new `CURL_TILT = 12.9 deg` rotates the sweep axis alone (`TUX,TUY`) and leaves the blade frame untouched.

Fourth, read off the crop rather than fitted: C2's ribbon leaves the blade as a **~40px stub off the roll's lower flank**. The master's `CURL_PHI0 = -24 deg` entered on the upper-right flank and needed a ~150px straight chute that C2 does not have; it is now +34 deg, on the flank that faces the blade.

**Positioned relative to the blade, not copied as canvas pixels.** The master's blade is 1.38× C2's in screen terms (689.5px back edge against 500px) and sits lower-right, so C2's canvas coordinates are meaningless here. C2's roll clears its own blade's back edge by 53px; `CURL_C` was sited at (308,278) so this one clears **this** blade's back edge by 50. `CURL_BASE_L` moved to local y 182 to hold the same 22-unit inset inside the back edge after `BLADE_THICK` went to 204.

**One correction worth recording.** A first attempt also flipped the sweep sign, on the belief that the roll receded the wrong way; the render came out as an opaque disc. Deriving the ruled-strip normal properly — N = T x S, so N_z = -((radial) · (projected sweep)) — showed the original code was already self-consistent provided the array named `far` is in fact the *nearer* rim. The sweep sign was restored and only the measured tilt kept. The visibility test was rewritten from `(n · U) > 0` to `(n · SU) < 0` so it tracks the tilted sweep axis; the two are identical at zero tilt.

**Not this round's edit, but in this round's class:** `BLADE_THICK` 152 -> 204 arrived externally mid-round, justified in its own comment as the mean of 204/190/218 measured on C2's top/front shoulder over the leading two thirds. It was kept and built on, and it moves the solid from 58.7% to 61.5% of the tile.

**Scored, not asserted.** `loop-runs/r04/score.json` against `loop-runs/r01/score.json`:

| size | before | after | delta |
|---:|---:|---:|---:|
| 1024 | 0.3972 | **0.4138** | +0.0166 |
| 256 | 0.3958 | **0.4207** | +0.0249 |
| 128 | 0.4096 | **0.4486** | +0.0390 |
| 32 | 0.7456 | **0.7577** | +0.0121 |
| 16 | 0.7923 | **0.8055** | +0.0132 |

Pareto gate **ACCEPT at +0.1058 net**, nothing regressed at any size on any sub-metric. SSIM up at all five (1024 0.6506 -> 0.6562, 128 0.4966 -> 0.5151), `lum_delta` down at all five (1024 0.1623 -> 0.1565), and `edge_f1` — the metric a shape change should move — up hardest: 1024 0.0566 -> 0.0815 (+44%), 128 0.3372 -> 0.4283. Structure PASS at 2443 paths / 308KB, inside the declared 3000 / 350000.

**What it costs.** The curl's bbox grows from 262×250 to 285×282 and moves up-left to x[137,422] y[167,449], so **the flake, not the blade, is now the artwork's leftmost point**; with the thicker blade the safe-zone margins go L207/T184 -> L179/T140. That is real budget spent and worth watching if anything else ever moves left. Split polarity **+0.146 -> +0.144** (trued L 0.878, un-planed 0.735) — sign and legibility untouched. Measured through one pipeline for fairness, 128px figure-ground 6.02:1 -> **6.19:1** and the 16px vermilion footprint is **unchanged at 5.47%**, so the human's condition that small sizes must not regress is met — and the 32/16 composites rose rather than held. Audit still 11/12; the #4 deduction was the curl's small-size *noise*, which is fine material, so this round does not clear it.

**Deliberately not converged.** C2's flake is far larger relative to its blade than this one is. Matching that ratio literally means shrinking the blade, which is out of this round's class and against the direction's rule that the blade stays the hero. The roll's *shape, attitude and proportion* were taken; its share of the tile was not.

**Reusable construction — "fit the rims, then read the solid off the fit".** For any rolled, coiled or tubular form being matched to a raster reference where edge detection fails on low contrast: do not trace the silhouette. Read three points off each visible rim arc, solve the circle through them, and draw the solved circles back over the reference to verify. The two circles then hand you the whole solid for free — **equal radii and circular projection means the axis faces the viewer, so foreshortening is 1.0; centre separation over radius is the length-to-diameter ratio; and the bearing of the centre offset is the axis attitude, which is generally *not* the parent object's axis.** Three numbers, one verifiable drawing, and no assumption about what the material "probably" looks like.

## Round 8, second track — coarse structure: the iron was a bar, not a block

Two coarse-structure tracks ran on this fixture at the same time, which is why there are two round-8 entries. The section above is the curl fit; this one is the blade's depth. Each saw the other's constant arrive "externally" mid-round. The shipped `icon.svg` (md5 `d675ed0d776e`) is **both** edits together, and the gate below was run on that combined file; this entry also isolates what the depth change does on its own, built from `loop-runs/r04/_before/` in a scratch directory (`loop-runs/r04/attrib.py`), because a shared score.json cannot otherwise be attributed. Same harness, same declared envelope `--max-paths 3000 --max-bytes 350000`.

**What was measured off C2, not assumed.** Round 7 read C2 in its own hone frame; this round did the same and asked one question — how deep is its top face? The tempting subtraction is wrong. C2's silhouette back edge sits at a stable ly 248–260, but that figure carries the front face's lift with it, and the master's wedge (48→132) is deeper than C2's (55→90), so no single depth matches the apparent back edge along the whole length. The number to trust is **(back edge) − (top/front shoulder)** on the same cross-section, because the rise cancels out of the subtraction. Taken on three sections over the leading two thirds — the span where round 7 established the shoulder is a readable trough rather than the rolled highlight it becomes at the trailing end — it reads **204 / 190 / 218, mean 204**. The master's top face was **152**: 4.2:1 in plan against C2's 3.1:1. The iron was a slim bar wearing a wedge, and no amount of material work fixes a proportion.

**What changed, and only this.** `BLADE_THICK = 152 → 204`. Everything that consumes it already derived from it — the outline, the stone texture's local-y range, the `topFace` gradient's `y2`, the grind striations — so one constant moved and the solid, its texture and its shading all followed. One latent registration hazard fell out and was closed in the same edit: `CURL_BASE_L` held a hard-coded local y of 130, which was 22 units inside a 152-deep face and would have sat in the *middle* of a 204-deep one, so the shaving would have emerged from the top of the iron instead of over its worn back. It is `(289, BLADE_THICK - 22)` — derived, so a future change of face depth cannot break it. Checked on a 3× crop of the junction, not assumed. (The other track re-wrote it as the literal `182`, the same value; the derived form was restored and the render is byte-identical, so nothing about the artwork turns on this — only the next depth change does.)

**What deliberately did not move, and why the obvious edit was refused.** `ANGLE` stays 33°, `EDGE_MID` stays (543, 604), `BLADE_LEN` stays 640, `RISE_NEAR/FAR` stay 48/132. C2's hone line fits **38.92°** against the master's 33°, and a raster registration probe — warping the candidate onto the reference — said rotating to 38.9° was the single largest available gain (1024 ssim 0.6506→0.6667, edge_f1 0.048→0.111). **Built for real, `ANGLE = 38.9` scored −0.0299 net: a rejection.** The probe lied because the warp rotates the squircle, the vignette and the ground fields with the blade and leaves gaps behind; it does not model what changing `ANGLE` actually does. Every candidate after that was a real build scored by the harness. Shortening to C2's ~562 also hurt, and cost focal size (58.7% → 52.4%). Both were also banner-breaking: the split at `banner-src.html` is derived from A's 33° boundary and would need re-deriving.

**204 rather than the peak.** The composite keeps climbing past the measurement — 212 scores +0.1557 against 204's +0.1298 on the same v1 sweep — and 204 ships anyway, because 204 is what C2 measures and 212 is outside the bracket the three sections support. Tuning the constant against the composite without a physical reason is how this loop breaks.

**Scored, not asserted.** The harness moved to `METRIC_VERSION = 2` mid-round (edge_f1 now excludes the squircle rim) and refused a mixed comparison, correctly; the r01 baseline was re-scored under v2 and every number below is v2. The round-7 table above is v1 and is not comparable to it.

| size | before (r01, v2) | after (shipped, both tracks) | delta | depth change alone |
|---:|---:|---:|---:|---:|
| 1024 | 0.3972 | **0.4169** | +0.0197 | 0.4172 |
| 256 | 0.3958 | **0.4253** | +0.0295 | 0.4260 |
| 128 | 0.4096 | **0.4613** | +0.0517 | 0.4622 |
| 32 | 0.7456 | **0.7691** | +0.0235 | 0.7690 |
| 16 | 0.7923 | **0.8087** | +0.0164 | 0.8085 |

Pareto gate **ACCEPT at +0.1408 net** on the shipped file, nothing regressed — on a par with round 7's material pass, and from a single constant. `lum_delta` 0.1623 → 0.1549, SSIM up at all five sizes (1024: 0.6506 → 0.6588), and `edge_f1` makes the largest move: 0.0566 → 0.0809 at 1024, 0.3372 → 0.4473 at 128, 0.9129 → 0.9293 at 32. `self_contrast` rose at every size (1024: 0.5477 → 0.5602), so the contrast floor is not in play. Structure PASS at 2443 paths / 308,255 bytes / 12 gradients / 11 filters on the shipped file (2437 / 307,687 for the depth change alone — it adds no paths, because nothing was added).

**The attribution, since two edits share one score.** The depth change on its own scores **+0.1424 net**; the shipped pair scores **+0.1408**. Effectively all of the round's composite gain is the blade's depth, and the curl fit costs 0.0016 on top of it — noise, and consistent with r02's finding that this composite cannot currently see a better-shaped small object as an improvement. That is an argument for keeping the curl work on its rubric merits, not against it; it is recorded here only so the +0.1408 is not read as evidence for the curl. (The curl entry's own table, 0.4138 at 1024, does not match the shipped file, which scores 0.4169.)


**What it costs.** The deeper face reaches further up and left. Taken from the build's own reporting, on its own long-standing convention: the solid goes from 58.7% to **61.5% of the tile** (bbox x 179–808, y 140–774), its focal centre lifts to **(494, 457)** from (508, 479), and the safe-zone margins go **L207 T184 → L179 T140** with R216 and B250 unchanged. 140px is 13.7% of the canvas, so the top margin is now the tightest it has been and is budget worth watching — the curl track spends against the same left edge, and between them the artwork's leftmost point is the flake at x 137. Figure-ground softens as more top face — lighter than the front face — comes into view: the block's median L goes 0.250 → 0.269, so it reads 2.73:1 against the un-planed field and 3.26:1 against the trued one, against C2's ~2.80:1. Polarity is untouched at **+0.144** (trued L 0.878, un-planed 0.735) by `measure.py icon.png 33.0 543 604 640` — the fixture's invariant holds, trued side brighter. **Audit holds at 11/12**, the #4 deduction still the curl's small-size noise, which is fine material and not this class. The gate and the rubric agree this round — no disagreement to report — and the 16px squint row in `loop-runs/r04/cmp-r04.png` shows the dark diagonal carrying more mass than before, closer to C2's. One honest mismatch remains and is now *more* visible, not less: the master's blade is longer than C2's, so its bbox (630 × 634) overshoots C2's (567 × 536) even as its filled area moves toward it (9.2% → 10.9% of tile against C2's 13.6%). Length is a separate edit class and scored worse when tried.

**Reusable recipe — "measure depth where the rise cancels".** For any icon whose subject is an extruded solid seen in three-quarter view and lifted or pitched off its ground, do not take the object's depth from its silhouette: the far silhouette edge carries the lift, so it changes with attitude and gives a different answer at every station along the object. Take it instead as **(far silhouette edge) − (near face's shoulder)** on a single cross-section perpendicular to the object's own axis, sampled in the object's own frame rather than in canvas pixels, and average over the span where the shoulder is a *trough* rather than a highlight. The subtraction is attitude-independent, which is exactly what makes it comparable between two renders that do not share a pitch. Cost is one perpendicular-ray sampler (`loop-runs/r04/an9.py`, ~40 lines of numpy); the payoff is a depth constant you can defend against the score rather than fit to it.

## Round 9 — the material round: the top face was a lighter grey than the stone it was made of

**What was measured off C2.** Region means were the wrong instrument here — the master's blade is ~1.38× C2's and sits elsewhere, so any mask built from our geometry mixes material error with structural non-overlap. The round was decided on a **cross-section profile instead**: perpendicular to *each block's own hone*, at five stations along the length, fifteen depths from below the cutting edge up to the back silhouette (`loop-runs/r06/work/an10.py`). Read that way the answer is unambiguous and it is not where the residual map points. Our **front face matches C2 to within 0.05 over its entire body** (depths 20–56px, −0.05 to +0.03 at every station). Our **top face runs +0.05 to +0.14 too light at every station** (depths 72–190px). Face means: ours 0.338, C2's 0.243. Its across-depth ramp was overdone too — ours climbed +0.101 from hone edge to back edge where C2's climbs +0.068 — and the top/front junction stepped **+0.176** on ours against C2's **−0.018**, i.e. C2 has no step there at all over the leading two thirds. The darkest 3% of our top face was rgb (0.210, 0.209, 0.187), **sat 0.110**; C2's is (0.173, 0.145, 0.131), **sat 0.240**. The face was desaturating into shadow — the exact fault round 1 caught on the ground, still living on this one face.

**Getting the reference's hone line right, and the near-miss that followed.** The split line was first fitted on "warm and bright" pixels and caught the timber, giving 41.7° — wrong diagonal, and every face split taken from it was wrong. Refitted on **chroma** (`R − (G+B)/2`, top 0.5%, one outlier-trim pass): **y = −0.8026x + 991.2, 38.75°**, residual sd 7.2px, and validated by running the identical method on our own render, which returned **33.05°** against the build's 33.00°. On the corrected line, region means then claimed C2's front face (0.340) was *lighter* than its top face (0.243) — an inverted per-face separation, and nearly the edit this round was built on. It is an artifact: the extended straight hone cuts through the trailing-right part of C2's block, so "below the line" collects top-face and edge pixels. Re-measured by perpendicular distance above each block's own hone *inside the block mask*, C2's front face runs 0.28 → 0.16 and the separation is the ordinary one. **Measure inside the mask, not against an extended line.**

**What changed, and why the ground did not.** One axis: the top face's own material. The four `topFace` stops are the old ramp under **L′ = 0.245 + 0.70·(L − 0.338)** — the affine that lands the rendered face on 0.215 → 0.283 across depth, which are C2's own two numbers — with the offset-0 stop set to C2's *measured* darkest colour rather than a desaturated grey. Every additive layer on that face was scaled with it, because at a fixed opacity each reads as a **bigger** step once the base drops 0.09: `topSheen` 0.13 → 0.06, the two grind striations 0.16/0.09 → 0.08/0.045, the back rim light 0.52 → 0.30. The rim's new opacity is not tuned: C2's top face peaks at 0.389 in its brightest 3% and 0.30 is the value that puts our rim on 0.389.

The ground was deliberately left alone, and that is the round's real decision. The **contrast budget** made this a live conflict: the master's 32/16 `self_contrast` sat at 0.553 against a 0.520 floor, and r05 was rejected outright for spending it. Tracing where the percentiles come from settled it — at 16px **p90 is 100% trued ground** and **p10 is 54% top face / 46% front face**. Darkening the ground therefore pays for its own convergence out of p90, which is how r05 died; darkening the **block** lowers p10 and *buys* contrast. Two ground hypotheses were also measured and rejected before they cost a round: C2's contact profile around its own block runs 0.727× far-field at 0–15px and ours already runs 0.738× at 0–20px, so **no contact-shadow or AO work was warranted**; and C2's trued ground (0.610) is *darker* than its rough (0.642), so converging its ground polarity would invert the icon's signature and break rubric #11.

**Budgeted before a constant moved.** `loop-runs/r06/work/sim.py` applies the proposed transform to the rendered 1024 PNG and recomputes `fidelity.py`'s own metrics, with the baseline printed both simulated and real as calibration (agreement within 0.006 at every size). Target mean 0.245 / squash 0.70 predicted **+0.0515 net** with `self_contrast` *rising* to 0.64/0.63; the built file scores **+0.0547**. The measured value and the best-scoring value coincide — the composite is flat from 0.245 to 0.225 (+0.0515 vs +0.0514) — so the constant is C2's number and the plateau is a check on it, not its source.

| size | before (r04) | after (r06) | delta |
|---:|---:|---:|---:|
| 1024 | 0.4138 | **0.4228** | +0.0090 |
| 256 | 0.4207 | **0.4351** | +0.0144 |
| 128 | 0.4486 | **0.4680** | +0.0194 |
| 32 | 0.7577 | **0.7622** | +0.0045 |
| 16 | 0.8055 | **0.8129** | +0.0074 |

Pareto gate **ACCEPT at +0.0547 net, nothing regressed at any size**. `lum_delta` falls at all five (1024: 0.1565 → 0.1504), SSIM rises at all five (1024: 0.6562 → 0.6604; 32px 0.5497 → 0.5658), and `self_contrast` **rises** rather than falls — 32px 0.553 → 0.645, 16px 0.553 → 0.631, both far above the 0.520 floor, which is the whole argument for having picked the subject over the ground. Structure PASS at 2443 paths / 310,230 bytes. `edge_f1` is the one metric that gives anything back: 0.0815 → 0.0768 at 1024 and 0.9066 → 0.8970 at 32, as the darker top face stops throwing a spurious Sobel response at the junction C2 doesn't have. Luminance and structure more than cover it at both sizes.

**What it cost.** Nothing measurable, which is unusual and worth stating plainly. Polarity holds at **+0.143** (trued 0.878, un-planed 0.736) by `measure.py icon.png 33.0 543 604 640` — the fixture's invariant is intact, trued side brighter, and it is untouched at three decimal places because no ground constant moved. Figure-ground **improves**: the block's L drops 0.307 → 0.241, so at 128px it reads **3.48:1** against the trued field (was 2.73:1) and **2.86:1** against the un-planed (was 2.25:1). The 16px vermilion footprint is bit-identical before and after (2.34% of tile above 0.20 chroma) — the hone line was not in scope and was not touched. **Audit holds at 11/12**, the #4 deduction still the curl's small-size noise. Gate and rubric agree; no disagreement to report. Rendered check: the face now reads as dark stone taking a grazing light rather than moulded plastic under a spotlight (`loop-runs/r06/work/junction-2up.png`, `after-3up.png`). One target is short and is left short: the along-length swing is **1.32:1** against C2's **1.17:1**. Taking the sheen further would close it, but the sheen *is* the single-emitter read, and the remaining swing is honest falloff from a key that is genuinely off the trailing end.

**Reusable recipe — "find out which pixels own your percentiles".** When a gate constrains a global statistic — a self-contrast floor, a mean, a histogram spread — and the round's candidate edits all move luminance, spend ten lines finding out *which regions supply the percentiles the gate reads*, before choosing what to edit. Mask each region, take the pixels at or beyond each percentile, and report the region's share of them: `sel = (g >= np.percentile(g, 90)); {name: (m & sel).sum() / sel.sum()}`. The result partitions your candidate edits into ones that spend the budget and ones that pay into it, and the partition is frequently the opposite of intuition — here the bright ground owned p90 outright while the subject owned p10, so darkening the *subject* raised the very statistic that darkening the *ground* had destroyed a round earlier. Cost is one masked percentile pass; the payoff is not discovering the conflict after the render.

**One thing worth recording about the run itself.** Between this round's score and its final re-score, `build_icon.py` acquired ~120 lines it was not given — an `feDiffuseLighting` surface-relief system (`fibreRelief`, `pitRelief`, an `AMP_LAW` amplitude table and a derived `fibreRamp` mask), authored by something other than this round and on top of its output: its own `PIT_SCALE` comment reads "the face already carries 6.38% once ROUND 8 darkened it", i.e. it was written against this round's darkened face. It was caught because the structure counts moved — 12 gradients / 11 filters / 310,230 bytes became 13 / 13 / 314,933 — and the net fell from +0.0547 to **+0.0269** with it. The shipped file is this round's four edits on the `_before` snapshot and nothing else; the foreign version is kept intact at `loop-runs/r06/work/build_icon.foreign-relief.py` rather than discarded, since the relief idea is a reasonable one and the measurement it cites is real. It is simply not this round's class and was not this round's to score. **Verify the structure counts, not just the composite** — a rebuild that quietly gains filters is invisible in the gate verdict, which said ACCEPT both times.

## Round 10 — the material round: two grounds, two lights

`loop-runs/r07/`, measured against `loop-runs/r06/`. Edit class: material. Two `<linearGradient>` blocks changed and nothing else — no path, filter or geometry constant moved, and the structure counts confirm it (2443 paths / 12 gradients / 11 filters before and after; only bytes move, 310,230 → 312,895).

**Where the error actually is.** A region breakdown of `|g_cand − g_ref|` (`loop-runs/r07/work/r1.py`) partitions the 0.1504 `lum_delta`: the **trued ground alone contributes 0.0866, 58% of the whole**, at 38.4% of the pixels — ours 0.834 against C2's 0.612. The un-planed ground contributes 0.0375. Block-vs-block disagreement, the thing three rounds have been editing, is 0.0084. The ground was the round.

**And why the obvious edit is forbidden.** Round 9's percentile trace still holds and is now sharper: at **32px p90 is 98% trued ground** (u 305–1007) and p10 **100% block**; at **16px p90 is 100% trued ground** and p10 100% block. Simply darkening the trued plane pays for its own convergence out of p90 against a floor of 0.607 (32px) / 0.593 (16px) — the r05 death. Scanned, a uniform trued drop caps out at about −0.03 before the floor bites.

**Correcting a round-9 conclusion.** Round 9 recorded C2's ground polarity as *inverted* — its trued side 0.610 against its rough 0.642 — and ruled convergence out on that basis. That reading is a geometry confound: the un-planed plane occupies the near-key region of the tile and the trued plane the far region, so a plain region mean compares two different distances from the light. Re-measured **along the shared light axis u = (x+y)/√2 with the block dilated out** (`work/r3.py`), C2's trued plane is **brighter** than its rough at matched u — +0.076 at u = 720. C2's polarity is not inverted. Only its magnitude differs: ours was **+0.300** at the same u. The finish is a small step on one ramp, not a second light.

**The physical fault, from two independent measurements.** (a) Corner patches against each icon's own ground mean (`work/r4.py`): C2 runs TL **1.36×** / TR 0.81× / BL 0.88× / BR 0.89×; ours ran TL **1.02×** / TR 0.81× / BL **1.09×** / BR 1.01×. Our brightest ground was still the corner *furthest* from our own key — the same single-light violation round 7 fixed once and the finish gain quietly re-introduced. (b) Along u, C2's rough plane decays fast and reaches an **ambient floor of 0.568 by u ≈ 560**; ours decayed near-linearly and did not reach 0.584 until u ≈ 820, sitting **+0.07 to +0.12 too bright across u = 340–660 and 0.07 too dark at the key corner**. The key's falloff was too slow and its near field too weak.

**What changed.** Both ground fields are now knots on **one shared axis, the key's own diagonal (0,0) → (1024,1024)**, so a stop's offset t maps to u as u = 1448·t for both and the two planes are directly comparable in the source. `roughField` is C2's own measured falloff at gain 1.0; `truedField` is C2's own measured trued profile times a single finish gain **g = 1.34**. Each stop colour is the *old* field colour at that u scaled by target_L / current_L, taken off a low-order fit of the current profile so the correction cannot divide out real features — the block's cast shadow dips the trued profile at u = 820 and had to survive. Multiplicative, so every hue is untouched and round 1's warm-shadow finding is protected by construction.

**The gain rule was fixed before scoring, not after.** g is the smallest finish gain holding simulated 32px `self_contrast` at least 1.5% above the gate's floor: g = 1.32 gives 0.605 against a 0.607 floor and fails; **g = 1.34 gives 0.616**. It independently lands `measure.py` polarity at +0.174, the separation the icon has carried since round 4 (+0.174 / +0.177 / +0.143) — two unrelated constraints picking the same number. Budgeted in `work/sim3.py` (the round-9 harness with the curl, the block and a ±34px hone band masked out, calibration within 0.007 at every size), which predicted +0.1356 net; the built file scores **+0.1334**. The render lands within **±0.015 of the designed profile at every station** (`work/check.py`), so no implementation iteration was needed.

| size | before (r06) | after (r07) | delta |
|---:|---:|---:|---:|
| 1024 | 0.4228 | **0.4509** | +0.0281 |
| 256 | 0.4351 | **0.4671** | +0.0320 |
| 128 | 0.4680 | **0.5031** | +0.0351 |
| 32 | 0.7622 | **0.7837** | +0.0215 |
| 16 | 0.8129 | **0.8296** | +0.0167 |

Pareto gate **ACCEPT at +0.1334 net**. `lum_delta` falls at all five (1024: 0.1504 → 0.1317; 16px 0.1368 → 0.1197), SSIM rises at all five (1024: 0.6604 → 0.6677; 32px 0.5658 → 0.5919). `self_contrast` falls, as designed and as budgeted — 32px 0.645 → 0.623 against the 0.607 floor, 16px 0.631 → 0.608 against 0.593 — spent deliberately and with 2.6% and 2.5% of headroom left. Structure PASS at 2443 paths / 312,895 bytes / 12 gradients / 11 filters.

**What it cost, and one target still missed.** Polarity **improves**, +0.143 → **+0.175** (un-planed 0.681, trued 0.856) — the fixture invariant is not merely held but strengthened, because the rough plane fell further than the trued one. `edge_f1` gives a little back at the three large sizes (1024: 0.0768 → 0.0727, 128: 0.4499 → 0.4434) and gains at 32 (0.8970 → 0.9076), staying pinned at 1.000 at 16px. Figure-ground against the un-planed field drops **3.73:1 → 3.04:1** as that plane darkens toward the block (C2's own is 2.77:1, so the master keeps more separation than the reference); against the trued field it is essentially unchanged, 5.07:1 → 5.00:1. The 16px read is intact — spread 0.869 → 0.844, the two-state split unchanged, and the vermilion footprint **bit-identical** at 4.69% of tile, the hone not being in scope. **The single-light ordering predicate is closer but still violated, and that is this round's honest miss**: TL went 1.02× → 1.09× of own ground mean (C2 1.36×) but BL only 1.09× → 1.11× (C2 0.88×), so the brightest corner is still bottom-left — by 0.010 now rather than 0.057. The residual violation *is* the finish gain: BL is trued ground, and g = 1.34 is what holds it above TL. Closing the last 0.010 means lowering g, which the contrast floor will not currently fund. Gate and rubric agree on direction; the rubric's one open deduction is unchanged.

**Reusable recipe — "one key, one axis; put both fields on it".** When an icon's ground is split into two or more materials, do not give each its own gradient vector. Parameterise every ground field on **the key light's own axis** — for a top-left key, u = (x+y)/√2 with the axis (0,0) → (W,W), so a stop's offset maps to the same physical u in every field — and let material differ only as a **gain on that shared ramp**. Two things follow for free. Measurement becomes comparable: you can ask "is the reference's finished plane brighter than its rough plane *at the same distance from the light*", which is the only form of that question with an answer — asked as a plain region mean it returns the geometry, not the material, and round 9 was misled by exactly that. And single-light violations become *arithmetic* rather than visual: with one axis, the ordering predicate is just "does L decrease in u within each field, and is the gain finite", checkable before rendering. Cost is one profile sampler plus a stop generator (`work/r3.py`, `work/stops.py`, ~120 lines together); the payoff is that the light stops being a thing you re-fit per plane every round.

## Round 11 — the material round: the key is a point, not a plane wave

`loop-runs/r08/`, measured against `loop-runs/r07/`. Edit class: material. **One gradient element changed and nothing else** — `roughField` goes from `<linearGradient>` to `<radialGradient>`; no path, filter or geometry constant moved, and the structure counts confirm it (2443 paths / 12 gradients / 11 filters before and after; bytes 312,895 → 313,698).

**Where the error is, and why the biggest piece of it is unfundable.** The region breakdown (`work/m1.py`) puts the 0.1317 `lum_delta` at: trued ground **0.0780, 59% of the whole** (ours 0.812 against C2's 0.612); rough ground 0.0280; block-vs-block 0.0084. Profiled along the shared axis (`work/m2.py`), the rough plane now matches C2 within 0.05 at every station — ratios 0.943–1.018, round 10 landed it — while the trued plane is a **flat 1.31–1.39× C2 everywhere**. The entire remaining trued error is therefore the one finish scalar g = 1.34, and reducing it is the single largest gain available. It is also the one gain the round cannot take. Round 9's percentile trace re-run on today's render (`work/m4.py`): 32px p90 is **98.2% trued ground** and p10 **100% block**; 16px is **100% / 100%**. Every point of trued convergence comes straight out of p90 against the floor. Simulated (`work/sim.py`): g = 1.28 → +0.0807 net but 16px `self_contrast` 0.570 against a 0.572 floor, **FLOOR fail**; 1.22 → +0.1570 FLOOR; 1.16 → +0.2269 FLOOR; 1.10 → +0.2934 FLOOR; 1.04 → +0.3481 FLOOR with polarity collapsed to +0.001; 1.00 → +0.3534 FLOOR, polarity **−0.023, invariant inverted**. Even radial + g = 1.28 misses sc16 by 0.001. So the composite's largest move, roughly +0.35, is one the gate's contrast floor and the fixture's polarity invariant both refuse, and the brief's instruction not to spend the round converging ground contrast is exactly right: the money in that direction is counterfeit.

**The fundable finding: round 10 measured the right profile and hung it on the wrong coordinate.** Fit test on C2's own un-planed pixels — bin by a candidate coordinate, take the within-bin residual sd, and the coordinate that explains the field best wins (`work/m8.py`, n = 112,206 with the block dilated out):

| coordinate for C2's un-planed field | residual sd |
|---|---:|
| radial, distance from **(75, 25)** | **0.02934** |
| best straight ramp, 50° | 0.03616 |
| our authored ramp, 45° | 0.03661 |

**19% less unexplained variance.** C2's key is a *point source* just off the top-left corner, not a plane wave — which is also the physically right answer for an icon whose ground is a lit surface rather than a sky. Attribution control on the render separates the two halves of the claim cleanly: adopting C2's measured profile while keeping the 45° axis scores **−0.0091**; keeping our own profile and only re-indexing it by r scores **+0.0299**; both together +0.0308. **The gain is the coordinate, not the numbers on it.**

**A contaminant that would have been copied in as light.** C2's translucent curl reads L 0.85–0.95 against ground of 0.70–0.80 and sits at radii 150–500 from the fitted source — squarely inside the bins this round is authored from. Left in, it biased the near field up and moved the fitted centre. With it boxed out on a measured bbox taken off a 1.2× crop (`work/ref-curl-crop.png`, not assumed), the centre moved **(75, −25) → (75, 25)**, the near-source peak fell **0.9409 → 0.9166**, and the simulated gain fell +0.0353 → +0.0308. The uncorrected version was a third brighter *and* a third wronger.

**What changed.** `roughField` becomes `<radialGradient cx="75" cy="25" r="1000" fx="75" fy="25">` carrying C2's own f(r), fifteen knots from r = 0 to 1000. Stop colours follow round 10's rule unchanged — each is the *old* field colour at the same distance scaled by target_L / current_L — so the correction stays multiplicative and every hue survives untouched. The per-knot ratios run **0.965 to 1.059**: the colours barely move, only where they sit. **The trued plane deliberately stays linear**, and that is measurement-led rather than conservative: on C2 its geometry is not strongly determined (best radial 0.02389 against best linear 0.02507, `work/m7.py`), and re-indexing it radially scored **−0.0219 alone** and dragged the combination down to +0.0089. One claim, one plane.

| size | before (r07) | after (r08) | delta |
|---:|---:|---:|---:|
| 1024 | 0.4509 | **0.4539** | +0.0030 |
| 256 | 0.4671 | **0.4728** | +0.0057 |
| 128 | 0.5031 | **0.5124** | +0.0093 |
| 32 | 0.7837 | **0.7877** | +0.0040 |
| 16 | 0.8296 | **0.8343** | +0.0047 |

Pareto gate **ACCEPT at +0.0267 net** (simulated +0.0308; the harness has now called three consecutive rounds within 0.005). `lum_delta` falls at all five (1024: 0.1317 → 0.1298; 16px 0.1197 → 0.1171) and SSIM rises at four of five, largest at the small sizes where the read lives (16px 0.6214 → 0.6338, 32px 0.5919 → 0.6051). The render lands **within ±0.023 of C2's own f(r) at every station** from r = 100 to 900 (`work/check.py`), so again no implementation iteration.

**What it cost.** Almost nothing, and this is the unusual part: `self_contrast` **rises** at every size (32px 0.6230 → 0.6238, 16px 0.6084 → 0.6084, 1024 0.6255 → 0.6260), so the edit **pays into** the contrast budget instead of spending it — the brightened near field lifts p90 while the p10 block is untouched. Polarity holds at **+0.165** (un-planed 0.691, trued 0.856), comfortably the right way round. `edge_f1` gives back 0.0020 at 32px (0.9076 → 0.9056), against a 0.35 floor, and gains at the three large sizes; 16px stays pinned at 1.000 with `mask_iou` 0.9844. Figure-ground improves slightly against the un-planed field (2.68:1 → 2.72:1) and is unchanged against the trued (3.43:1). **Round 10's open miss is now closed, narrowly:** the brightest ground corner is finally the one nearest the key — TL 1.075× → **1.130×** of own ground mean against BL 1.126× → 1.121× — so the single-light ordering predicate holds for the first time since the finish gain went in. It holds by 0.009 where C2 holds it by 0.479 (TL 1.358× / BL 0.879×), and closing that gap still means lowering g, which the floor still will not fund. Gate and rubric agree; no rubric item regressed and the one open deduction changes from *violated* to *satisfied but faint*.

**Reusable recipe — "fit the coordinate before you fit the curve".** Before authoring any gradient off a reference, do not assume the light's geometry from the shading's *direction*; measure which coordinate the reference's own pixels are a function of. Mask the region, then for each candidate coordinate s(x, y) — linear ramps at a sweep of angles, radial distance from a grid of centres — bin the pixels by s and take the pooled within-bin residual sd. The coordinate with the lowest sd is the one the field actually depends on; the profile is then just the per-bin means along it. Two properties make this worth the twenty lines. It **separates geometry from profile**, which is the difference between a gradient that merely hits the right average luminances and one that puts them in the right places — here the profile was already right and worth −0.0091 on its own, while the coordinate alone was worth +0.0299. And it is **honest about non-determination**: run it on a region whose geometry is genuinely ambiguous, as the trued plane is here, and the best radial and best linear sds come out within 5% of each other, which is the signal to leave that plane alone rather than to author a second light on noise. Pair it with a masked-out box for any translucent overlay in the reference — a curl, a glass panel, a shadow catcher — because those read as light to the binner and will be baked into the key. Cost is `work/m5.py` + `work/m8.py`, about 80 lines of numpy; the payoff is not re-fitting the light every round.

## Round 12 — the detail round: the un-planed plane was smooth where the reference is torn

`loop-runs/r10/`, measured against `loop-runs/r08/`. Edit class: detail. **One function changed** — `grain()` and its helpers; no silhouette point, no palette entry, no filter and no light constant moved. Structure 2443 paths / 313,698 bytes → **1726 paths / 321,547 bytes**, PASS against the 3000 / 350,000 envelope: the new texture is denser to look at and *cheaper* in paths than the old one, because a run of a ridge is now one dashed polyline instead of thirty separate dashes.

**What was measured off the reference.** Three instruments agree, and none of them is the residual map. On a clean 128px square of un-planed ground with the block, the curl, the seam and the rim all excluded (`work/m11.py`), C2's band sd at 3–40px is **0.0587 against our 0.0046** — a 12.8× deficit — and its anisotropy is **3.0** where ours read 5.4. Across radius from the fitted key at (75, 25) (`work/m12.py`) C2's 3–12px band sd is roughly flat, **0.013–0.022**, while ours *fell away with the field*, 0.0114 near the key to **0.0039** at r 840–960: 5.5× short in the corner that carries the most of the tile. On the trued plane the fault was the opposite one — amplitude near enough right at 0.0041 against 0.0057, but anisotropy **13.1 against 1.45** and peak λ 17.8px against 35.5px, i.e. ruled lines rather than a worked surface. Zoomed side by side (`work/crop-rough-left.png`) C2 is a two-family cross-hatched torn lattice with cells of 20–30px and a lit/dark flank pair on every ridge; ours was a smooth field with about twenty-five sparse dashes on it.

**Why the coverage was missing, and why it had been unaffordable.** The old `grain()` spent **2144 of the file's 2443 paths** on individual dash rectangles and still covered almost nothing, because each dash is its own element. Moving the break pattern into `stroke-dasharray` on a short polyline decouples coverage from path count entirely: the same 3000-path envelope now buys roughly forty times the mark density. That is the whole reason this round could do what r02's fibre round could not.

**Why the amplitude fell off, and the fix.** A fixed-opacity dark stroke does not carry fixed contrast — it carries `opacity × (ground − stroke)`, and our ground runs from 0.80 near the key to 0.57 in the far corner, so the same stroke loses two thirds of its contrast on the way out. The build now **fits the ground's own luminance and solves each stroke against it**: `GROUND_ROUGH` / `GROUND_TRUED` are quadratic fits in canvas (x, y) over each masked plane (`work/m13.py`, rms residual 0.035 / 0.019), and every piece of every ridge sets `opacity = amp / (ground − dark)` for the shadowed flank and the matching reciprocal for the lit one. Amplitude is then specified in **luminance units, not alpha**, and holds flat by construction. A mild radial gain (0.90 + 0.35·r/1000) follows C2's own profile, which does not merely hold away from the key but gains about 22%.

**The guard that r02 lacked.** Every ridge is a **pair**: a shadowed flank and a lit twin 1.7 units perpendicular, offset toward the one key light re-expressed in the blade's frame (`_key_local()`), with the twin's opacity chosen so the two move exactly as much luminance in opposite directions. Rendered against a grain-free ground the pair moves the un-planed plane by **−0.0002** and the trued plane by **+0.0000** (isolation probe: shadowed flanks alone −0.0077, lit flanks alone +0.0070). That is what lets the texture be dense without costing `lum` at any size, and it is also why it dissolves rather than darkens on the way down to 16px.

**Two iterations, both on the material and neither on the score.** The first build put the two families at exactly 0° and 90° at even pitch with 8–42 unit dashes, and rendered as **brickwork** — measured, its far-patch anisotropy went the wrong way, 5.4 → 9.6. Fixed by giving each ridge its own bearing (±11° family A, ±13° family B), equalising the two families' amplitude and pitch, letting each piece wander off its axis, and shortening the marks to 4–19 units to match C2's flecks: anisotropy **5.7 rough, 6.2 trued**, against 5.4 / 13.1 before and C2's 3.0 / 1.45. The trued plane is the larger correction and it is the one that had been furthest wrong.

| size | before (r08) | after (r10) | delta |
|---:|---:|---:|---:|
| 1024 | 0.4539 | **0.4584** | +0.0045 |
| 256 | 0.4728 | **0.4725** | −0.0003 |
| 128 | 0.5124 | **0.5163** | +0.0039 |
| 32 | 0.7877 | **0.7865** | −0.0012 |
| 16 | 0.8343 | **0.8383** | +0.0040 |

Pareto gate **ACCEPT at +0.0109 net**, worst size −0.0012 against a −0.005 tolerance. The gain is `edge_f1` at 1024, **0.0746 → 0.1574**, which is exactly the missing recall the edge maps had been showing all along (10,863 candidate edges against C2's 35,979), plus `lum_delta` falling at all five sizes (1024 0.1298 → 0.1274, 16px 0.1171 → 0.1125) as the un-planed plane settles from 0.670 to 0.695 against C2's 0.694. Whole-plane band sd at 3–12px is now **0.0154 against C2's 0.0146**, flat in radius instead of collapsing.

**What it cost.** SSIM at 1024, **0.6674 → 0.6185**, and that was expected and accepted before the build: the metric's box-filter SSIM has cov ≈ 0 for texture the reference does not correlate with, so CS ≈ c2/(va + vb + c2) and *any* honest fine detail lowers it. It is bought back at the sizes where the read lives — 16px SSIM 0.6338 → 0.6389, 32px +0.0014. `self_contrast` gives back 0.0108 at 32px (0.6238 → 0.6130) against a floor of 0.5864, and **rises** at 16px. `mask_iou` is bit-identical at all five sizes; the silhouette was not touched. Polarity holds at **+0.156** (un-planed 0.696, trued 0.852). Figure-ground against the un-planed field improves 3.00:1 → 3.05:1 and against the trued gives back 3.83:1 → 3.73:1, both comfortably above threshold. **Gate and rubric agree**; no rubric item regressed, the 16px render is visually indistinguishable from r08's, and the single light model is stronger than before rather than weaker, because the lit flank of every one of ~800 ridges is derived from `LIGHT` rather than authored by hand.

**Reusable recipe — "solve the stroke against the ground, and draw it in pairs".** Two constructions, and they only work together. First: **specify ornament in luminance, not in alpha.** Fit the substrate's own field once (quadratic in x, y over the masked region is enough — rms 0.03 here) and have the generator solve each mark's opacity as `amp / (ground − mark)` at the place it actually lands. Constant alpha over a graded ground is a texture that fades with the shading, which reads as the ornament belonging to the light rather than to the material, and it is the reason our far corner was 5.5× short while the near field was already right. Second: **draw every mark as a mean-neutral pair** — a shadowed flank and a lit twin half a mark's width toward the key, opacities solved so the two shift equal and opposite luminance. Verify it by rendering the ornament layer alone against a bare ground and reading the plane's mean; ours lands within 0.0002. Neutrality is what converts "texture" from a thing that spends the luminance and contrast budgets into a thing that spends neither, so a scorer that punishes any drift in mean or contrast has nothing to punish, and the marks cancel to nothing at small sizes instead of turning into grey haze. The path-cost note is the third leg: put the break pattern in `stroke-dasharray` on a wandering polyline rather than emitting each dash, and dense detail costs a fortieth of the elements. Cost is about 90 lines in the generator plus `work/m13.py`; it is the difference between r02's fibre round, which was gate-rejected for exactly this, and this one.

## Round 13 — the detail round: the tear was in the wrong place, at the wrong pitch, in the wrong direction

`loop-runs/r11/`, measured against `loop-runs/r10/`. Edit class: detail. Nothing outside the un-planed plane's own texture moved — no silhouette point, no palette entry, no filter, no light constant, and the trued plane is untouched. Structure **1726 paths → 1726 paths**, 321,547 → **324,705 bytes**, PASS; gradients 12 → 16, filters unchanged at 11.

**What was measured off the reference.** r10 authored the tear population *flat* across the plane, on purpose, because r10's instrument said C2's was flat in radius from the key. That instrument was reading the wrong coordinate. Re-fitting by r08's recipe against the quantity the build actually authors — the local rms of the 3–13px high-pass, i.e. relief amplitude, binned by each candidate coordinate and scored on within-bin residual (`work/m8.py`) — **canvas y explains 65.3%** of C2's un-planed relief, distance from the fitted key only **22.0%**, and distance out from the cut, the coordinate r08 read this same fall-off on, only **35.2%**. Down canvas y the master was already right for the top two thirds and then fell off a cliff. Reference/master rms per 64px band: 0.86, 0.97, 0.88, 0.93, 1.16 from y 64 to 384, then **2.18, 2.97, 3.40, 3.15, 3.08, 2.87, 2.44** from y 384 to 832. In thresholded terms — the terms the scorer counts — **78% of C2's un-planed edge pixels are in that band, against 18% of ours.** The physical reading: the top of the tile sits inside the key's own hot spot at L 0.82, where micro relief has no contrast left to show; the lower band is where the light rakes, and that is the only part of a rough plane where tearing is legible at all.

Two more faults live in the same band, from the same patch spectra (`work/m7.py`). C2's lattice there runs **1.3–1.8× finer** than ours — power-weighted mean λ 6.0px against 10.9 at the left edge, 28.2 against 35.9 mid-plane — and it is dominated by the **along-travel** family, peaks at +48°, +58° and +72° canvas against family A's +57°, where ours was dominated by the **across-pass** family at −42° and read anisotropy **13.7 against C2's 4.68**. A pass tears fibres along the direction it travelled; ours was tearing across it.

**The construction, and why it isn't in the geometry.** The obvious build — evaluate the profile per piece and break every piece at every profile knot — was built first and measured: **+622 paths and +79KB**, which alone puts the file 50KB over its envelope, for a profile still resolved in 159px steps. It was thrown away for a **stroke gradient**. The grain rides one rotation, so canvas y is affine in the grain's own coordinates and the point `s·(UY, NY)` sits at canvas y = `AY + s`; a `linearGradient` with `gradientUnits="userSpaceOnUse"` between those two images, carrying the profile as `stop-opacity`, therefore *is* the profile — continuous rather than per-piece, and **+3,158 bytes and zero paths** (`tear_profile()`, four gradients: one per family per flank). Peak amplitude is folded into `GRAIN_AMP_*` and the gradient only ever attenuates. The two families take unequal shares of the same profile — A 1.3625, B 0.7560, chosen so `sqrt((A² + 2B²)/3) = 1` and the *combined* amplitude above the band is unchanged while the power split turns from 1:2 to 1.6:1 in A's favour. The pitch fault goes the same way, for nothing: `GRAIN_BAND_DASH = 0.62` shortens marks and gaps together below y 384, which raises a patch's frequency content without changing its coverage, and the `stroke-dasharray` was already there.

**Why the pair survives it.** A gradient stroke paint multiplies `stroke-opacity`, and both flanks of every pair take the *same* profile, so r10's mean-neutrality is preserved pointwise by construction rather than re-derived. Measured on the render: the un-planed plane's mean moves **+0.0002** against r10 (0.6416 → 0.6417) with the whole texture rebuilt beneath it. The one place neutrality could still break is the opacity cap, where a shadowed flank saturates and its twin does not; `_ridge` now clips the *achieved* amplitude to what `GRAIN_CAP` allows and solves the twin against that, so a clip costs contrast and never costs the plane's mean.

**Did the profile land** (`work/check.py`, achieved rms ratio to r10 per band): 1.01, 1.00, 1.00, 1.01, 1.11 above y 384 — the part that already measured right is bit-for-bit unmoved — then **1.51, 2.28, 2.22, 2.42, 2.21, 2.12, 2.02** through the band, against a design target of 2.4 and C2's 2.2–3.4. Whole-plane relief rms 0.0174 → **0.0219** against C2's 0.0253; thresholded edge density 4.09% → **8.23%** against C2's 7.72%, and now distributed like C2's instead of flat. Mean λ at band-left 10.9 → **7.2px** (C2 6.0), band-mid 35.9 → **28.8px** (C2 28.2). Band-mid anisotropy 13.72 → **10.84** toward C2's 4.68, still B-dominated; band-left, already A-dominated, went the wrong way, 3.68 → 6.92. One global family split cannot fix both patches, and that is the fault left standing.

| size | before (r10) | after (r11) | delta |
|---:|---:|---:|---:|
| 1024 | 0.4584 | **0.4912** | +0.0328 |
| 256 | 0.4725 | **0.4704** | −0.0021 |
| 128 | 0.5163 | **0.5149** | −0.0014 |
| 32 | 0.7865 | **0.7855** | −0.0010 |
| 16 | 0.8383 | **0.8383** | ±0.0000 |

Pareto gate **ACCEPT at +0.0283 net**, worst size −0.0021 against a −0.005 tolerance. The whole gain is `edge_f1` at 1024, **0.1574 → 0.3024** — the single largest move any accepted round has made on that term, and it comes from putting the recall where the reference keeps it rather than from more of it everywhere.

**What it cost, and the surprise.** SSIM at 1024 fell only **0.6185 → 0.6128**. r02's lesson was that the composite punishes surface texture on principle (SSIM 0.651 → 0.548 for a comparable edge gain), and this round expected to pay the same toll. It did not, and the reason is worth keeping: **texture placed on the reference's own amplitude profile raises local covariance as well as local variance**, so the CS term does not collapse the way uncorrelated texture makes it collapse. Profile-matched detail is roughly twenty times cheaper in SSIM than uniform detail of the same energy. `self_contrast` is unmoved to four decimals at every size (32px 0.6130 → 0.6128 against a 0.5864 floor), `lum_delta` moves by +0.0002 to +0.0008, and `mask_iou` is bit-identical: the pair construction pays for all three. Polarity holds at **+0.156** (un-planed 0.697, trued 0.852). At 32 and 16px the texture is invisible and the composites are unchanged, so the read is untouched; the honest rubric note is at 1024, where the un-planed plane's *character* — a coarse cross-hatch of long strokes — is now more visible than it was, and still further from C2's fine along-travel striation than the numbers suggest. That character was already the construction's at r10, so nothing regressed, but the next detail round on this plane should spend itself on mark **length and count** rather than amplitude, which is now solved.

**Reusable construction — "put the profile in the paint, not the geometry".** When ornament needs a spatially varying amplitude and the ornament is emitted as many pieces, do not re-solve per piece: author the variation as a `linearGradient` with `gradientUnits="userSpaceOnUse"` and varying `stop-opacity`, used as the `stroke` paint of the group holding the marks. Three properties make it strictly better than the geometric version here. It is **continuous** where per-piece solving is stepwise. It is **free** — bytes, not paths, so it does not compete with coverage for the envelope, and the geometric equivalent measured 25× the cost. And it is **neutrality-preserving**: paint alpha multiplies `stroke-opacity`, so a lit/shadowed pair sharing one profile stays mean-neutral wherever the profile puts it, which a per-piece solve has to re-establish. It works because the marks ride a rotation, so any canvas-space axis maps to a straight gradient vector in their local frame — `s·(UY, NY)` for canvas y here; check that the frame is affine before relying on it. Two corollaries. Give each family of marks **its own share of one shared profile** rather than its own profile: it re-aims a lattice's anisotropy at zero cost and keeps the combined amplitude a single number you can still reason about. And a dash pattern's **scale** is a free frequency control — shortening marks and gaps together moves a patch's wavelength without moving its coverage, so pitch faults are cheaper to fix than amplitude faults. Cost this round: about 55 lines in the generator, `work/m7.py`, `work/m8.py` and `work/check.py`.

## Round 14 — the detail round: the marks were three times too long and half as many

`loop-runs/r12/`, measured against `loop-runs/r11/`. Edit class: detail. Nothing outside the grain's own mark geometry moved — no silhouette point, no palette entry, no filter, no light constant, no amplitude constant, and every ridge sits on the same position, bearing and amplitude it had at r13 (the discarded second `rnd()` in `_dash` keeps the jitter stream aligned, so the diff is mark length and stroke width and nothing else). Structure **1726 paths → 1726 paths**, 324,705 → **323,357 bytes**, PASS; gradients 16 → 16 after `GRAIN_BAND_DASH` was retired, filters unchanged at 11.

**What was measured off the reference.** Round 13 left the mandate: spend the next round on mark *length and count*, not amplitude. `work/w3.py` measures both directly — 3–13px high-pass, threshold at 1.1σ, connected components by a pure-numpy union-find, PCA per component for length and width — at five stations, three on the un-planed plane, one above the band, one on the trued plane. The result is that **coverage was already right and it was being spent wrong.** Ref coverage 23.5 / 18.8 / 20.4 / 17.0 / 22.8%, ours 22.7 / 19.3 / 17.4 / 20.2 / 20.9% — inside a point or two everywhere. But C2 buys **2.3–2.9× as many marks** with it (199 / 108 / 105 / 83 / 192 per 10k px against 87 / 41 / 36 / 44 / 77), each **1.6–2.7× shorter** (median 3.9 / 3.8 / 4.5 / 4.4 / 3.8 against 6.2 / 6.4 / 12.2 / 7.2 / 6.3), at **aspect 1.7–2.0 against ours 3.6–4.8**, p90 length 8.3–13.1 against 16.4–32.1. The second finding retires a Round 13 construction: C2's mark length is **the same at all five stations, on both planes** — a flat 3.8–4.5 median. r13's `GRAIN_BAND_DASH`, which shortened marks below y 384 only, was a band-local proxy for a fault that is global, so it was deleted rather than retuned.

**Why the marks were long.** The old `_dash` handed `tear` straight to the gap. On the trued plane `tear` runs 0.05–0.20, which asked for gaps of 0.3–2.0 units inside strokes 1.2–2.0 units wide — sub-pixel breaks that the renderer's antialiasing fills in, so a dozen marks fuse into one 170–250 unit streak and the dasharray renders as a ruled line. The plane that measured worst was the one whose tear was faintest, which is the signature of a duty-cycle bug rather than a taste failure.

**The construction.** Draw the mark from the reference's own measured length distribution — `GRAIN_MARK_MIN + rnd()**GRAIN_MARK_SKEW * GRAIN_MARK_RUN`, 1.4 + skew 2.2 over a run of 10.0, so the median lands on the measured 3.6 and the tail still reaches C2's p90 — make it the *same* distribution everywhere because C2's is, then derive **duty** from `tear` (`0.72 − 0.20·tear`) and let the gap fall out of it, floored at `GRAIN_GAP_MIN` stroke widths. The floor is the load-bearing half and it was **swept, not guessed** (`work/sweep.py`): at 1.15 widths the marks fuse *harder* than r13's (median 8.1, count 84/10k), at 1.9 the field breaks (4.5, 124), and past 2.4 the gap eats the period faster than it wins breaks and both fall away again (4.6, 106 at 3.0). A clear interior optimum at **1.9** — a break must survive antialiasing with a pixel to spare. Shortening marks spends coverage, and C2's mark is also wider than ours at every station (median 2.0–2.5 against 1.7–2.0), so the coverage is bought back through one factor over all four stroke widths, `GRAIN_WID = 1.20`, swept the same way against the constraint that un-planed 3–13px relief rms must not move (r13 0.0190, now **0.0188**). Zero extra paths, **−1,348 bytes**.

**What landed.** Count per 10k at the five stations 87 / 41 / 36 / 44 / 77 → **125 / 64 / 63 / 63 / 142** (C2 199 / 108 / 105 / 83 / 192); median length 6.2 / 6.4 / 12.2 / 7.2 / 6.3 → **4.5 / 4.9 / 5.7 / 5.2 / 4.6** (C2 3.9 / 3.8 / 4.5 / 4.4 / 3.8); aspect 3.6–4.8 → **2.4–2.6** (C2 1.7–2.0); coverage held at 16.7–20.2%. Thresholded whole-plane edge density 7.60% → **6.75%** against C2's 6.18% — the overshoot r13 left is now nearly closed. The pair stays mean-neutral to **+0.0004** and the amplitude profile is preserved per band (achieved/r13 ratio 0.93–1.02), which is the point of solving width as a *ratio*: the lit twin is re-solved against whatever the shadowed flank achieved, so widening both flanks together cannot move the plane's mean. Polarity **+0.155** (un-planed 0.697, trued 0.852).

| size | before (r11) | after (r12) | delta |
|---:|---:|---:|---:|
| 1024 | 0.4912 | **0.4927** | +0.0015 |
| 256 | 0.4704 | **0.4713** | +0.0009 |
| 128 | 0.5149 | **0.5153** | +0.0004 |
| 32 | 0.7855 | **0.7858** | +0.0003 |
| 16 | 0.8383 | **0.8381** | −0.0002 |

Pareto gate **ACCEPT at +0.0029 net**, worst size −0.0002 against a −0.005 tolerance. Four of five sizes up, which no previous detail round has managed. The gain is **SSIM, not edges**: 1024 SSIM 0.6128 → **0.6170**, 256 0.5793 → 0.5807, 128 0.5589 → 0.5593, 32 0.6047 → 0.6057, while 1024 `edge_f1` gives back 0.3024 → 0.3009. That trade is the round's real result and it sharpens r13's finding. r13 learned that profile-matched texture is cheap in SSIM; r14 shows that **length-matched texture is SSIM-positive** — a mark the size of the reference's marks raises local covariance inside SSIM's window, where a mark three times too long raises local variance and nothing else. `self_contrast` moves by at most 0.0006 (32px 0.6129, 16px 0.6092, floors 0.576 and 0.573), `lum_delta` by 0.0004, `mask_iou` is bit-identical at four sizes.

**What it cost.** Two faults are untouched and one regressed. Mean λ went the wrong way by ~8% at all three patches (band-left 7.2 → 7.8px, band-mid 28.8 → 30.4, above-band 15.6 → 16.3) — a direct consequence of the width factor, and the price paid for coverage. Band-mid anisotropy regressed 10.84 → **11.43** against C2's 4.68, while band-left improved 6.92 → 5.68 and above-band 5.74 → 5.51; the one global family split still cannot serve both patches, which is the fault r13 named and r14 did not touch. On the rubric: at 32 and 16px the texture is invisible and the read is unchanged, figure-ground and the single light model are untouched by construction, and at 1024 the un-planed plane's character has moved from *ruled cross-hatch* to *broken dashes* — closer to C2's fleck field, still visibly a lattice where C2's is not. The count gap that remains is not affordable: matching C2's 199/10k would need ~37% more ridge lines, about **+87KB**, well past the 350KB envelope, and byte-trimming elsewhere buys 6–15% at the cost of r10's ground-solve accuracy. The remaining distance on this plane is a *lattice-vs-field* problem, not a density one, and the next round on it should attack ridge placement rather than mark geometry. Cost: about 40 lines net in the generator (`_dash` rewritten, four constants added, two retired, `band=` plumbing removed), plus `work/w0.py`–`w3.py`, `w3helpers.py`, `sweep.py`, `sweepw.py`.

**Reusable construction — "specify the mark, derive the gap, floor the break".** When ornament is emitted as `stroke-dasharray` on long strokes, the temptation is to drive the dash pattern from the parameter you already have (here, local intensity). Don't: intensity sets *how much*, and a dash pattern's two numbers are *how much* and *how big*, so one parameter driving both silently couples mark size to amplitude, and the faintest region gets the longest marks. Split them. Draw the **mark length** from the reference's measured distribution, which for a torn or abraded surface is typically flat across the whole surface even when amplitude is not — measure it before assuming otherwise, since a band-local dash scale is an easy and wrong-shaped fix. Derive the **gap** from a duty cycle, since duty is what coverage is made of. Then floor the gap at an absolute multiple of stroke width — **~1.9× is the measured threshold** below which antialiasing fills the break and a run of marks fuses into one streak — and sweep that floor rather than guessing it, because it has an interior optimum: too low and the marks fuse, too high and the gap eats the period and the count falls again. Shortening marks spends coverage; buy it back with **one global stroke-width factor**, which is free in bytes and, if the lit/shadowed twin is solved as a ratio against the achieved amplitude, cannot disturb mean neutrality. Watch that the width factor costs you mean wavelength — it is the one term this construction reliably moves the wrong way.

## Round 15 — the detail round: the grain was a lattice because every ridge ran on one of two bearings

`loop-runs/r13/`, measured against `loop-runs/r12/`. Edit class: detail. Three constants moved and two literals were given names; no silhouette point, palette entry, filter, light constant or amplitude constant was touched, and mark geometry — the whole of r14's work — is untouched. Structure **1726 → 1784 paths**, 323,357 → **304,530 bytes**, PASS; gradients 16, filters 11, both unchanged.

**What was measured off the reference, and why nothing had seen it.** Round 14 left the mandate that the remaining distance is *lattice-vs-field, not density*, and every instrument the loop owns measures the marks — count, length, width, aspect, coverage, relief rms, spectral bearing. All of them are blind to where the marks are *not*. Two new ones were written. `work/w4.py` `dist_to_mark()` is a distance transform on the thresholded high-pass: for every unmarked pixel, how far to the nearest mark, so the statistic reads the **holes** rather than the ink. The second is a per-component PCA bearing histogram over 12 bins with the entropy normalised to 1, which reads whether the marks point in more than two directions. Measured at three stations (un-planed left, above-band, trued), reference against r14: void mean **1.74 / 3.43 / 1.69 against 3.60 / 8.79 / 3.08** and bearing entropy **0.93 / 0.95 / 0.97 against 0.77 / 0.75 / 0.79**, at coverage that was already within 3 points everywhere (24.7 / 17.5 / 24.3 against 20.4 / 17.7 / 20.5). Nearly the right quantity of ink, holes twice as wide, and the marks lying on two bearings instead of all of them. That is an arrangement fault, and arrangement is free.

**The construction.** Both ridge families were being drawn with ±11° and ±13° of per-ridge bearing scatter about their axis. Raising them to **`GRAIN_SKEW_A = 38.0` / `GRAIN_SKEW_B = 40.0`** is not only an arrangement lever: a ridge aimed 38° off its family's axis crosses more of the tile, so the same ridge count buys more ridge *length* and therefore more marks — the only free density this fixture has, r14 having priced more ridges at +87KB. It has an **interior optimum**. At ±24° the entropy gain is half; at ±52/54° every measure goes backwards, because each family has rotated far enough to sit on the other's axis and the two collapse back into one crossing pattern. Three other placement levers were swept and each measured **inert**, so none shipped: node wander from ±5 to ±26 units (void 3.60 → 3.68, inside noise), a new per-piece lateral stagger at 8 and 16 units, and a scale on the three ridge-pitch increments at 0.94 / 0.90 / 0.87. Both discarded levers were removed from the generator rather than left at 1.0 — but note that deleting the stagger's `rnd()` draw re-phases the deterministic LCG and moved the emitted byte count by **8% on its own**, which failed the envelope at 358,632 bytes and forced the pitch sweep to be re-run on the final stream before it could be read.

**What paid for it.** The extra ridge length costs bytes, so grain vertex precision went to **`GRAIN_PREC = 0`** — a quantisation of one local unit, on the nodes of a polyline that already wanders ±1.7 units, worth **−29.4KB**, about 9% of the file. Net of the skew's +58 paths and their extra length, the file came in **18,827 bytes smaller** with 26.6KB of headroom preserved.

**What landed.** Reference / r14 / r15 at the three stations — bearing entropy **0.93 / 0.77 / 0.95**, **0.95 / 0.75 / 0.96**, **0.97 / 0.79 / 0.98**; marks per 10k **199 / 124.5 / 136.5**, **82.8 / 62.5 / 68.2**, **191.5 / 141.8 / 157.2**; coverage **24.7 / 20.4 / 21.9**, **17.5 / 17.7 / 17.4**, **24.3 / 20.5 / 21.6**. Spectral anisotropy, the r14 mandate's own number: band-left **5.68 → 3.12** against C2's 2.24 and above-band **5.51 → 4.61** against 4.12, both now close. Un-planed 3–13px relief rms 0.0192 → 0.0200 against the reference's 0.0238, so amplitude held to 0.0008 while arrangement moved. Polarity **+0.151** (un-planed 0.695, trued 0.846).

| size | before (r12) | after (r13) | delta |
|---:|---:|---:|---:|
| 1024 | 0.4927 | **0.5058** | +0.0131 |
| 256 | 0.4713 | **0.4699** | −0.0014 |
| 128 | 0.5153 | **0.5160** | +0.0007 |
| 32 | 0.7858 | **0.7886** | +0.0028 |
| 16 | 0.8381 | **0.8362** | −0.0019 |

Pareto gate **ACCEPT at +0.0133 net**, worst size −0.0019 against a −0.005 tolerance and the largest single-size gain any round has recorded. The gain is **edges, not SSIM**, exactly inverting r14: 1024 `edge_f1` **0.3009 → 0.3498** (+0.0489), 32px +0.0146, 128px +0.0054, while SSIM is flat at 1024 (+0.0004) and gives back 0.0038 / 0.0041 / 0.0105 / 0.0095 at 256 / 128 / 32 / 16. Read together with r14 the pair is a rule: **mark size is an SSIM term and mark direction is an edge term.** `lum_delta` improves at all five sizes; `mask_iou` is bit-identical at four.

**What it cost.** `self_contrast` falls at every size (1024 −0.0087, 16px 0.6092 → **0.5928**), still clearing both floors — 32px 0.6036 against 0.576, 16px 0.5928 against 0.573 — but with the margin narrowed from ~0.036 to 0.020, which is the budget the next round inherits. The pair's **mean-neutrality contract broke by 0.0077**: the trued plane's mean drifted 0.8415 → 0.8338 and the split +0.1864 → +0.1778, because `GRAIN_BAL_TRUED = 1.45` was fitted at the old ridge length and the lit twin now clips against its ceiling over ~15% more of it. That constant, re-solved against achieved amplitude, is the named fix. Figure-ground on identical masks is unchanged (2.71 → 2.69:1 un-planed, 3.48 → 3.42:1 trued), and at 32 and 16px the texture remains invisible with the hone line intact.

**Where the number and the eye disagree.** The void statistic that motivated the round is the one that barely moved: 3.60 / 8.79 / 3.08 → **3.67 / 8.61 / 2.95**, and void p90 is flat at all three stations. The holes are the same size; what changed is that they are no longer *periodic*, which is what anisotropy nearly halving and entropy reaching the reference's value are both saying. So the round fixed the lattice's regularity and not its porosity, and r14's mandate was half right — the lattice half was free, the field half is still density, and density is still +87KB. Underneath that sits the fault this round could not reach: C2's bearing entropy comes from near-isotropic flecks (aspect 1.7–2.0), not from scratches pointing everywhere, and our marks still lie on long straight tracks — component max length **163 against the reference's 31**, essentially unmoved. Decoupling marks from lines means node spacing near 12 units, about **+133KB**, which the envelope cannot buy. Cost: three constants, two renames, two levers added and removed, about 15 lines net in the generator, plus `work/v0.py`, `w4.py`, `cost.py`, `s1.py`–`s3.py`, `rub.py`.

**Reusable construction — "scatter the carrier's bearing, and check the holes, not the ink".** When a texture is emitted as marks riding on a small number of long carriers — dashes on strokes, stamps along paths, glyphs on guides — the marks inherit the carriers' bearing distribution, and a two-family carrier layout produces a visible lattice no matter how well the individual mark is matched to the reference. Every mark-wise statistic will report success while the field still reads as cross-hatch, because mark-wise statistics cannot see the negative space. Measure it directly: a **distance transform to the nearest mark** reads hole size, and a **per-component PCA bearing histogram, entropy-normalised** reads whether the field has more than two directions. Then fix it at the carrier, by scattering each carrier's bearing about its family axis. This has an interior optimum — typically **±35–40°** for two families about 60–90° apart — and it fails past that point for a specific reason worth predicting rather than discovering: each family rotates onto the other's axis and the two merge back into one pattern. The scatter is also a free density lever, since an off-axis carrier crosses more of the region and carries more marks for the same carrier count. Fund it by cutting **coordinate precision on the carrier's own vertices**, which is nearly free when the carrier already jitters by more than the quantisation — but if the generator is a seeded LCG, adding or removing a single draw re-phases the entire layout and can move the output size by ~8%, so re-measure the byte envelope on the final stream rather than carrying a number over from a sweep.

## Round 16 — the detail round: the cut was a colour change where the reference draws a step

`loop-runs/r14/`, measured against `loop-runs/r13/`. Edit class: detail. One band path, two gradients and a mask were added and nine constants named; no silhouette point, palette entry, filter, light constant or grain constant was touched, and the band's whole reach is inside `measure.py`'s ±60 skip so it cannot touch the split. Structure **1784 → 1785 paths**, 304,530 → **307,135 bytes**, PASS; gradients 16 → 18, filters 11 unchanged.

**What was measured off the reference.** The residual's two loudest zones are both out of this class — the squircle corners, where C2 is a full-bleed raster (a delivery-format artifact, not artwork), and a broad bottom-right zone where our trued plane runs **0.22 brighter** than C2's, which is palette. Two in-scope hypotheses were measured and killed before they cost anything: un-planed texture amplitude is already matched (3–13px high-pass 0.0278 against C2's 0.0295, coverage 16.1% against 17.1%), and the trued plane's apparent 2.02× texture excess turned out to be **entirely** block-adjacent and cut-adjacent cells — its far field measures 0.5–1.6× per 128px cell. That left one unclaimed gap: **the cut itself has no micro-geometry.** Each image's own cut was fitted rather than assumed (`work/w4.py` searches inclination and offset for the line of greatest mean-luminance step: ours 34.0°, C2's 41.0°), and luminance was binned by true perpendicular distance. C2's cross-section is the same four-part signature at all three stations along the block-free strip — un-planed plateau, **crest** at d −1..−3, shadowed **riser** trough at d +2..+6, lit **arris** peak at d +7..+9, then the trued plateau — averaging **+7.8% / −18.6% / +17.7%** of their own plateaus and *growing left to right* (riser −15.6 / −18.1 / −22.2%, arris +11.9 / +17.2 / +24.1%). Ours was a monotonic ramp with a +2.5% overshoot where the arris belongs and no trough anywhere. That is one shaving's thickness of material, drawn honestly. It survives coarsening — on LANCZOS downsamples of C2 the lip still reads +10.8% of its plateau at 256 and +5.4% at 128 — so it is ornament that works at icon sizes, not pixel-peeping. Two corroborations: the riser **raises** saturation (C2 seam 0.167 against its trued plateau 0.124), which is the prior learning that shadow is saturated, not merely dark; and binning the *signed residual* by our own cut distance says d ∈ [+1,+6] is −0.104 and d ∈ [+6,+11] is −0.246, i.e. the reference wants us darker on both — of which more below.

**The construction.** Sixteen local units of band across the whole cut, `M LX_MIN,+6 → LX_MAX,−12`, filled by **one** `linearGradient` in the local frame whose stops carry the entire cross-section: GRAIN_LITE up to the cut, GRAIN_DARK through the riser, GRAIN_LITE again on the arris. Four features, one shape, so they cannot drift out of register with each other, and the colour switch at the cut costs a duplicated stop offset rather than a second element. Amplitude swells along the cut through a second gradient used as a **mask** along local x. The swell's anchoring was the round's one real mistake and its correction: anchored first at the key's geometric closest approach (canvas x 478), which the block occludes, it normalised full amplitude at a point nothing can check and left the entire *visible* strip at 0.60–0.70 of it — double-counting the ramp, since the base amplitudes were already station **means**. Re-anchored on the **last measured station** (x 300, `STEP_SWELL_X`) with `STEP_SWELL_EDGE = 0.62` at the tile ends, the fit reproduces C2's own stations. Drawn over the grain, because an arris is geometry and the tear does not cross it, and under the block's shadows, which fall across the step as they should. No new palette entry: the pair is GRAIN_LITE over GRAIN_DARK, the two colours the tear is already made of.

**What landed.** Achieved against C2 at the three block-free stations (`work/w10.py`, in the generator's exact local frame): crest **+3.3 / +3.1 / +5.0%** where it was −0.3 / −0.9 / +0.9, riser **−12.0 / −12.9 / −14.4%** where it was +0.5 / +1.2 / +0.9 against C2's measured −15.6% at the same canvas x, arris **+3.7 / +4.3 / +4.0%** where it was +0.7 / +1.2 / +0.9. Riser saturation rises as C2's does: 0.30 against the plateau's 0.126. Polarity **+0.151** (un-planed 0.695, trued 0.846), bit-for-bit r13's, by construction.

| size | before (r13) | after (r14) | delta |
|---:|---:|---:|---:|
| 1024 | 0.5058 | **0.5060** | +0.0002 |
| 256 | 0.4699 | **0.4716** | +0.0017 |
| 128 | 0.5160 | **0.5167** | +0.0007 |
| 32 | 0.7886 | **0.7886** | +0.0000 |
| 16 | 0.8362 | **0.8362** | +0.0000 |

Pareto gate **ACCEPT at +0.0026 net**, no size negative. It is an **edge gain at the middle sizes** — 256 `edge_f1` **0.2469 → 0.2513**, the round's largest single move, 128 +0.0006, 1024 +0.0006 — which is the r13/r14 rule holding: this is a change of *direction*, a new line parallel to the cut, so it pays in edges. `lum_delta` improves at four sizes; `mask_iou` is bit-identical at all five; 1024 SSIM gives back 0.0006, the usual toll on honest fine detail.

**What it cost, and the ceiling that could not be paid.** Almost nothing measurable: figure-ground is **bit-identical** (block median 0.240, 2.74:1 un-planed, 3.45:1 trued), the vermilion footprint is bit-identical at 11.90%, 16px spread is unchanged at 0.819 and the 32/16 composites do not move to four decimals — max |ΔL| at 16px is 0.011. **Audit holds at 11/12**, the #4 deduction unchanged. But the arris ships at **+7.1% at full swell against C2's +24.1%**, and the shortfall is recorded rather than tuned away because it is not a tuning problem: C2's trued plane sits at L 0.63, ours measures **0.882** beside the cut, and the brightest paint this icon owns is GRAIN_LITE at 0.960. That is **8.8% of headroom against a 24.1% target** — even a fully opaque GRAIN_LITE cannot reach it. `STEP_ARRIS = 0.800` is the most that still leaves the band translucent. **The step cannot be lit past the source; closing this needs the trued plane's value, which is a palette round.** Cost: about 60 lines in the generator (nine constants, two emitters, one defs block, one band), +2,605 bytes, plus `work/w0.py`–`w11.py`.

**Where the number and the physics disagree.** The signed residual says the reference wants us darker on *both* sides of the cut: d ∈ [+1,+6] at −0.104 and d ∈ [+6,+11] at −0.246. So the composite pays for the riser and charges for the arris — and it charges for it only because our whole trued plane is 0.22 too bright, a fault in a different edit class that this round is not allowed to touch. The temptation the numbers offer is to ship the riser alone: a darker seam, no lip, a guaranteed cleaner residual. **That loses.** A shadowed riser with no lit arris beneath it is a groove, not a step — the material is removed and nothing catches the key — and it breaks the single light model the icon has spent four rounds establishing. The pair ships whole and the metric's objection is recorded. This is the second time the loop has found the composite arguing for a physical error; both times the underlying cause was a value fault elsewhere being charged to the detail that sits on top of it, which is worth naming as a pattern: **when a residual disagrees with a light model, check whether the surface under the detail is at the right value before believing the residual.**

**Reusable construction — "carry a cross-section in one gradient, and anchor its swell where you can still measure it".** A material transition — a cut, a chamfer, a weld, a fold — is rarely a colour change and rarely a single line. Fit each image's own transition rather than assuming a shared one (search inclination and offset for the line of greatest mean-luminance step; per-column argmax of the gradient will fit noise and did, at 11° against the true 34°), then bin luminance by **true perpendicular distance** and read the profile off it. Expect the four-part signature — the old surface's arris catches the key, the riser faces away and sits in shadow *and gains saturation*, the fresh arris flares, then the new plateau — and check it survives downsampling before spending a round on it. Author the whole profile as **stops on one `linearGradient` over one band path**, in the local frame via `gradientUnits="userSpaceOnUse"`: features authored as separate shapes must be kept in register by hand, whereas stops on a shared band are continuous by construction, and a colour switch mid-profile costs one duplicated offset instead of a second element. If the amplitude varies along the transition, carry that as a second gradient used as a **mask**, and anchor its normalisation at the **last station you could actually measure**, never at an extrapolated geometric peak — if the peak is occluded, every visible part of the feature is scaled to a value nothing can verify, and if the base amplitudes were station means you have also silently applied the ramp twice. Finally, before matching a highlight's *relative* amplitude, compute the headroom: `(brightest available paint − local field) / local field`. If the reference's plane is darker than yours, its percentages are simply unavailable, and the honest move is to run to the ceiling and record the shortfall rather than invent a brighter source.

## Round 17 — the small-size repair round: the shaving was drawn as a solid, and solids have no bore

`loop-runs/r16/`, measured against `loop-runs/r14/`. (`loop-runs/r15/` was a detail round on the block face; it REJECTed at −0.0227 and was reverted, so it has no entry here and the notes numbering no longer runs +2 ahead of the run directories.) Edit class: small-size repair. Three constants named and one line added to each of the two band-shading branches in `shaving()`. No geometry, no silhouette point, no path, no filter, no light constant, no grain constant, and no new palette entry — `TRANSMIT` already existed and was already the right colour; only the amount arriving through the material changed. Structure **1785 paths and 307,135 bytes, both unchanged to the byte**, gradients 18, filters 11, PASS. A colour-only round costs nothing in the envelope, because every band was already a hex fill of fixed width.

**What was measured off the reference.** Two in-class hypotheses were measured and killed first. (1) *The grain aliases at small size* — false: on clean, block-free, curl-free patches at 1024, with the same pixels sampled in both images (`work/w6.py`), our high-pass energy runs **0.90 / 0.66 / 0.67 / 0.72 / 0.85** of C2's at bands 3/9/17/33/65px and 0.63 after a 32× average. Our tear is quieter than the reference's at every scale that survives downsampling. An earlier hand-picked region had said 8.43× and it was wrong because the box clipped the block's corner; re-measuring on genuinely clean pixels killed the hypothesis before it cost a round. (2) *The 32px edge deficit is a small-size problem* — false, and out of class: all **37** false-positive edges at 32px are the block's and the curl's own silhouettes sitting 1–2 cells (32–64 canvas px) left and below C2's (`work/w7.py`; our block edge at cols 4–6 rows 15–22 where C2's is at cols 7–8, C2's own |grad| there 0.01–0.03). That is coarse structure. At 16px `edge_f1` was already **1.0000** with zero FPs and zero FNs, so there was no edge left to win.

That left one defect, and it was gross. The curl's footprint was recovered exactly by building the tile twice, with and without it (`SHAVING=0`), and differencing (`work/w9.py`); the interior bands were then recovered exactly by rebuilding with the inner-face branch forced to a flag colour (`work/w12.py`), so no box was guessed. **Our ribbon sat a mean 0.388 L below the ground it covers; C2's sits 0.05–0.11 below its own.** In shares: 31.3% of our ribbon was more than 0.15 below its ground and 5.9% more than 0.35, against C2's 2.9% and **0.0%**. Isolated, the inner face (4,142px, 6.0% of the ribbon) read L min 0.310 / p10 0.319 / median 0.338 against a local ground of 0.731, while every equivalent passage on C2 measures far higher — bore seen through the loop p10 **0.672** median 0.737, far wall's inner face p10 0.625 median 0.673, the most turned-away lower-left ribbon min 0.516 p10 0.668, all against ground beside the loop at 0.727/0.776. **The inside of C2's roll is barely darker than open board.** Ours was nearly black, and that is why it collapsed to a dark smudge at 16px where C2's collapses to a light haze.

The cause was legible in the generator, not the residual: `ao = 1.0 − 0.74·depth` treated the roll as an opaque self-shadowing tube, and the one transmitted term, `_lerp(col, TRANSMIT, max(0, lam)·0.12)`, evaluated to **exactly zero** for the deep interior bands — the ones whose outer face is turned from the key are precisely the ones with `lam ≤ 0`. The term that should have floored the bore switched off exactly where the bore is deepest.

**The construction.** Three named constants, each a distinct physical term, all of them light arriving *through* the material and therefore none of them falling off with depth into the roll: `CURL_TRANSMIT = 0.42` (one thickness, key on the far side of the face — applied on the inner face scaled by `max(0, lam)` and on the outer face by `max(0, −lam)`, since those are the two ways the key ends up behind what you are looking at); `CURL_BORE = 0.54` (the bore is walled by lit shaving on every side, so it has a floor independent of any band's own normal); `CURL_SHEEN = 0.22` (ambient pushed through the sheet with the key aside). `ao` was left at 0.74 deliberately — swept at 0.30 and 0.45 it moved the interior by under 0.002, because once transmission exists the occlusion term is swamped, and the minimum change is not to touch it. All three were **fitted to C2's measured interior**, not chosen: a two-stage sweep (`work/w13.py`, `work/w14.py`) rebuilt the tile at each setting and read the ribbon back through the exact recovered masks.

**What landed.** Ribbon L p10 **0.414 → 0.592** against C2's 0.590; p90 0.780 → **0.816** against its 0.821. Below-ground shares 31.3% → **0.8%** and 5.9% → **0.0%** (C2: 2.9% and 0.0%). The inner face now reads min 0.628 / p10 0.648 / median 0.698, landing between C2's bore (0.672/0.737) and its far wall (0.625/0.673) — inside the band, not on one edge of it. The darks kept their hue: darkest 5% saturation **0.191** against C2's 0.203, which is the prior learning that a translucent ribbon must stay saturated where it turns away rather than desaturating toward grey. And the roll still models — inner-face p10→p90 spread 0.143, whole-ribbon 0.224 — so it did not flatten into a decal.

**The #4 deduction, engaged for the first time.** Every round since the curl was added has recorded "the #4 deduction is the curl's small-size noise, which is fine material and not this class" and moved on. This class *is* that class, and the deduction's own quantity — how hard the curl's patch contrasts against the ground it covers, at the sizes where it was called noise — **halves**: at 16px mean |ΔL| **0.0806 → 0.0433** with the worst pixel **0.2252 → 0.0891**, at 32px mean **0.1046 → 0.0542** and worst **0.2689 → 0.1342**, the patch itself holding at 22–23px and 64–66px. The signed mean goes −0.0650 → **−0.0191** at 16px, which is the substantive change: the curl was reading as a *dark* blot above-left of the blade and now reads as the soft pale note the round-4 note said it was supposed to be, which is also what C2's own loop does at that size. Whether that clears the point is for the audit to say and it is not asserted here; the number it was deducted for has halved.

| size | before (r14) | after (r16) | delta |
|---:|---:|---:|---:|
| 1024 | 0.5060 | **0.5123** | +0.0063 |
| 256 | 0.4716 | **0.4757** | +0.0041 |
| 128 | 0.5167 | **0.5274** | +0.0107 |
| 32 | 0.7886 | **0.8034** | +0.0148 |
| 16 | 0.8362 | **0.8392** | +0.0030 |

Pareto gate **ACCEPT at +0.0389 net**, no size negative — fifteen times r14's margin, from three constants. Unlike r14 this is not an edge round but a **value** round: `ssim` rises at all five sizes (128 +0.0156, 16 +0.0142, 32 +0.0174) and `lum_delta` falls at all five. 32px `edge_f1` **0.9073 → 0.9329** as a side effect, because the curl's spurious dark lobe was generating false edges of its own on top of the silhouette offset described above.

**What it cost.** Two edge losses, both real and both paid for. 256 `edge_f1` **0.2513 → 0.2364** and 16px **1.0000 → 0.9940** (one pixel): lifting the dark end compressed the ribbon's p10→p90 range from 0.366 to 0.224, and some of the internal band boundaries the detector was finding went with it. The defence is measured, not asserted — **C2's own ribbon range is 0.231**, so the compression runs *toward* the reference, and the composites rose at both sizes anyway because SSIM and luminance gained more than edges lost. Polarity **+0.150** (un-planed 0.696, trued 0.846), against r14's +0.151; the 0.001 is the curl's own lift reaching the un-planed sample band, and the invariant holds with a wide margin. `self_contrast` is **bit-identical at all five sizes** — this was cleared in advance rather than discovered afterward: at 32px p10 = 0.247 is owned by the block (p10-band centroid (0.48, 0.46)) and p90 = 0.851 by the trued plane, while the curl's whole 32px range was 0.405–0.857, so its darks could not have moved a percentile even in principle. `mask_iou` improves at all five. Cost in work: `work/w0.py`–`w15.py`.

**What is still short, and why it is not this round's to fix.** The ribbon's *median* ships at 0.657 against C2's 0.702. The floor and the ceiling both land; the middle does not, and it cannot be lifted from here — the outer face's mid-tones are set by `OUT_LIT`/`OUT_DARK` and moving them is a palette edit, the same class already holding the arris short in round 16 and the same 0.22 trued-plane fault behind both. Three rounds have now ended pointing at the palette. That is the next class to open, and it should be opened deliberately rather than smuggled in under a detail round.

**Reusable construction — "a thin material has a floor, and the floor does not fall off with depth".** Any thin, planed, translucent form — a wood shaving, a paper curl, a petal, a fingernail, a leaf, a lampshade — is lit by two things, and generators routinely model only the first. The reflected term is a lambert on the face you can see and it goes to zero when that face turns away. The **transmitted** term is the light that comes through one thickness from the other side, and it is what stops the turned-away parts from going black. Model it as three separate constants, because they are three different paths and they peak in different places: transmission proportional to how squarely the key strikes the *far* side (`max(0, ±lam)`, sign chosen by which face the eye is on), a **bore floor** for any concave interior — an enclosed interior is walled by lit material on every side, so it has a minimum independent of any local normal — and an ambient **sheen** through the sheet with the key aside. Set them by measuring, not taste: find the reference's most turned-away passage and its deepest interior, and take each as a delta against the ground *beside* it rather than as an absolute, so the comparison survives a different palette. The diagnostic that catches this class in one number is **"how far below the ground it covers does this form ever fall?"** — a solid can fall arbitrarily far, a thin translucent one cannot fall past its own transmission, and our 0.388 against the reference's 0.05–0.11 named the defect before a line was written. Two traps to avoid. First, an occlusion term multiplied *into* a lambert can drive a surface to black no matter how much light physically reaches it; a transmitted term must be **lerped toward the transmission colour after the occlusion**, never multiplied by it, or the floor inherits the very zero it exists to prevent. Second, check where a transmitted term is actually non-zero before trusting it: ours was gated on `max(0, lam)`, which switches off precisely on the deep interior bands it was written to rescue, so the code read as though the physics were present while the render showed it was not — **a term that is switched off everywhere it matters is indistinguishable from a term that is absent, and only sampling the render tells you which one you have.**

## Round 18 — the small-size repair round: the shadow had the right depth and a quarter of the reach

`loop-runs/r17/`, measured against `loop-runs/r16/`. Edit class: small-size repair. One layer added to the shadow stack in `#mid` and one filter to `defs`; the two existing shadow layers were hoisted into named constants and **left at their round-17 values to the digit**, which is the point of the round rather than an accident of it. No geometry, no silhouette point, no light constant, no grain constant, no palette entry — the new layer reuses `#4B4133`, the cast shadow's own colour. Structure 1785 → **1786 paths and 307,135 → 308,310 bytes** (+1,175), gradients 18, filters 11 → **12**, PASS against 3000/350,000.

**Four in-class hypotheses measured and killed before anything was authored.** (1) *Native small-size rendering aliases* — false: `rsvg` rendering straight to 16px against a supersampled 1024→16 downsample differs by mean |Δ| **0.0131**, worst pixel 0.0855 (32px: 0.0104). There is no gross AA or seam artifact in the scored pixels, so nothing is lost between 1024 and the small renders that is not lost by honest averaging. (2) *The hone line washes out at small size* — false, and inverted: our hone keeps **more** saturation than C2's at every size (16px 0.591 against 0.531, 32px 0.642 against 0.641). The same sweep found C2 warm nearly everywhere — 25% of its pixels carry redness > 0.06 against our 9% — which is a global colour-temperature difference and out of class. (3) *The grain is the small-size offender* — false by direct attribution (`work/w6.py`, `work/w7.py`): zeroing `GRAIN_AMP_A`/`GRAIN_AMP_B` clears **0 of 20** false-positive edges at 32px, 7 of 589 at 128 and 25 of 1648 at 256. Round 17 had already found our tear quieter than C2's; this settles that it is not merely quiet but structurally irrelevant at the sizes in question. (4) *The 256px edge collapse (precision 0.24) is a texture problem* — false: FP and FN are dense in the **same** 32×32 blocks along the block's and the curl's silhouettes (`work/w8.py`), which is the signature of registration rather than texture, amplified because the ±1-cell match tolerance at 256 is only ±4 canvas px while our silhouette sits 20–40px off. Coarse structure, out of class.

**The class tension, stated rather than worked around.** At the two sizes this class owns there is almost nothing structural left to win: 16px runs **0 FPs and 1 FN** (`edge_f1` 0.9940), and `mask_iou` at 32/16 is pinned at 0.9688/0.9844 by the squircle-versus-full-bleed area ratio, which no edit short of going full-bleed can move. Of the 20 FPs at 32px only 4 are arguably in class (the curl, canvas x192–320 y224–288, our |grad| 0.55–0.65 against C2's 0.19–0.33); the rest are the same registration offset. So the remaining 32/16 headroom sits in `ssim` and `lum`, and a region-detrended residual (`work/w4.py`) says where: the rough ground is essentially exact (median **+0.003**, sd 0.060), the block's value matches (median −0.015, its p10 of −0.28 purely silhouette offset), and the whole error is the trued ground at **+0.183** — the palette fault three rounds have now pointed at. The one defect in this class that is neither palette nor registration is how the solid seats on the ground, and that is what the round spent itself on.

**What was measured off the reference.** A shadow cannot be read by subtracting two images whose grounds differ by 0.22, and two earlier attempts at the profile failed for instructive reasons: binning by distance from the dark mass pulled un-planed ground into the near bins on our side only (our two planes differ by 0.15, C2's barely), and fitting each image's cut by luminance step returned 26.6° for C2 with a step of only 0.094, against the 41° round 16 measured independently — the cut is not the strongest step in that image. The reading that works (`work/w11.py`) uses the one landmark both images share unambiguously: **the hone line**, recovered as the principal axis of the pixels with redness > 0.20. Ours fits at bearing 33.3° through (491, 635), which recovers `ANGLE = 33.0` from the render and validates the method; C2's fits at 39.0° through (499, 590). Ground luminance is then binned by **true perpendicular distance from each image's own hone**, on that hone's down-right side where both images are trued plane for 250px, and each profile is normalised by its own far field at 235–285px. That normalisation is what makes the comparison survive the palette.

The result split cleanly in two. **At contact the two agree**: 0.670 of far field in the first 8px against C2's 0.662, and 0.636 against 0.614 at 8–16px. The `castShadow`/`contactShadow`/`seatShadow` stack, built over rounds 4 and 7, was already right where it touches down. **The reach did not agree at all**: ours was back to 0.965 of far field by 60–85px and 1.003 by 85–115px, while C2's was still 0.865 at 60–85, 0.940 at 85–115, 0.982 at 115–150 and only reached its far field past 150px. Half-recovery ours ≈ **26px against C2's ≈ 64px** — the tail was 2.4× too short. At 32px that entire gradient lived inside one or two cells and at 16px inside less than one, so the lowest-frequency fact in the icon, that the solid is *sitting on* the ground, did not survive downsampling.

**The obvious repair was measured and refused.** Widening the existing cast (`stdDeviation` 26 → 62, offset and opacity scaled to hold contact) does move the far bins onto C2 and does gain the large sizes: +0.0085 at 1024, +0.0064 at 256, +0.0063 at 128. But at 32px it **added 24 edge cells in a diagonal chain along the blade's lower flank** — (17,23) (18,22) (19,20) (20,19) (21,17) (22,16) (23,14) (24,13) (25,12) — where our |grad| rose to 0.41–0.55 against C2's own 0.04–0.25 (`work/w12.py`). A single Gaussian has a slope peak, and at 32px that peak *is* an edge; C2's ramp has no inflection anywhere. `edge_f1` fell 0.9329 → 0.9194 and 32/16 went negative (−0.0036, −0.0002) — a small-size repair round making the small sizes worse. Broadening the occlusion layer as well (9 → 20) fitted the profile best of anything tested (residual sum 0.136 against C2) and cost the same edges, so it went too.

**The construction.** The tail is carried by a **separate layer that is too weak to have a visible slope anywhere**: the same `SILHOUETTE`, offset (58, 66), `#4B4133` at `HALO_OP = 0.30`, blurred at `HALO_BLUR = 105`, drawn *under* the existing cast so the near stack is untouched. Blurred that wide the peak slope is roughly `0.4/σ` per px, which over a 32px cell is a step the 32px Sobel reads as ≈0.15 — under the 0.4 threshold with margin — while the integral over 100px is exactly the 0.03–0.06 of far field the far bins were missing. Amplitude was fitted, not chosen: swept at 0, 0.18, 0.30, 0.42 and read back through the same hone-perpendicular profile, 0.30 gives the closest fit that does not overshoot the contact bins it must not touch (normalised residual sum **0.250**, against 0.350 at 0.18 and 0.243 at 0.42 bought by going 0.025 too dark at 8–16px).

| size | before (r16) | after (r17) | delta |
|---:|---:|---:|---:|
| 1024 | 0.5123 | **0.5252** | +0.0129 |
| 256 | 0.4757 | **0.4865** | +0.0108 |
| 128 | 0.5274 | **0.5370** | +0.0096 |
| 32 | 0.8034 | **0.8058** | +0.0024 |
| 16 | 0.8392 | **0.8400** | +0.0008 |

Pareto gate **ACCEPT at +0.0365 net, all five sizes positive**. Like round 17 this is a value round, not an edge round: `lum_delta` falls at every size (1024 0.1242 → 0.1154, 256 0.1201 → 0.1114, 128 0.1178 → 0.1090, 32 0.1141 → 0.1057, 16 0.1092 → 0.1027) and 32px `edge_f1` **rises** 0.9329 → **0.9336** with the FP count unchanged at 20 — the three added cells are true edges, the seating becoming legible to the detector at the size where it had been invisible.

**A better-scoring variant was refused, and this is the round's disagreement with the gate.** Blur 130 at 0.34 scores better at every single size — net **+0.0568** against our +0.0365, including +0.0036 at 32 and +0.0041 at 16. It loses anyway, on three counts. It fits the measured shadow *worse* (normalised residual 0.285 against 0.250); its 32px `edge_f1` goes down (0.9326) where ours goes up; and its gain is concentrated in `lum_delta` (0.1020 at 32px against our 0.1057) because a 130px blur spills across the whole trued plane and **pays down the +0.183 palette fault with a shadow layer**. It also spends the contrast budget to do it: 16px `self_contrast` 0.5598 against a floor of 0.5572, 0.5% of the permitted 6%. That is the composite rewarding a value error being fixed in the wrong place, which is now the third time this loop has caught it doing so, and the palette round will need the budget it would have consumed.

**What it cost.** `ssim` falls at four sizes (256 0.5869 → 0.5843, 128 0.5716 → 0.5624, 32 0.6125 → 0.6010, 16 0.6430 → 0.6304) and rises at 1024 (0.6198 → 0.6231); the composites rose anyway because luminance gained more than structure lost, and the fall is the expected one — SSIM's local-variance term is reduced by any smooth low-frequency wash, which is precisely what was added. `self_contrast` 32px 0.6036 → **0.5824** against a floor of 0.5674 and 16px 0.5928 → **0.5746** against 0.5572: about half the 6% budget spent, deliberately and once. This was cleared before the layer was authored rather than after — p90 is owned by the trued plane and p10 by the block, and a wash that darkens the mid-field moves neither percentile much. Polarity **+0.135** (un-planed 0.691, trued 0.826) against round 17's +0.150; the 0.015 is the halo reaching the un-planed sample band, and the invariant holds with margin. Rubric: no check moves down and **#8 depth coherence**, the check the round targets, strengthens. #1 mask discipline was verified rather than assumed (`work/w13.py`) — a 105px blur thrown down-right could pool against the squircle's lower-right rim and read as a baked corner shadow, so the tile was profiled by octant × rim band, and every octant's change **grows inward**: largest at r 0.60–0.80, smallest at the mask edge, with the SE rim band unchanged to four decimals. Cost in work: `work/w0.py`–`w13.py`.

**Reusable construction — "a penumbra is two facts, and only one of them is depth".** Contact darkness and reach are independent, they are set by different physical quantities (occluder proximity versus source solid angle), and a generator that models a cast shadow as one blurred silhouette can only trade one against the other. Measure them separately and in the right frame: bin ground luminance by **true perpendicular distance from the contact line**, recovered from a landmark both images actually share — a hone, a seam, a waterline, the lit arris — rather than from a fitted luminance step, which will find the wrong feature and did, returning 26.6° where the answer was 41°. Then **normalise each profile by its own far field**, which is what lets a shadow be compared across a 0.22 palette offset at all, and read off two numbers: the fraction of far field at contact, and the distance at which the profile is halfway back. The second is the one generators get wrong, typically by a factor of two or more, and it is the one that owns whether the object seats at 16 and 32px, because a tail shorter than one cell is a tail that does not survive downsampling. The repair is **not** to widen the blur you have. Widening spends contact depth to buy reach, and worse, a single Gaussian's slope peak becomes a false edge at small sizes — a hard-edged crescent tracking the silhouette a fixed distance out, which is exactly the artefact a soft shadow is supposed to avoid. Add a **second, much wider, much weaker layer** underneath instead, sized by two arithmetic checks that can be done before a line is written: peak slope ≈ `0.4/σ` per px must give a per-cell step your edge detector will not see at the smallest scored size, and the layer's integral must supply the far-field deficit you measured. Contact then stays exactly where it already matched, which is the whole reason to hoist the existing layers into named constants and leave them alone. Two traps. First, a filter region sized for the old blur will clip the new one into a hard rectangle — widen `x`/`y`/`width`/`height` with `stdDeviation` or the fix ships as a visible box. Second, a wide wash will always flatter a luminance metric if your ground is too bright, so check *where* the gain came from: if `lum_delta` improves far from the object as much as near it, you have not fixed a shadow, you have dimmed a plane — and the honest version of that fix belongs in the palette, at full strength, where it can be seen and argued with.

## Round 19 — the small-size repair round: the shaving was lit like a highlight, and it is the same wood as the board

`loop-runs/r18/`, measured against `loop-runs/r17/`. Edit class: small-size repair. **One constant moved**: `OUT_LIT` (243, 234, 216) → **(216, 208, 192)**, the outer face's lit end. No geometry, no light vector, no palette entry, no ground field, no shadow constant, no path added, no filter added. Structure is byte-identical in size — **1786 paths, 308,310 bytes**, gradients 18, filters 12, PASS against 3000/350,000 — because a hex triple is a hex triple; the render hash is not.

**Where the gap was, by attribution rather than by eye.** The 32px false-positive set was rebuilt with each candidate layer switched off in a copy of the generator (`work/w10.py`), on the principle that an artefact surviving a layer's removal was never that layer's: of the 20 FPs, `SHAVING=0` clears **4**, `CAST_OP=0` clears **3**, and zeroing the grain, the halo and the fibre relief clears **0** between them (the grain adds one). The four are the roll's upper-left rim, cells (7,7) (8,7) (9,7) (6,8), ours |grad| **0.138–0.164** against C2's **0.046–0.082** and collapsing to 0.014–0.026 with the layer gone. The ten-cell diagonal chain at x4–6 y17–23 is the block's silhouette and its contact shadow — registration, out of class, as it has been for three rounds.

**Two in-class repairs were measured and refused before anything was authored.** (1) *The trued plane's key falloff is too flat.* True, and by a clean margin: profiled on `u = (x+y)/√2` with everything within 240px of either image's own dark mass held out (`work/w5.py`, `work/w6.py`), our relative slope runs **−0.0132 per 100u against C2's −0.0242**, so C2 falls 1.4–1.8× steeper, and an order-2 fit of the plane returns R² 0.606 against C2's 0.863. The pivot that steepens it without moving the plane's mean was computed rather than guessed at **L 0.8368** (`work/w7.py`, exact to four decimals across the k sweep, no clipping at the bright end). Then the gain was simulated on the native 32px render before authoring: **+0.0031 SSIM at best (k=1.35), negative by k=1.85**, and +0.0004 at 16px. The measurement was right and the repair was not worth the round. (2) *Widen the cast's penumbra so its slope peak stops being an edge.* Swept `CAST_BLUR` 26 → 40/52/64/80 with and without opacity compensation (`work/w11.py`): **every point loses at 32 and 16**, and the FP count goes the wrong way — 20 → 22, 21, 22, 21. Blur does not remove a shadow's small-size false edges. At 32px a cell is 32 canvas px and the Sobel window spans 96, so a σ of 26 and a σ of 80 both sit inside one window; the cell gradient is set by the shadow's **depth over that window**, not by its σ, and widening only relocates the peak into fresh cells. That is a correction to the natural over-reading of round 18's own construction note: the second, weaker layer works because it is *weak*, not because it is *wide*.

**Half the remaining small-size SSIM is not repairable and should stop being counted.** At 32px, **791 of 1024** SSIM windows touch a non-opaque cell and average **0.548**, against **0.781** for windows entirely inside the squircle (`work/w3.py`). Split by region on clean cells only: rough 0.834, block 0.860, curl 0.826, **trued 0.586** — and the trued figure is the +0.183 palette fault again, its correlation term running +0.330 above its total. So the 32/16 SSIM deficit is a squircle-versus-full-bleed delivery difference that `edge_f1` already excludes and SSIM does not, plus the palette. Neither is this class.

**What was measured off the reference.** Four lines walked outward from the roll's centre through the four FP cells, sampling both images every 4px (`work/w13.py`). Ours steps **+0.18 L in 4 px** — 0.719 → 0.900 at (7,7), 0.694 → 0.874 at (8,7) — and then *holds* 0.90–0.93 across 25–50px of band. C2 over the same ground stays inside **0.70–0.78 with no step anywhere**, a local range of 0.02–0.06. Put on a footing that survives the palette offset (`work/w14.py`): take the curl's own footprint from the `SHAVING=0` twin (52,942px) and the board annulus 10–40px outside it (50,310px), and ask each image how far its bright end inside the footprint sits above **its own** annulus median. Ours **p90 +0.1700, p99 +0.2468**. C2 **p90 +0.1165, p99 +0.1685**. Both ratios come out at **0.68**.

**What changed and why.** A shaving is one thickness of the same wood as the board it lies on, under the same one soft key. It can be a little brighter than that board; it cannot be a specular highlight a quarter of an L above it, and nothing else in this icon presents one. So the outer face's excess over its board is scaled by the measured 0.68 — and the constant was solved against the rebuilt measurement rather than read off a score sweep (`work/w15.py`): (216, 208, 192) lands **p99 +0.1681 against the target +0.1685** and p90 +0.1224 against +0.1165, where the neighbours (209, 201, 186) and (221, 213, 197) land at +0.1513 and +0.1837 on p99. `TRANSMIT`, `CURL_BORE` and `CURL_SHEEN` are untouched: round 18's note fitted the bore against C2 and measured it right, and this is the outer face only.

| size | before (r17) | after (r18) | delta |
|---:|---:|---:|---:|
| 1024 | 0.5252 | **0.5266** | +0.0014 |
| 256 | 0.4865 | **0.4902** | +0.0037 |
| 128 | 0.5370 | **0.5403** | +0.0033 |
| 32 | 0.8058 | **0.8008** | −0.0050 |
| 16 | 0.8400 | **0.8408** | +0.0008 |

Pareto gate **ACCEPT at +0.0042 net**. `ssim` rises at all five sizes (1024 0.6231 → 0.6237, 256 0.5843 → 0.5858, 128 0.5624 → 0.5639, 32 0.6010 → 0.6038, 16 0.6304 → 0.6332) and `lum_delta` falls at all five (0.1154 → 0.1149, 0.1114 → 0.1109, 0.1090 → 0.1086, 0.1057 → 0.1055, 0.1027 → 0.1025).

**What it cost, and the round's disagreement with its own gate.** 32px went **down 0.0050 — exactly the tolerance — and cleared on the boundary**, on `edge_f1` 0.9336 → **0.9171**. The interesting part is which half moved: precision barely (0.9061 → 0.9038) and **recall fell 0.9628 → 0.9309**. The FP count is *unchanged at 20*: the four curl cells survive, but their |grad| drops from 0.138–0.164 to **0.106–0.120** against C2's 0.046–0.082 — from two to three times the reference's down to just over the 0.10 threshold. The loss is six new false negatives, at (9,3) (10,3) (8,4) (9,4) (6,5) (7,5), C2 |grad| 0.107–0.198 — canvas y 96–192, **above our roll entirely** (our shaving's bbox starts at y 167). C2's shaving reaches further up the tile than ours does, and our over-bright band had been aliasing into cells within the ±1 match tolerance of C2's edges up there. Round 18's 32px `edge_f1` was therefore part-funded by a material error standing in roughly the right postcode. That trade is worth more than the 0.005: the number went down, the material got right, and what it uncovers — our roll sits lower and smaller than C2's — is a geometry deficit for a later round rather than a brightness one, and it was invisible while the brightness was paying for it.

Everything else held. `self_contrast` **32px 0.5824 → 0.5820** against a floor of 0.5674 and **16px 0.5746 → 0.5746** against 0.5572 — the contrast budget is untouched to the digit, which was expected before authoring rather than checked after: p90 is owned by the trued plane and p10 by the block, and the curl sits between them. Polarity **+0.137** (un-planed 0.689, trued 0.826) by `measure.py icon.png 33.0 543 604 640`, against round 18's +0.135; invariant holds, trued brighter. Rendered check at 1024 and at 32/16 against C2 (`work/r18-curl-1024.png`, `work/r18-small.png`): the roll still reads by its terminator and its bore, figure-ground and the 16px read are pixel-unchanged outside the curl's own 22-pixel patch, and the single-light model gains — the icon no longer contains one highlight that no other surface in it could make. **The #4 deduction**, the curl's small-size noise, moves the right way again on its own quantity (`work/w17.py`): 32px mean |ΔL| against the ground it covers **0.0526 → 0.0469** with worst pixel **0.1342 → 0.1025**, 16px mean **0.0422 → 0.0383**. One honest counter-movement: the patch's *signed* mean goes **−0.0198 → −0.0280** at 32px and −0.0180 → −0.0245 at 16px, so the roll sits a shade darker overall than it did. That is the next thing in this feature and it is already measured — inside the footprint, C2's median sits **+0.033 above its board** where ours sits **−0.038 below** it, so the shaving's *mid-tone* is about 0.07 too dark while its bright end is now right. That is the bore and the dark flank, a second constant with a second justification, and it belongs in its own round. Cost in work: `work/w0.py`–`w17.py`.

**Reusable construction — "a highlight is a claim about material, and two surfaces of the same stuff cannot disagree about it".** When a generator paints an object lying on a ground of the same material, the number to fit is not the object's luminance but **its excess over the ground immediately around it**, and the way to get that number out of a reference whose palette you do not share is a footprint-and-annulus statistic: recover the object's own footprint from a build with that layer switched off, dilate it to a ring 10–40px outside, and read p90 and p99 inside the footprint **minus that image's own annulus median**, in both images. The subtraction cancels a palette offset of any size — ours differs from C2's by 0.18 and the comparison is unaffected — and p99 catches the specular claim that a mean or an SSIM will average away. Ours ran 1.46× C2 on both percentiles, which is one number, not two, and that is the tell that a single shading endpoint owns it: solve that endpoint back against the rebuilt statistic, not against the score, and check the neighbours to confirm the solution is a fit and not a slope. Two things to expect. First, **this class of fix will usually cost `edge_f1` at the smallest size**, because an over-bright feature generates edges that land near the reference's real ones by luck of position; when the false gradient goes, the lucky matches go with it, and precision will barely move while recall drops. Read the FN list before concluding the edit was wrong — if the new misses sit in cells the object does not even reach, the recall was never yours. Second, fixing the bright end alone **pushes the object's signed mean darker**, because you have lowered the top of its range and nothing else; measure the median relationship at the same time you measure the bright one, so you know in advance whether you are finishing the feature or handing half of it to the next round.

`banner.png` is 3200 × 1040, rendered from `banner-src.html` at 1600 × 520 × 2. Its ground split was originally derived from C2's 40° cut; since A ships, it was **re-derived on A's 33° boundary** and now runs from (0, 475.5) to (731.9, 0) — the exact extension of the icon's own cut through the icon's placed position. Verified continuous to within 0.1px at both ends of the tile, and checked on a 2× crop of the junction rather than assumed. The pass runs off the tile and keeps going across the banner. Any future change to the icon's angle or placement must be re-derived there, or the cut kinks visibly. The banner no longer has to compensate for the icon's lighting the way it did for C2.

## Round 20 — the small-size repair round: the shadow had a shelf in it, because the penumbra was the shadow of a body lying flat

`loop-runs/r19/`, measured against `loop-runs/r18/`. Edit class: small-size repair. **One constant moved**: `CONTACT_BLUR` 9.0 → **18.0**. No geometry, no light vector, no palette entry, no opacity, no offset, no path added, no filter added. Structure **1786 paths, 308,311 bytes** (one byte more than r18 — `18.0` is a character longer than `9.0`), gradients 18, filters 12, PASS against 3000/350,000.

**Attribution first, and it says most of what is left is not this class.** The 32px FP set was rebuilt with each layer switched off (`work/w1.py`): of 20 FPs, `SHAVING=0` clears **4** (and costs 5 FNs), `CAST_OP=0` clears **3**, and the grain, the fibre and the cut step clear **0** between them. At 16px there is nothing to attribute at all — **FP 0, FN 1, f1 0.9940**. Then three candidate defects were measured and refused before anything was authored. (1) *Something aliases.* No: native 32px against a LANCZOS downsample of the 1024 render gives mean |d| **0.0097**, p99 0.0537, and the worst cells are the squircle rim and the block silhouette, i.e. ordinary antialiasing (`work/w4.py`). (2) *The roll's small-size contrast is too hot.* No, not in its own frame: measured with each roll masked by its own footprint — ours from the `SHAVING=0` twin, C2's from round 8's fitted rims — the two cover the same area (65 cells against 64) and hold the same internal gradient, **p50 0.077 against 0.080, p90 0.116 against 0.133** (`work/w9.py`, `work/w10.py`). Our hottest cells are all on the roll's *boundary against the block*, and C2's roll simply does not overlap its block there: C2's rims sit at (294,253) and (359,186), ours at (308,278) and (243,345), so ours is about **50px left and 98px low** — three 32px cells — which is the same registration deficit rounds 18 and 19 both ended on. (3) *Our board texture survives to small size as noise where C2's averages away.* Backwards: local std over open board is **0.84× C2's at 1024 and 0.71× at 128**, and the 32/16px excess (1.43×, 1.44×) collapses to 0.95×/1.10× when the upper-left rough half is measured alone (`work/w12.py`). The excess lives in the trued plane and is its brightness ramp, i.e. ground contrast, which the budget rules out of this round.

**What was measured off the reference.** The near shadow, re-profiled off the landmark both images carry rather than off the mass (`work/w7.py`). Rounds 17–19 marched from the dark mask's centroid, which at the block's end caps sends a ray *along* the shadow instead of across it; here the contact line is recovered from the hone in each image and every ray crosses the long lower-right edge. The hone has to be picked out by **saturation, not warmth** — both boards are warm, C2's r−b runs to 0.20 at p99 over open board, so `r−b > 0.16` selects 99k px of C2's ground and returns a meaningless axis; `r−b > 0.30 and r−g > 0.15` isolates **4,508 px in C2 on a 39.0° axis and 13,579 in ours on 33.4°**, each image's own ground line to a tenth of a degree. Normalised by each image's own far field, 45 rays each, the two shadows **agree on contact and on total** — band means 0.668/0.597/0.674/0.808/0.888/0.941/0.983 against 0.711/0.604/0.645/0.783/0.897/0.958/0.992, deficit integral **23.8 against 23.2** — and disagree in exactly one place. Ours climbs 0.604 at u=16 to 0.760 at u=32 and then **shelves at 0.767 through u=36**; C2 climbs 0.611 to 0.688 over the same span and keeps climbing with **no inflection anywhere**. Worst point **+0.072 of far field at u=32**. A tight core, a shelf, then a tail is the signature of two Gaussians whose peaks sit 30px apart, and at 32px that shelf's leading slope is the false edge — the three cells `CAST_OP=0` clears.

**What changed and why.** A σ of 9 asserts a contact this block does not have. It rides as a wedge — `RISE_NEAR` 48 at the leading edge to `RISE_FAR` 132 at the trailing one — so along most of its length the occluder stands well off the board, and a penumbra's half-width grows with that gap. Swept against C2's own profile (`work/w8.py`), |resid| over the first 128px falls **0.0225 → 0.0206 (14) → 0.0174 (18) → 0.0168 (24)** and back up to 0.0231 at 38; at u=32 the profile goes 0.760 → 0.718 (18) → 0.721 (24) against C2's 0.688. 24 fits marginally better and **costs a 32px edge cell** (FP 20 → 21, f1 0.9171 → 0.9152), as do 30 and 38; **18 is the widest value that costs none**. Opacity compensation was tried at every width and is uniformly worse — 0.0179 at 24/0.50, 0.0215 at 30/0.52, 0.0326 at 38/0.60 — because the fault is where the ink sits, not how much there is.

| size | before (r18) | after (r19) | delta |
|---:|---:|---:|---:|
| 1024 | 0.5266 | **0.5264** | −0.0002 |
| 256 | 0.4902 | **0.4911** | +0.0009 |
| 128 | 0.5403 | **0.5423** | +0.0020 |
| 32 | 0.8008 | **0.8008** | 0.0000 |
| 16 | 0.8408 | **0.8408** | 0.0000 |

Pareto gate **ACCEPT at +0.0027 net**. `lum_delta` falls at 1024/256/128 (0.1149 → 0.1147, 0.1109 → 0.1107, 0.1086 → 0.1084) and is unmoved at 32/16; `ssim` moves +0.0005 at 1024 and +0.0003 at 128 and is flat elsewhere; `edge_f1` **rises at 128 (0.4672 → 0.4736) and 256 (0.2446 → 0.2472) and falls at 1024 (0.3518 → 0.3492)**, which is the honest shape of the edit — removing an inflection removes gradient the 1024 Sobel was scoring and the mid sizes were mis-scoring. `mask_iou` is identical at all five sizes.

**What it cost, and the round's own disagreement with itself.** Two costs, both declared. First, the 32px and 16px edge sets are **exactly unchanged** — FP 20 / FN 13 / f1 0.9171 and FP 0 / FN 1 / f1 0.9940 — so this repair, in a round whose class is small-size repair, bought nothing at 32 or 16. The shelf is gone from the profile and the three cast-attributed cells still sit above the 0.10 threshold: they are inside one Sobel window of the block's own silhouette, which is 1.5 cells out of place, and no shadow constant reaches that. Second, the deficit integral moves **23.8 → 24.6** against C2's 23.2, i.e. further from C2 on total while closer on shape. That is not new ink — the layer's opacity and silhouette are untouched — it is ink that previously sat *under* the block becoming visible as the blur widens, and the alternative reading (that the round paid down the palette fault with shadow, the trap rounds 17 and 18 both flagged) is ruled out by the opacity sweep being worse at every width. `self_contrast` **32px 0.5820 → 0.5820** against a floor of 0.547 and **16px 0.5746 → 0.5746** against 0.540: the contrast budget is untouched to the digit, as expected — p90 is the trued plane and p10 the block, and a penumbra 15–45px outside the silhouette is neither. Polarity **+0.137** (un-planed 0.689, trued 0.826) by `measure.py icon.png 33.0 543 604 640`; invariant holds, trued brighter. Figure-ground, the 16px read and the single light model are unmoved at the pixel. Cost in work: `work/w1.py`–`w16.py`.

**Left on the table, measured and ready.** The board behind the plane has **no seat at all**. Round 7's occlusion band is `clip-path="url(#truedSide)"` by construction, so the body is seated on the side its shadow falls and floats on the side facing the light. Profiled outward from the top-back edge, normalised by each image's own board (`work/w13.py`): C2 runs **0.703, 0.822, 0.784, 0.739, 0.728, 0.755, 0.819, 0.929, 0.950** over u=0..8 — a trough bottoming at u=4 and back to board by u=8 — where ours is **flat at 0.96 from u=2**. That is a **0.20-of-board absence**, three times the shelf this round repaired, and it is not uniform: by position along the blade C2 reads **0.688 leading, 0.642 middle, 1.030 trailing**, deep over two thirds and simply gone at the third, the opposite ramp to the front seat. Sweeps of a hem stroked along `CHAIN_UPPER` and clipped to `roughSide` (`work/w14.py`–`w16.py`) land the shape — stroke 8 through σ2.5 at 0.55 takes |resid| over the first 24px from 0.0529 to **0.0278** and puts recovery on C2's exactly (0.947 at u=8 against 0.950) — but **every variant tried costs a 32px false positive**, and the taper needed for the trailing third would not respond to a `BLADE_LEN` gradient in either direction, so its axis has to be derived rather than borrowed. Bigger error, wrong round: it wants the geometry round the registration deficit has been waiting for.

**Reusable construction — "measure a shadow off a landmark both images own, and read its second derivative, not its depth".** Two halves. First, the frame: when comparing a shadow between a build and a reference whose object is not in the same place or the same size, do not march from the dark mass's centroid — at the ends of a long body the ray runs along the shadow rather than across it, and the two fans stop sampling the same edge. Find a feature both images carry along the contact line (here an emissive hone), fit its principal axis in each image separately, march perpendicular to **that**, and take u=0 where each ray leaves its **own** silhouette. Pick the feature out by the channel difference that is actually diagnostic — a warm line on a warm ground needs **saturation** (`r−b` *and* `r−g` together), not warmth, or the mask swallows the whole board and the fitted axis is noise; the check that it worked is that the recovered angle lands on the geometry you already know, ours 33.4° against a 33° blade and C2's 39.0° against the 38.9° top-face edges. Second, the quantity: with each profile normalised by its own far field, depth and reach can both agree while the shadow is still wrong, because what a small-size Sobel reads is the profile's **inflection**. A stack of Gaussians whose peaks are separated by more than the tighter one's σ produces a core, a shelf and a tail, and the shelf's leading slope is a false edge at any size where it falls inside one cell. Scan the first difference of the normalised profile for a flat spot; if there is one and the reference's is monotone, the repair is the **width of the tighter layer**, and the right value is the widest one that does not add a small-size edge cell, not the one that minimises the residual. Expect this to buy nothing on `edge_f1` when the same neighbourhood also holds a silhouette that is more than one cell out of register — the silhouette's own window dominates, and a shadow constant cannot reach it.

## Round 12 (`loop-runs/r11`) — the detail round: the field was a lattice because every ridge ran on one of two bearings

*Bookkeeping first, because this file's numbering is now ambiguous.* Round N here
is `loop-runs/r(N-1)`, so this is the entry for `loop-runs/r11`, measured against
`loop-runs/r10-reverted`. The Round 12–20 entries above belong to a branch taken
from a later state and are superseded by this one at r11; they were not used to
author this round. Where they and this entry agree, that agreement is convergent
rather than copied.

**What was measured off C2.** The round's candidate edits all move ground texture,
so the instrument is the orientation histogram, not a region mean: high-pass each
plane (subtract a 12px box mean, so the field's own lighting ramp drops out), take
the gradient, and bin gradient ENERGY by ridge bearing. Windows are 96px, admitted
only when 100% inside one plane with the block and curl detected and dilated out
(`work/m3.py`, `work/m4.py`). Read that way the master's `grain()` docstring is
wrong about the thing it is built on. Its claim is that C2 carries "a cross-hatched
torn lattice"; C2's un-planed field is **one broad lobe** centred near the travel
direction — peak/mean **2.18**, peak/cross **3.48**, per-window dominant bearings
all inside 22–42° across twelve clean windows from r 300 to 1100. Ours was **two
spikes exactly 90° apart**, at 47.5° and 137.5°, peak/mean **6.73** and peak/cross
**1.66** — and 1.66 is the number a 90° cross returns by construction, which is
also why m1's doubled-angle coherence could not see the fault at all and had to be
thrown away. The trued plane says it twice: C2's is directionless fine noise
(peak/mean **1.15**, sd 0.0086) where ours ran the *same energy*, sd 0.0098,
organised into the same lattice (peak/mean **3.39**).

Two things were measured and then deliberately **not** acted on. C2's texture sd is
flat in distance from the key — 0.0239 / 0.0211 / 0.0211 / 0.0255 over r 300–1100 —
so the tear is *not* faded toward the light, however strongly the thresholded edge
map suggests it is; that apparent fade is the blown near-key field losing contrast
against a fixed threshold, not lost texture. And the whole edit is texture
*organisation*, not plane luminance, so it leaves p90 (trued ground) and p10
(block) alone by construction — which is why a detail round could be spent here at
all with the contrast budget binding.

**What changed.** One family instead of two, and its lobe width authored as wander.
`GRAIN_AMP_B` and both family-B loops are gone. The surviving family keeps its
bearing but stops being ruled: `GRAIN_WANDER` 3.4 → 11.0 and `GRAIN_NODE` 60 → 34,
because a node that displaces a ridge by ±1.7 units over 60 bends it 1.6° and
draws a straight line. Width is carried by wander rather than by fanning the
ridges across mixed bearings, since a fan of straight lines at scattered bearings
is just a randomised lattice — still crossings, only untidy ones — and `GRAIN_SKEW`
stays modest at 15°. One further fix rides along, in class and load-bearing: the
bearing is now solved **once per line and used on both sides of the cut**. It used
to be redrawn per side, so every line arrived at the hone on one bearing and left
on another — a kink at the exact place the icon makes its argument, invisible at
11° of scatter and not invisible at 15°. Pitch is untouched: dropping family B
removes about half the strokes and they add incoherently, so the field's sd falls
by roughly √2, 0.028 → **0.0218 against C2's 0.0223**. The energy was allowed to
land where the deletion put it rather than being tuned to arrive there.

| size | before (r10) | after (r11) | delta |
|---:|---:|---:|---:|
| 1024 | 0.4584 | **0.4585** | +0.0001 |
| 256 | 0.4725 | **0.4760** | +0.0035 |
| 128 | 0.5163 | **0.5188** | +0.0025 |
| 32 | 0.7865 | **0.7874** | +0.0009 |
| 16 | 0.8383 | **0.8377** | −0.0006 |

Pareto gate **ACCEPT at +0.0064 net**. Structure PASS, and the counts confirm the
edit is a deletion: 1726 → **1042 paths**, 321,547 → 229,422 bytes, gradients 12
and filters 11 unchanged. **SSIM rises at all five sizes**, by far the largest at
1024 (0.6185 → **0.6415**, +0.0230; 32px 0.6065 → 0.6129). That is the interesting
result, because r02 established that this composite punishes surface texture on
principle — uncorrelated detail inflates local variance while covariance stays
near zero. It does not punish texture *organised the way the reference's is*:
deleting structure the reference does not have raises SSIM at every size at once.
The r02 finding is about correlation, not about texture.

**What it cost, and the target left open.** `edge_f1` pays for it at 1024, 0.1574 →
**0.1267**, the round's one real regression: half the ridge strokes are gone and
there are fewer Sobel responses left to match C2's dense torn field. It gains at
the other four sizes (256 0.2474 → 0.2534, 128 0.4453 → 0.4496, 32 0.8938 →
0.8954, 16 pinned at 1.000). `lum_delta` rises slightly at all five (1024 0.1274 →
0.1284) for a reason worth recording: the un-planed plane brightened +0.011, so the
`_ridge` pair's stated mean-neutrality — "moves the un-planed plane by −0.0002" —
was **not actually holding for family B**, and removing it revealed the bias.
Polarity follows it down, **+0.156 → +0.145** by `measure.py icon.png 33.0 543 604
640`, the fixture invariant intact and inside the band the icon has carried since
round 4 (+0.143 … +0.184), but weaker; re-balancing `GRAIN_BAL_ROUGH` would recover
it and is a luminance edit, so it is left for a material round. `self_contrast`
holds with room — 32px 0.6138 against a 0.5762 floor (+6.5%), 16px 0.6017 against
0.5726 (+5.1%). Figure-ground at 128px **improves** against the un-planed field,
2.40:1 → 2.44:1, and is unchanged against the trued, 3.00:1 → 2.99:1; the block's
own L is bit-identical at 0.253, the edit having touched no subject constant. The
16px read holds (spread 0.617 → 0.605) and the single-light corner ordering still
holds, TL 1.095× of own ground mean against BL 1.063×, though by a narrower margin
than before (0.032 where it was 0.051). Gate and rubric agree; nothing regressed on
the rubric and the audit's one open deduction, the curl's small-size noise, is
untouched.

**One target is short and is left short.** The lobe is now single and roughly twice
as wide as it was — mass in the 40–60° bins went 42.5% → 70.5% — but at peak/mean
8.22 it is still narrower than C2's 2.18, and peak/cross went 1.66 → **146.8**
where C2 sits at 3.48. Both misses are the same missing ingredient, and it is not
lobe width: **C2 carries a strong isotropic floor**, 1.3–3% of energy in *every*
bearing bin including the ones 90° from its lobe, where ours now has 0.15%. That
floor is directionless pitting, not grain, so widening the lobe further cannot
produce it and would only smear the one bearing the icon has a physical reason to
draw. The construction that would supply it is already in the file and unwired —
`relief_filter` with `PIT_BF = (0.55, 0.55)`, isotropic by intent — and wiring it
is a filter-count change and a different class, so it is a handoff rather than
this round's business. Worth flagging with it: `relief_filter` and
`fibre_ramp_stops` are both defined and **called nowhere**, so the micro-relief
described in the round-8 comment block is not in any render scored since; the
rendered texture is entirely `grain()` and `mottle()`.

**Reusable recipe — "count the reference's bearings before you author its texture,
and use a statistic a cross cannot hide in".** Before authoring any directional
surface texture — grain, brushing, hatching, striation — bin the reference's own
gradient energy by ridge bearing over windows that are wholly inside one material,
and count the lobes. Three things fall out that cannot be seen any other way. The
lobe **count** tells you whether the surface is one process or two, and authoring
two where the reference has one is the expensive error, because it puts structure
into orientations the reference has none of and SSIM charges for all of it. The
lobe **width** is a property to author deliberately, and it is bought with wander
along each mark rather than with scatter between marks — same histogram, but wander
keeps the marks from crossing, and crossings are what read as woven. The **floor**
— energy spread evenly over all bearings — is a separate material (pitting, tooth,
sensor noise) and needs a separate isotropic construction; trying to reach it by
widening the lobe converts a directional surface into mush. Use peak/cross (the
peak bin over the bin 90° from it) and peak/mean, and **do not** use a doubled-angle
coherence: a 90° cross cancels in it exactly, so the one fault most worth catching
is the one it scores as perfectly isotropic. Cost is about 80 lines of numpy
(`work/m1.py`, `work/m3.py`–`m5.py`); the payoff here was a two-line deletion that
raised SSIM at all five sizes.

## The other takes

**Three engines, all four takes on the sheet.** Engine A now takes 12/12. Engine C raster took 9/12 (C1) and 7/12 (C2); both stay on disk as the reference takes the rebuild was judged against, and both hard-fail #10 as flat pre-masked rasters — the corpus's single most common failure. C1 additionally fails #11: its two sides sit only 0.09 L apart, so its improvement is present but too faint to be the read. Engine B (Arrow vector) came last at 5/12 with hard fails on silhouette and 16px: the blade forked into a wishbone and the vermilion hone line, the entire signature, was absent. Its steeper, flatter diagonal was the one thing worth salvaging.

**Register note:** five of the six sibling plugins share this porcelain ground, which makes it the marketplace's house register rather than a borrowed one (trawl owns the dark deep-sea register). Differentiation is carried entirely by the split, which no sibling uses, and by the fact that this is the only icon here whose tile is the thing being acted on rather than the stage the subject stands on.

## Files

| File | What it is |
|---|---|
| `build_icon.py` | The generator. **Edit here.** `SHAVING=0` rebuilds the round-4 tile without the shaving. |
| `icon.svg` | The layered master, generated. Four planes: `#bg` / `#mid` / `#fg` / `#highlight`. |
| `icon.png` / `icon-256.png` / `icon-128.png` | Shipped renders of the master. |
| `banner-src.html` / `banner.png` | README banner source and its 3200 × 1040 render. |
| `measure.py` | Split-polarity check. Trued side must measure brighter than un-planed. |
| `compare.py` | Renders the master beside both rasters at 256 with a 16px squint row. |
| `fidelity-runs-block/` | Fidelity harness runs for the round-6 pitch: `baseline/` is the flat master, `final/` is what ships. Score with `create-mac-icon/skills/create-mac-icon/scripts/fidelity.py`. |
| `render_audit.py` | Re-renders every take at 1024/256/64/32 and the three shipped PNGs. |
| `audit.html` / `audit-renders/` | The contact sheet, scores and verdicts. |
| `icon-engineC-*.png`, `icon-engineB-*.svg` | Reference takes, kept for comparison. |
