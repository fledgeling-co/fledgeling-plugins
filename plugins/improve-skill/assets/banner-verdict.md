Ships, re-rendered to close the same pre-existing drift as create-mac-icon's.

Its icon was revised on 08 August against a banner built on 07 August, so the banner showed a superseded tile. Fifteen hours of drift is small enough that the difference is subtle, which is exactly why nothing caught it and why the check is a hash comparison rather than an eye.

Only the artwork changed. IBM Plex Sans is linked, the frame is right, and the palette already came from the icon.

One liability inherited from the icon rather than introduced here: `improve-skill`'s `icon.svg` fails the structure gate at 1042 paths against a 400-path soup guard and 231KB against a 200KB envelope. That is an icon defect on the audit's list and not a banner one, but a banner derived from a path-soup master cannot be re-derived cleanly from named constants, so it is worth knowing where this composition came from.

Rows 5, 6, 8, 9, 10 and 11 are one person's assessment rather than a panel's.
