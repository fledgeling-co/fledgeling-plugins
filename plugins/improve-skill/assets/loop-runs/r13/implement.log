**Gate: ACCEPT** (net −0.0097, every size inside the 0.005 tolerance).

| size | r11 baseline | r13 |
|---:|---:|---:|
| 1024 | 0.4585 | 0.4538 |
| 256 | 0.4760 | 0.4732 |
| 128 | 0.5188 | 0.5175 |
| 32 | 0.7874 | 0.7871 |
| 16 | 0.8377 | 0.8371 |

One note on the fixture: the tree started from r12's rejected candidate (filters 12), not r11, so this round had to repair that loss before gaining.

**What I measured.** A control render — the same SVG with `filter="url(#ironGrit)"` stripped — showed the filter darkening its subject −0.0017 at 1024 but −0.0335 at 16. Replacing the turbulence and lighting with a constant flood reproduced 97% of that, so r12's undersampled-height-field diagnosis was wrong. Mapped at 128, the loss is a one-device-pixel dark seam around the group's silhouette, interior identically zero: `feComposite arithmetic` applied `k1 = 1/sin(50°)` to premultiplied **alpha** as well as colour, so every boundary pixel came out too opaque. Perimeter/area — 0.2% of the block at 1024, ~13% at 16.

**Change.** The normalisation moved to an `feComponentTransfer` (linear slope on R/G/B, alpha untouched); `k1` falls to 1. Interior bit-identical, 1042 paths, +214 bytes.

Rubric improves: figure-ground 2.86:1 was previously inflated by the fault; polarity +0.145 unchanged. r12's 256/128 `edge_f1` gains were the seam and are now given back.

**Reusable:** a scalar meant for colour must never ride in on `feComposite arithmetic`'s k1/k2/k3 — those coefficients cannot tell colour from coverage.

---STDERR---
