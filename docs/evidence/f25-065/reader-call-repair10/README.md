# F25-065 executable-helper and balanced-call repair

Fresh primary `a6dc3884` and distinct additional `9a131e61` are operative FAIL receipts against `d9f3d87`. Helper calls stored in an uninvoked closure or inactive `#if` could borrow a later direct reader; an uninvoked `@autoclosure` argument was mistaken for a later reader; and a top-level return did not terminate credit. The first repair also invalidated ten real Perch `withRetryStore` trailing-closure bindings, which its receipt incorrectly summarized as zero invalid scopes despite the log showing ten. That receipt is corrected in place and retained as failed evidence.

A helper occurrence must now be executable: direct calls and calls inside recognized control-flow blocks are accepted, while arbitrary closure and conditional-compilation placements are refused. Parenthesized calls are balanced through `)` before later-reader search, so arguments cannot count. A trailing closure is accepted only when the exact hashed helper's final closure parameter is directly invoked in its body. Top-level `return` and `throw` terminate later-reader credit.

Ten actual-scanner mutants independently arm syntax, references, reader closures/nesting/directives, helper execution context, helper closure invocation, balanced call tails, terminators and generic invocation context. All fail the permanent public-CLI fixture and restore source exactly. Focused 21-test and full 114-test gates pass twice. The actual Perch corpus again accepts 108 scopes with zero invalid records and retains four blind bodies plus REQ-056: five findings.

CP §7 self-review covered exact body/caller binding, qualified calls, loop contexts, stored closures, inactive directives, autoclosures, balanced calls, terminators, mutation restoration and correction of the false prior receipt. This remains a bounded lexical proof with deliberate false negatives. No install, publication, cache edit, push, Perch floor/credit rewrite, native app, provider, Keychain or live call occurred. Fresh primary and distinct additional reviews are owed.
