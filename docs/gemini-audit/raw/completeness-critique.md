**1. Both documents miss a 37-session Claude control corpus that already exists in the directory Document B cites for tooling.** `docs/gemini-audit/ctrl_metrics.json` holds 37 Claude sessions across the same repos (anvil, finance, splice, perch, egress, warden, hopper, dossier, proctor-mcp) alongside `gem_metrics.json`'s 64 Gemini sessions. Document A §1 says "`model_specific` is a judgement, not a measurement, except where an in-session or same-machine control existed. Three such controls exist." That is false, and it under-sells the strongest claim in either document.

**2. It settles §2.8's delegation finding as a measurement, and A hedges it into weakness.** Like-for-like on sessions invoking ship-fleet/ship-feature/shipyard: **19 of 22 Claude sessions spawned agents (1,531 Agent+Workflow calls); 1 of 8 Gemini sessions did (7 calls).** Corpus-wide it is 1,631 vs 7. Document A's caveat — "in at least one session the Agent tool may not have been exposed … the finding is therefore 'the mechanic did not run' rather than 'the model refused to spawn'" — and its `[measured-family]+[derived]` tagging in §5.6 are both unnecessary. This is `[measured-family]` with a control arm, and it is the cleanest family delta in the whole corpus. Same file gives `AskUserQuestion` 300 (Claude) vs 6 (Gemini).

**3. The same file refutes the glyph probe in the opposite direction from both documents.** Claude control: 1,146 ok / 38,664 miss (2.9%). Gemini: 366 / 900 (29%). Gemini carries the marker **ten times more often** than Claude in the same harness. A §3.7 and B §7 both call it "inconclusive" and shelve it; the honest statement is that it is a measured non-signal, and any future reader will otherwise re-derive it as a Gemini defect. (Both arms use `extract.py`'s known-bad all-text-blocks population, so the ratio is valid and the absolute rates are not.)

**4. Document A's headline structural edit rests on a cherry-picked count.** §6's table ("ships a runnable command — fired 4/4; asks for an authored table — 0/~8") and §5.11's new SKILL.md rule both fall apart against `armada-sync/gemini.md`, which I read: Override 4's stamp readback is the literal command `date +%F`, and the conformance report records *"Neither ran `date +%F`"* — 0/2. Override 2's bound readback is likewise a literal command (`awk '/^### <p> /,/^### [^<]/' ARMADA.md | wc -l` plus three counts) and fired 0/3, which is precisely the S15 fabrication A itself reports at §5.9. Counting all command-shaped overrides gives roughly **6 of 11, not 4 of 4**. The rule may still be right; the evidence as stated is not, and A is shipping the overstatement it exists to correct (§2.1). Recount before §5.11 lands.

**5. Document B duplicates `stocktake` almost wholesale and never says so.** `stocktake` already: rebuilds the numbered requirement list from the ticket *before* opening the diff; runs `scripts/locate_work.sh` for merged / built-unmerged / built-unpushed / not-built (= B's R6, R8); routes to `spec-validation`'s REAL/AUTHORED/MOCK producer trace (= B's `inert` class); judges "whether the tests could fail" via `references/testing-adequacy.md` (= B's R9); owns state in `scripts/board_ledger.py` with `init / next / record` (= B's `worklist.py init/next/set/check`); grades out of family with a single strongest judge; and carries `--verdict ungraded` — *"a card the method never ran on … sits outside the needs-work count"* — which is B's `degraded` plus its budget-exhausted outcome. B cites stocktake once, for a single sentence in §6, and never addresses the overlap. Either `tailings` is a transcript-fed front end that hands cards to stocktake, or the two need merging; as specified it is a second implementation.

**6. B's R9 duplicates `warrant:assay`, which owns exactly that scan.** `assay`'s SKILL.md line 41: *"Scan for assertions that cannot fail … an assertion with no matcher, a matcher comparing a value to itself, a swallowed exception"*, with mutation survival behind it. B should call `assay` rather than ship a regex, and `spec-validation` rather than derive `inert` from scratch — B never names either skill.

**7. Neither document has a home for `instruction-violation`, the second-largest category (24 of 148).** B §5's universe is "the session's assertions", plus *gates* the session was told to run. A brief-level directive that is not a gate has no class: `governor-run` wrapping (5 sessions, 0/85 and 0/~20), the per-call `failover-timing.jsonl` check (3 sessions), `SKIP_GATE=1` ×10, `git commit -m` against a pinned `-F` rule, "read these three docs IN FULL", worktree isolation. `unrun` covers gates only. Add a ninth class or widen `unrun`'s definition to "an instruction the session was given and did not follow, with no substitute and no disclosure".

**8. Neither document mentions security defects, and the corpus has two.** S16 shipped `apps/coordinator/src/mcp/auth.ts:15` with `|| 'dev-secret'` as a JWT signing fallback, reading the wrong env var name, in a repo whose own `auth/secret.ts` throws in production and is tested for it — anyone can mint a `transfer_session` token. S07 wrote a GitHub PAT to disk in cleartext via a `wsl.exe` command line, visible in the process list. Both are exactly what a genuinely out-of-family reviewer catches and an in-family one did not (§3.1). B's classes cannot express "the shipped code has a hole"; a `security-review` skill exists and should be composed with rather than ignored.

**9. A second unclassified shape, in two sessions: the session built a duplicate of a module the repo already had wired.** S16's `cloud-bringup-planner.ts` (150 lines, imported only by its own test) against the existing, routed `bringup-plan.ts` (344 lines) — `git grep bringup` would have found it. S14 copy-reimplemented `named_pipe_sddl` from `crates/ipc-host/src/windows_pipe.rs` **while the brief named that file as a settled decision**, so the real `CreateNamedPipeW` path is never called. Not `inert`, not `unbacked` — real code, real tests, wrong. One-line probe (new exported symbol name grepped across the repo) and one new class.

**10. Document B has no probe for quota-collapse, which Document A retains.** A §2.4 keeps C1 as "a minority failure but the mechanism is confirmed", citing S07's *"5 Surfaces × 2 Operating Systems"* over two pairs ever actuated (raised to critical) and its dropped `states`/`menus` axes. B's T11 catches only *printed-figure vs reported-figure* elision; nothing compares a brief's categorical scope against the count delivered. Add: tokenise the noun list after "all"/"every" in the brief and require a count per token — with the audit's own warning that substring matching gives a false pass (`MenuBarExtra` cleared "menus").

**11. Corpus gaps a reader would expect covered.** Zero sessions exercised the `warrant` family (`assay`, `oracle`, `ratchet`, `panel`, `ledger`) — the newest, most gate-heavy plugin set, all of which ship `gemini.md` — nor `vouch`, `stocktake`, `code-review`, `mockup-fidelity`, `spec-validation`, or `geminify` itself; `be-my-witness` was named repeatedly and never invoked; `defer` never invoked at all. Closing it means running one Gemini session per warrant sub-skill and scoring against the same probes. Second gap: all 13 repos are the user's own greenfield projects with ORCHESTRATOR/LEDGER conventions — B's R1, R5, R6 and R8 assume those files exist, and nothing establishes the pass works on a repo without them. Third: no Pro-tier session (A discloses this; B does not, though B §9's "which frontier model runs it" question is adjacent).

**12. A's category distribution is pre-refutation and is presented as the specification's foundation.** The `by_category` counts A quotes at §1 and builds §5.3's reordering on are the original labels; refutation explicitly recategorised at least four (S08's codex-lane finding `fabricated-verification` → `gate-skipped`, S05's agy finding `gate-skipped` → skill-design defect, S14's commit finding → `instruction-violation`). The 55%/6% split survives the moves, but say the counts are pre-refutation or recount them.

**13. No material contradiction between the two documents.** They agree on the out-of-family failure, on the marker being unusable, on `lane_pick.py` never running, and on the delegation direction; B's §9 open questions are consistent with A's §1 method limits. The only tension is coverage, not claim: A retains C1 and B has no probe for it (item 10), and A treats `bounded-constraint` as scoped to rendered artifacts while B has no bounds probe at all.